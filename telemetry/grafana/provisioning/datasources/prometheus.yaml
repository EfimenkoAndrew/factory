# Grafana file-provisioned datasource (spine AD-6 — dashboards-as-code).
apiVersion: 1

datasources:
  - name: Factory Prometheus
    uid: prometheus-factory
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 15s
