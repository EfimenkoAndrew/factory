# Known Issues — engine registry

This is the factory's **append-only** registry of known issues, limitations, accepted
constraints, and by-design behaviours. Read it before changing the engine; add a `KI-*`
row in the SAME change that introduces or fixes an issue. A change that adds a limitation
without a row here is the silent-divergence failure mode this registry exists to prevent.

The lib self-test (`node _workflow/lib/_selftest.mjs`) is the regression net for the
correctness-class entries — keep it green.

> This shipped registry documents the **engine invariants** only. Your own operational
> findings (per-cycle issues, host-specific quirks) accumulate here as you run the factory.

## Engine invariants & operational constraints (by design — not bugs, but must be known)

| ID | Constraint |
|----|-----------|
| KI-E1 | **No mutating git, ever** — no `commit`/`add`/`checkout`/`restore`/`stash`/`reset`/`clean` by the factory or any subagent. Fixes land on `factory/<id>` worktree branches as UNSTAGED changes; **the human commits**. |
| KI-E2 | **The Workflow runtime cannot touch the filesystem or git, cannot `require()`, and `Date.now()`/`Math.random()`/argless `new Date()` throw.** Hence the two-half split: `driver.mjs` (Node — fs/git/ledger) + `factory.js` (Workflow — agents only). Pure helpers used inside `factory.js` are inlined byte-for-byte from `_workflow/lib/`; change both copies together. |
| KI-E3 | **The filesystem is the checkpoint** (`state/ledger.json` + worktree inspection) — resumable after a kill. The ledger has a **single writer** (the driver); hand-editing it risks corruption. The findings-graph is hand-editable. |
| KI-E4 | **Scope red-lines are hard stops** — an item whose only fix would cross a scope boundary you have declared MUST `scope-stop` (→ the human queue), never be "fixed" by adding the forbidden surface. |
| KI-E6 | **Graceful-stop drain guard** — a `state/STOP_REQUESTED.md` marker puts the factory in a graceful drain: `driver.mjs group` refuses while it exists (in-flight lanes finish, nothing new launches). `fold`/`reconstruct`/`escalations` stay unguarded so running work completes. Resuming is deliberate: delete the marker (or pass `group --stop-override`). |
| KI-E7 | **Telemetry is observational, never evidentiary.** Every factory action lands as an event on the append-only stream `telemetry/data/events.jsonl` (`FACTORY_TELEMETRY=0` disables). An emitted event never feeds a fold verdict; the factory runs fine with the telemetry stack down. `driver.mjs telemetry-report` renders the evaluation report from the JSONL directly. |
| KI-E17 | **Portable mounts** — the factory is mountable at any host path (submodule or plain clone). `_workflow/lib/rootfind.mjs` detects the host repo root (walk up from the mount's parent to the first `.git`; `FACTORY_REPO_ROOT` overrides) and rewrites the committed stock-prefixed config paths onto the real mount; an optional gitignored `config/factory.config.local.json` overlays per-host knobs. See `SETUP.md`. |
| KI-E18 | **AcceptanceScan** — a pre-band clause-coverage probe: the item's `acceptance` splits into checkable clauses (`lib/acceptance.mjs`, inlined byte-identically in factory.js — change both together, the selftest pins parity); a cheap probe answers per clause "does the diff carry concrete evidence?" BEFORE the gate band. Gaps feed ONE bounded fixer amend + one re-probe; still-uncovered clauses FAIL pre-band with clause-level gateDetails. Skipped for verificationOnly + single-clause acceptances; fail-open on a null/malformed probe. **Fix (multi-lens review, ported from a host-mount session, 2026-08-25):** the ACCEPTANCE-GAP AMEND prompt explicitly offers the fixer a note-only response ("or state in note precisely why a clause is already satisfied or out of this item's scope"), but the re-probe fired ONLY on `amend.applied` — a fixer taking that offered option got the item failed anyway on the stale pre-amend verdict, never re-evaluated. The re-probe now also fires on a genuine note-only response (non-empty `note`, no code change), explicitly feeding the probe that explanation and instructing it to judge the explanation critically rather than rubber-stamp it. Found and fixed as the twin of the identical bug independently introduced into KI-E87's sibling amend flow when it was ported here (see that entry) — this bug predates KI-E87 and was inherited when KI-E87 mirrored the pattern. |
| KI-E19 | **Evidence manifest** — `build-test.sh` trails every subcommand with a keyed `FACTORY::SUMMARY::<sub> …` marker (the suite carries its own counts); `parseVerifyRaw` prefers the keyed markers over the ambient type-agnostic dotnet summary line, so transcript append order can never misattribute counts. The fold adds a FULL-band PAIR rule: a FULL code item must show a green build marker AND a green suite signal in at least one transcript (results without `band` skip the rule — backward-compatible). |
| KI-E20 | **`driver recover <id>`** — the direct-recovery scaffold for a FAILED/ESCALATED item: dissent digest from the checkpoint's structured gateDetails, one delta re-gate prompt per dissenting role (role brief inlined), the evidence contract (integrate-raw vs mutation-proof separation + red-marker rules), the `#<cycle>r` fold skeleton (attemptsDelta:0; machine-evidence flags carried so recovery is never a lighter evidentiary path — review fix: `band` is among the carried flags, so a FULL-band prior arms the KI-E19 build+suite pair rule on the recovery fold too), and a `recovery_prepared` telemetry event. Prepare-only: the operator applies, re-gates, folds. Review fix: the fold override announces a gone/unreadable worktree (`debris/P9 diff checks SKIPPED`) instead of silently skipping the diff checks — transcript-only evidence is a visible state. |
| KI-E21 | **Sweep routing in `suggest`** — a cluster where one signature keyword spans >= 6 members (`--sweep-min` overrides) is recommended to the SWEEP band (`cluster.mjs --emit-pattern` + `driver sweep`) instead of pair-group lines — design-once + cheap applies + one pattern gate is measured ~4-10x cheaper per finding than pair lanes. |
| KI-E22 | **Acceptance-surface lint** — `graphaudit.mjs acceptanceSurfaceGaps`: path-like tokens + PascalCase type names in `acceptance` that resolve to a tracked repo file absent from `files[]` → advisory WARN at `graph-audit` and at `group` time (the file-lock cannot serialize an unlisted surface, so the fixer can be lock-forbidden from meeting acceptance). `graph-audit --fix` also appends the standards-ledger path for flagged shared-file gaps (the KI-E16 mechanical triage). |
| KI-E23 | **Telemetry completeness** — editorial verdicts are recorded in the gates map + gateDetails (advisory stays non-blocking); results and `item_folded` events carry `band` (LIGHT/FULL cost + gate-value split); the factory returns `usage` from the runtime budget counter and the fold emits a `usage` event, so cost analyses stop extrapolating from call counts. |
| KI-E24 | **`driver decisions-digest`** — one ranked page (severity x age) for the parked owner queue, with a one-line reply format (`<ID>: <letter>`) and same-target rule-together bundles; hand the digest to the owner instead of the raw queue wall. |
| KI-E40 | **The telemetry report counted only the literal `APPROVED` — 100%-passing reaudit/adjudicator/regate/direct-recovery rows rendered 0%, and KI-E23 usage events were never rendered.** The fold gates map carries role-specific ok-vocabularies (reaudit `code=ok security=ok`, adjudicator `UPHELD`, adjudicator-regate `CONFIRMED`, direct-recovery `converged-remedy …` prose); `renderTelemetryReport` counted `APPROVED` only, so those gates read as 0% ok — a poisoned quality signal (live: reaudit 0/11, adjudicator 0/7, both actually 100%). Fix: `verdictOk()` classifier in `lib/telemetry.mjs` (APPROVED/UPHELD/CONFIRMED, `converged-remedy` prefix, all-`key=ok` families) feeds the table; vocabulary the classifier does NOT recognize counts not-ok AND self-reports in a footnote (drift is visible, never silently 0%). `OVERRULED` joins the known-fail set deliberately — the adjudicator row's Ok-rate reads as the dissent-upheld calibration. Also: `usage` events aggregate + render (`## Fold-time token usage`), and the failure-concentration section carries a reading note (final=checkpoint = late-failure spend, not a checkpoint crash). Report-side only — fold evidence and emit paths untouched (KI-E7). |
| KI-E41 | **KI-L65 main-contamination detection ran only at FOLD — a relaunch after a killed run re-ran the full band against poisoned main** (witnessed cycle 47: the dead run's fixers had leaked fix variants — one factually wrong — into the MAIN tree; the sanctioned verbatim relaunch burned 2h+ of band before the fold detector fired). Fix: `resume` diffs each relaunch candidate's claim-time `main-snapshot.json` BEFORE printing the `Workflow({scriptPath})` launch line — dirty drift prints a loud ⚠ REPAIR-MAIN-FIRST block per item (committed-only drift prints the KI-E35 ℹ human-delivery variant). Detection-only, never blocks (same posture as the fold check: repairing main is operator judgment). A check that itself fails (corrupt snapshot, git error) ANNOUNCES `MAIN-GUARD <id> SKIPPED` — silence never reads as clean (review fix). |
| KI-E42 | **Killed-run debris survives into the relaunch artifact dir and misleads the relaunch's agents/reporters** (witnessed cycle 47: a dead attempt's stray `RESULT.md` — "false positive — already fixed, no action taken" — was read mid-relaunch; two gates burned findings on the debris). Canonical per-item artifact vocabulary = `STAGE_ARTIFACTS` + the `gate-*/review-*.md` patterns + the driver control files (`nonCanonicalArtifacts()` / `CONTROL_ARTIFACTS` in `lib/telemetry.mjs`). `resume` detects non-canonical files on every relaunch candidate and prints them; `resume --quarantine` moves them into `state/items/<id>/quarantine-<ts>/` (a plain `resume` stays read-only; directories are never touched; nothing is deleted). A scan that itself fails announces `DEBRIS-CHECK <id> SKIPPED` — never silently reads as clean (review fix). |
| KI-E43 | **LIGHT-band integrate false-fails on a Docker-less host — `baselineFailures` folds empty while the integrate full suite sees environmental failures** (witnessed cycle 47: ITEM-H15, 6 pre-existing Docker-unavailable Testcontainers failures vs baseline 0 → a 10/10-APPROVED item overridden to FAILED). Root cause: the LIGHT-band verify brief deliberately SKIPS the full suite (speed), so the runner's `baselineFailures` is structurally empty, yet the fold override compares the integrate suite's failure count against it. Fix — baseline capture, NOT an override weakening: (1) the RED brief instructs a pre-fix full-suite baseline capture (`state/items/<id>/baseline-raw.txt`) when `docker info` FAILS on the host — the tree is still unfixed at RED time, so the capture can never launder a fix-broken test; (2) the checkpoint prefers the RED-stage names (`test.baselineFailures`) over the verify-stage report; (3) the fold override merges `effectiveBaseline(reported, parse(baseline-raw.txt))` — the LARGER count wins, and failures beyond it stay regressions. A green baseline transcript contributes 0 (no free failure allowance). **reFix fence (review fix):** "the tree is still unfixed at RED time" holds only on a FIRST attempt — on a reFix (prevState FAILED/ESCALATED) the worktree already carries the prior fix, so the fold trusts the transcript only when its mtime PREDATES the reFix claim (a re-capture is ignored loudly) and the RED brief tells the reFix test-author not to re-capture. |
| KI-E44 | **`reconstruct` lost the run-level token usage — a reconstructed fold emitted no KI-E23 `usage` event.** Checkpoints carry no usage (factory.js reports the run total only at RETURN time, which a killed run never reaches), so a killed run's output tokens were visible only in the harness task output — invisible to cost telemetry. Fix: `reconstruct --usage-tokens <N>` stamps `payload.usage.outputTokens` into the reconstructed results file; the fold's existing KI-E23 usage emit then fires exactly as on the live path. Without the flag, reconstruct prints a loud hint naming the gap. Observational only — never affects a verdict. |
| KI-E45 | **The KI-L65 main-snapshot covered only `files[]` — contamination on an acceptance-named file OUTSIDE the lock set was invisible to every fold-time drift check** (witnessed cycle 48: ITEM-H16's lane wrote an acceptance-named CONTEXT.md in MAIN — the fold flagged 3 other lanes' drift but was structurally blind to this file; only a manual broad `git status` sweep caught it). The KI-E22 probe had predicted exactly this hole at group time. Fix: `group` retains the KI-E22 acceptance-resolved paths per item (`gapsByItem`) and the claim-time `main-snapshot.json` now hashes the UNION of `files[]` and those paths — the fold's KI-L65 re-hash and the KI-E41 resume main-guard both inherit the widened net for free. Detection-only widening; the lock set itself is unchanged. |
| KI-E46 | **The direct-recovery KPI recognized only the legacy gates-map signature — the factory's DOMINANT close path was invisible.** The KI-E20 `recover` scaffold folds with resultId `<id>#<cycle>r` and a note starting `direct-recovery`; only pre-scaffold hand-rolled folds carried a literal `direct-recovery` gates key. The KPI counted the key alone, so every scaffolded recovery close read as a plain re-band close — live: `Direct-recovery rate 4/29 = 14%` rendered while the true figure was **13/29 = 45%**. Fix: pure `isRecoveryResultId()` (`#Nr`/`#NrK`) + `isDirectRecoveryFold()` (emit-time `direct` stamp / gates key / recovery resultId / prefix-anchored note — a mid-note mention never classifies) in `lib/telemetry.mjs`; `aggregateEvents` classifies with it; the fold stamps `attrs.resultId` + `attrs.direct` on `item_folded` so future streams never need prose sniffing. |
| KI-E47 | **Recovery folds polluted the derived stage-duration authority.** A recovery fold re-derived the artifact-mtime timeline over the item dir, so the operator-paced remedy/re-gate window folded in as fake band durations (live: one item's 4 re-gate rounds over hours contributed to the `gates` p95 nearly doubling, 3692s → 6549s). AD-12's duration authority measures BAND-paced stage work only. Fix: the fold derives + emits NO stage timeline for a recovery result; the recovery's telemetry is its `item_folded` record (with the KI-E46 stamps). Historical polluted events stay in the append-only stream and age out of relevance — disclosed here. |
| KI-E48 | **Two report sections were structurally dead.** (a) The band-split KPI (FULL vs LIGHT first-pass) had data since `band` stamping (KI-E23) but was never rendered. (b) "Stage durations — agent-reported" rendered `_none_` forever: agents never pass `--durMs` (they cannot know their own wall-clock), and the aggregation only consumed `durMs`. Fix: (a) `renderTelemetryReport` renders per-band first-pass rows keyed by the band of the item's FIRST fold; a pure-legacy (unstamped) stream renders no rows. (b) `aggregateEvents` pairs each agent `stage_start`/`stage_end` per item+role and derives the duration from the two `ts` stamps (latest-start-wins for retries; gap-fenced; skipped when `durMs` is present) — an independent best-effort cross-check of the mtime-derived authority (AD-12 unchanged: still non-evidentiary). |
| KI-E49 | **Agents free-typed `--event` names into the stream** (live: `probe`, `dummy_probe`, `tool_use` — one review agent used it as a running commentary channel). The AD-10 agent seam defines exactly two events; unbounded vocabulary pollutes `byEvent` and invites drift. Fix: `clampAgentEvent()` in `lib/telemetry.mjs`; `telemetry-emit.mjs` clamps any non-canonical `--event` to `agent_note`, preserving the original name in `attrs.origEvent`. Bounded vocabulary, breadcrumb kept, still always-exit-0. |
| KI-E50 | **Main-tree contamination detection ran only at FOLD (KI-L65) and RESUME (KI-E41) — a mid-band write stayed invisible for hours while sibling agents read the poisoned tree** (witnessed cycle 48: 4/6 lanes wrote main during the band). Fix: new read-only, lock-free, lease-free `driver main-check <id> […]`: re-hashes each item's claim-time `main-snapshot.json` against MAIN (reuses the `mainguard` drift + KI-E35 committed/dirty split), prints ⚠ MAIN-DRIFT / ℹ committed-delivery / clean, always exits 0. The `factory.js` verifyHint has the runner execute it between verify and the gate band and paste any ⚠ line VERBATIM into its note — the gates + checkpoint see drift the moment it exists. WARN-ONLY by the same posture as fold/resume; prevention (sandboxing main read-only vs failing the lane fast) remains a host-owner policy decision the engine deliberately does not make. |
| KI-E51 | **"Fix-introduced defects" — errors the fix's OWN additions carry — became the #1 rejection class and no brief targeted it** (live: 3/4 of one cycle's FAILs + the next cycle's only FAIL: a countable claim the tree grep-disproves; a false cross-reference clause; narrative comments in the fix's own test file). The pre-band probes are RIGHT to pass these (clause coverage / deferral scans target different classes); only the adversarial band catches them, at full FAILED-round price. Fix, prompt-level, zero new agents: `fixer.md` **COUNT-CLAIM SELF-CHECK** (every countable/enumerable claim the diff adds is re-derived by grep/ls before finishing, derivation quoted in fix.json) + `test-author.md` **COMMENT POLICY** (no narrative comments in the new test file). Complements the deterministic KI-E11 path-claims lint, which covers paths but not counts. |
| KI-O1 | **`_workflow/opencode/` — an alternate runtime binding for a controlling session with no native `Workflow` tool** (e.g. OpenCode/other agentic CLIs — KI-E2's split assumed the controlling session IS Claude Code; this is the first binding for one that isn't). `factory.js` cannot run outside Claude Code's sandboxed Workflow runtime (it needs that runtime's `agent()` primitive); this directory re-implements the DETERMINISTIC half of `runItem()` as a real Node CLI (`runtime.mjs`) — routing/band/flow selection (`routing.mjs`, verbatim port), prompt composition (`compose.mjs`, verbatim port, reads the real `agents/*.md` briefs directly since this runs unsandboxed), structured-output schema validation (`schemas.mjs`, a minimal zero-dep JSON-Schema-subset validator, byte-checked against every `*_SCHEMA` const in `factory.js`), and build/test execution (`buildtest.mjs`, spawns the SAME unmodified `verify/build-test.sh` — no reimplementation — and **imports** `lib/verify.mjs`'s transcript parsers + `lib/preflight.mjs`'s docker/dotnet probes directly rather than re-deriving them, so there is zero drift risk from what the fold/preflight actually use). The controlling session drives one item at a time through a checkpoint protocol (`init`/`next`/`submit --role <key>`/`mech`/`status`/`finalize`) — `next` returns either a MECHANICAL step (run it directly, no LLM) or one-or-more AGENT steps (dispatch via the session's own subagent-launch tool, e.g. OpenCode's `Task`, in parallel when `next` returns more than one — mirrors `Promise.all` pooling), `mech checkpoint` writes a `state/items/<id>/result.json` in the same shape `driver.mjs fold` validates, and `finalize` wraps it into the `{mode,cycle,results:[...]}` envelope `fold` actually requires (a bare per-item file folds as "ZERO results" otherwise — a real stumble the first validation pass hit, fixed here rather than left as an operator trap). **Validated twice, end to end, on non-production .NET fixtures** (never against a real product item): (a) a hand-crafted-JSON pass through every phase including the adjudicator/re-gate path; (b) a REAL subagent-dispatch pass (`Task`, `general` subagent) through test-author → fixer → `mech verify` → review-edgecase → **a genuine CHANGES_REQUIRED** (the reviewer independently built a scratch harness and found a real regression: negative-length input now throwing where the pre-fix code didn't) → bounded amend → re-scan convergence → gates → PO → re-audit → `mech integrate` → `CLOSED`, with `driver.mjs fold` correctly rejecting the finished result for the expected reason (`unknown-id` — a non-graphed test item) and zero ledger side effects either time. **Documented fidelity gaps, not silently papered over:** (1) **no per-call model tiering** — `factory.js`'s `agent(prompt, opts.model)` selects opus/sonnet/haiku/fable-5 per role; a session without that capability (e.g. OpenCode's `Task` tool, which only selects a `subagent_type`, not a model) runs every role on whatever model backs its subagent — the RT/FLOW_RT tables in `routing.mjs` are kept as intended-routing metadata only, and `res.cost` records the actual session model used under a distinctly-named bucket, never claims the intended tier ran; (2) **no sweep-mode** (`cmdSweep`'s cluster-fix path) and **no true multi-item batch parallelism** — this binding drives ONE item at a time, by design, matching an operator-driven session's natural cadence. **Fixed during hardening (not left as gaps):** the decision-framer is now a REAL dispatched phase (`decision_frame`, `DECISION_SCHEMA`) — `frameAndBlock` no longer short-circuits straight to BLOCKED, matching `factory.js` exactly; `LEFTOVER_SCHEMA`'s `punt.line` was typed `number` in this port's first draft but is a STRING in the real schema (would have wrongly rejected a valid subagent response — caught by diffing against `factory.js` directly, not by inference); `ACCEPT_SCHEMA`/`LEFTOVER_SCHEMA`/`DECISION_SCHEMA`'s nested item schemas now carry the real `additionalProperties:false`/`required` too; `mech verify` now REFUSES a codeChange item on a non-FULL band with no `--filter` (previously fell through to a build-only run that `verdictFromParse` still reports as "tests green" — a silent false-green risk, since build-only machine evidence is indistinguishable downstream from "test ran and passed"). **Dry-run against a REAL production item (2026-07-28) surfaced + fixed one more gap:** `compose()` only told a dispatched agent to check for pre-existing worktree changes when `reFix` was true, but a shared/reused worktree can carry uncommitted work from a killed run even when `reFix` computes false (exactly this item's state: `prevState` was `READY` at the last `group` claim, so `reFix=false`, yet the worktree still held the killed cycle-4 attempt's uncommitted diff) — a dispatched agent could be confused finding unexpected changes with no cue they might be relevant. Fixed: an unconditional WORKTREE STATE CHECK guardrail line (always run `git status --porcelain` + `git diff HEAD` in the worktree first, regardless of reFix). Also closed a real coverage gap the same dry run exposed: every prior validation run used a LIGHT-band item, so the FULL-band 5-gate selection and the realInfra marker-probe FAILED path (`needsRealInfra:true` + a build+suite that genuinely passes but carries no `FACTORY::REALINFRA::` marker) had never been exercised. Both now validated: `gateRolesFor('FULL', ...)` pinned in `_selftest.mjs`, and the marker-probe FAILED path proven live against a real .NET project (build clean, suite green, correctly FAILED anyway for the missing container marker — the exact fail-closed behavior a `realInfra:true` production item depends on). **A follow-up review pass (this same change) fixed lifecycle divergences in the port:** edge-case-hunter verdicts are blocking again; the resultId cycle stamp matches the driver's `cycle+1`; `next` is truly idempotent and completed phases reject stale re-submits; `realInfraLikely`/pureCoverage match factory.js exactly; the escalate tier parks only AFTER refute+re-audit; the PO gate respects the band's gate set; the P10 codeChange refinement is implemented; count markers parse anchored/last-match; `extractJson` takes the LAST fenced block; FAILED/BLOCKED/ESCALATED terminals write result.json via checkpoint; and doc-only items skip the dotnet build. A schema-parity selftest now diffs `schemas.mjs` against factory.js's `*_SCHEMA` consts mechanically, and the opencode suite is invoked from the main selftest so the release/install gates cover it. |
| KI-O2 | **`FIX_SCHEMA.divergence` rejected the exact shape `agents/fixer.md` tells fixers to return.** The brief's own "Write + return" section says `divergence (null or {rule, ledgerAnchor})` — an OBJECT — but `FIX_SCHEMA` in `factory.js` typed the field `['string','null']` only. Live-caught 2026-07-28 (a real production item, real production run): a fixer subagent followed the brief exactly, returned `divergence:{rule,ledgerAnchor}`, and the structured-output layer rejected it (a real `agent()` StructuredOutput-retry-cap class failure in the native pipeline too, not just this port's validator — the same schema gates both). Confirmed nothing downstream ever reads `.divergence` programmatically (`driver.mjs`/`lib/*.mjs`/`factory.js` all grepped clean) — it is purely informational for human fold review — so the fix widens the schema (`['string','null','object']`) to match the brief's own contract instead of narrowing the brief to match an accidentally-stricter schema. Fixed in both `factory.js` (canonical) and `_workflow/opencode/schemas.mjs` (KI-O1's `FIX_SCHEMA` copy) in the same change. |
| KI-O3 | **`mech verify`/`mech integrate` silently tested the WRONG tree when given an absolute path outside the item's worktree.** Live-caught 2026-07-28 (a real production item): passed an absolute path to the host's MAIN repo instead of the item's linked-worktree copy — the main repo builds and tests fine on its own (unrelated to the fix), so `mech verify` reported a false green having tested the UNFIXED tree entirely; the new test genuinely didn't exist there ("No test matches the given testcase filter", a VACUOUS pass at exit=0 that the P3/verdictFromParse layer cannot distinguish from a real one on its own). Two related fixes in `runtime.mjs`'s `cmdMech`: (1) a bare/relative target (e.g. `src/<Solution>.sln`) now resolves against `ctx.worktreePath` automatically — removing the #1 way to hand-type the wrong absolute path; (2) an absolute target outside the item's worktree prefix now prints a loud WARNING (not a hard block — a deliberate cross-tree check is occasionally legitimate) instead of silently proceeding. Same KI-O1 dry run also caught a SECOND, compounding bug: FULL-band `mech verify` ran `suite` only (see the FULL+filter fix logged in this same entry's history) — both bugs together meant a FULL-band realInfra item's marker-probe FAILED for two independent reasons simultaneously (wrong tree AND wrong-shaped verify), which took two real re-runs to fully isolate and fix. |
| KI-E52 | **Versioned releases + team install/upgrade + per-developer telemetry bootstrap.** Before this, the factory had no version identity (no tags/releases), onboarding was the manual SETUP.md ritual, upgrades were ad-hoc pulls with no safety gate, and the per-developer observability infra (the compose stack + the KI-E33 session cost-telemetry env) was left to each developer — the env opt-in failed silently on real hosts three sessions in a row. Now: `VERSION` + `CHANGELOG.md` + `setup/release.sh` (maintainer: selftest-gated cut — bump, changelog prepend, annotated `vX.Y.Z` tag, push, GitHub Release) and `setup/install.sh` (team: curl-able one-liner). `install` clones at the latest release tag (falls back to `main` with a notice while no releases exist), runs `setup/init.mjs`, gates on the selftest, and bootstraps telemetry; `upgrade` is IN-PLACE and **selftest-gated with automatic rollback** to the previous ref on red — per-host state (`state/`, `reports/`, `queue/`, `telemetry/data/`, `telemetry/.env`, `*.local.*`) is gitignored and structurally untouched, making `upgrade --yes` unattended-safe (cron-able); `status` compares installed vs latest; `telemetry-up` writes the per-host `telemetry/.env` (+ now-gitignored — E2E-caught), generates `claude-code-telemetry.env` with the stack's OTLP port, merges the env block into the host `.claude/settings.local.json`, optionally installs a marker-guarded shell-profile block (the reliable KI-E33 path — some runtimes do not forward `OTEL_*` from settings env), starts the compose stack idempotently and health-checks Prometheus. A vendored (.git-less) mount is refused, never half-upgraded. E2E-tested end to end via the in-tree hermetic harness `setup/_e2e.sh` (local fixture remote, no network: broken-tag rollback, transactional-install cleanup, PR-only-main release fallback, stale-cut guard; an explicit `--host` beats script self-location — a redirect bug the E2E caught). **Deep-review hardening (2026-07-27):** release.sh verifies local main == origin/main BEFORE the cut and pushes `--atomic` (a rejected `--follow-tags` branch push still published the tag — demoed), then falls back to tag + `release/vX.Y.Z` branch + PR when main is PR-only (direct pushes blocked by ruleset — the factory's own repo is configured so); a failed install removes the mount it created (no "already installed" dead-end with init never run) and a telemetry failure never fails a good engine install; upgrade short-circuits when already current, warns on a downgrade, re-runs `setup/init.mjs` (host skill/scaffolding refresh), and a FAILED rollback announces itself; latest-release resolve dies loudly on an unreachable remote (never a silent channel-switch to main) and filters to strict `vX.Y.Z` (an rc/date tag never wins); an existing-but-unparseable host `settings.local.json` is REFUSED, never clobbered; hand-edited `telemetry/.env` files with missing keys fall back to defaults instead of dying under pipefail; shell-profile markers are mount-scoped (KI-E25 second stack). |
| KI-E39 | **Telemetry stack as a host-compose profile (documented alternative to the standalone project).** `telemetry/compose-profile.example.yaml` + a README section: the four services fold into the HOST repo's `docker-compose.yaml` under an opt-in `factory` profile — a dedicated network with aliases `exporter`/`otel-collector`/`prometheus` keeps this stack's configs byte-for-byte unmodified, no `container_name` (compose project scoping isolates hosts, so the KI-E25 collision class disappears), and host-owned ports are remapped (example: OTLP 4417/4418, Grafana 3100). Proven live in a host integration (3/3 Prometheus targets up, dashboard provisioned, session `claude_code_*` metrics flowing at the remapped port). The session env must then point at the REMAPPED OTLP port — `claude-code-telemetry.env.example` says so. |
| KI-E38 | **Real-shape seeding: a minimal test seed greens write paths that throw on real rows.** Live: a backfill endpoint's 5/5-green integration tests seeded a JSON-owned aggregate WITHOUT one of its optional owned collections; real rows carried it, and the apply path threw EF's `'…__synthesizedOrdinal' is part of a key` (whole-aggregate replacement re-parents owned-collection children) — after every gate had approved. Rule added to `test-author.md` (REAL-SHAPE SEEDING: populate every optional collection/nullable member the model declares, or justify the omission in the note) + a matching `review-testreview.md` quality check. Prompt-level only — no machinery change. |
| KI-E37 | **`gc` sweeps worktree-scoped docker-compose projects.** Agent/verify runs can `docker compose up` inside a worktree (default project name = worktree dir); removing the worktree orphans the project — live: 12 Created containers + 3 named volumes outlived their worktree. `gc` now lists (dry-run) / `down -v --remove-orphans`s (`--yes`) every compose project whose ConfigFiles ALL sit under the CONFIGURED worktrees root (`abs(cfg.paths.worktreesState)`, separator-anchored prefix per comma-joined entry — review fix: sibling dirs and hybrid host+override projects can never match; `worktree.parseComposeLs` + `strayComposeProjects`, behaviorally selftest-pinned). Best-effort: docker absent (ENOENT) → silently skipped; any OTHER enumeration failure prints a one-line SKIPPED notice (review fix — silence must not read as "no strays"). |
| KI-E36 | **The queue hints when a parked item was likely delivered in HEAD.** Delivered-in-HEAD detection existed only at graph seeding; an item ruled + hand-delivered MID-RUN sat BLOCKED with its acceptance already satisfied in HEAD (live: HOST-ITEM-A — output committed by the operator while the ledger still said BLOCKED). `escalations` AND the KI-E24 decisions-digest now carry a per-item hint when EVERY `files[]` entry has a commit newer than the park baseline — the history entry that ENTERED the parked state (`ledger.parkedAtMs`; `updatedAt` moves on later row writes — review fix), with the date logic pure + selftest-pinned (`ledger.allCommittedAfter`): "possibly DELIVERED in HEAD — verify, then `driver recover <id>`". Advisory-only (git log timestamps); never a state change; a corrupt graph disables hints with a stderr notice instead of silently (review fix). |
| KI-E35 | **Fold's KI-L65 warning distinguishes human-committed delivery from agent contamination.** KI-L65 always shouted "an agent likely wrote outside its worktree — inspect + repair", which is exactly wrong for the legitimate case of the operator committing the item's output mid-run (live: it fired on the intended human delivery of HOST-ITEM-A). The factory never commits — so drift on a path `git status --porcelain` reports clean (committed; not edited, not untracked) came via human commits: now reported as `ℹ COMMITTED DELIVERY … verify intent, no repair needed`; ANY uncommitted state — a dirty edit OR an untracked NEW file (invisible to `git diff HEAD`; the live ITEM-H5 stray-contamination shape — review fix) — keeps the ⚠ contamination wording (`mainguard.splitDriftByStatus`, behaviorally selftest-pinned; git failure → contamination, the conservative direction). Same WARN-only posture; still never blocks the fold. |
| KI-E34 | **`recover` handles BLOCKED (owner-ruling recovery).** Resolving a ruled BLOCKED item previously required internals surgery: hand-edit the graph, know that BLOCKED's only legal edge is → READY, hand-author a recovery fold with the `#<cycle>r` resultId + the full CLAIMED re-entry chain (live: HOST-ITEM-A took exactly that). `recover <id>` now accepts BLOCKED and scaffolds the protocol with the BLOCKED-specific steps spelled out: record the ruling on the graph item (`ownerDecision` + `ownerDecisionResolved: true`, ratified files into `files[]`), run `driver reset <id>` BEFORE the fold, and — when the ruling ratifies already-delivered work — the prior round's transcripts stand as evidence. `recoveryTransitions('BLOCKED')` = the full 10-hop chain (selftest-pinned). The fold's deterministic evidence override applies unchanged — a BLOCKED recovery is never a lighter evidentiary path (review fix: a prior-less skeleton — the never-ran seed-BLOCKED shape — FILL-prompts `codeChange`; unfilled reads truthy, so the override demands full code evidence and fails closed instead of silently taking the doc/config no-evidence path; the scaffold spells out the never-ran protocol, and `recover` explains the run-recover-before-reset ordering when refused). |
| KI-E33 | **A run's cost/token telemetry is silently NOT gathered unless the session opts in — now surfaced at preflight.** The dashboard's two cost panels read `claude_code_token_usage_*`, emitted by the **Claude Code session** (via OTLP), not by the factory. If the session was launched without `CLAUDE_CODE_ENABLE_TELEMETRY=1` + `OTEL_EXPORTER_OTLP_ENDPOINT`, an entire run's token telemetry is lost with no cue — `factory_*` metrics (driver/agent-stage/fold/transition/aggregate-usage from `events.jsonl`) still land, so it looks like telemetry "worked" (live: two full cycles gathered `factory_*` but zero `claude_code_*`). `preflight` now reports `cost telemetry: ready → <endpoint>` or `NOT gathered — <reason>` (`costTelemetryReady`, pure/selftest-covered — review fix: `ready` also requires `OTEL_METRICS_EXPORTER` to include `otlp`, because Claude Code has NO default metrics exporter and enable+endpoint alone export nothing; the metrics-specific `OTEL_EXPORTER_OTLP_METRICS_ENDPOINT` counts; a missing `OTEL_EXPORTER_OTLP_PROTOCOL` rides as a caution on ready; every missing var is named; `FACTORY_TELEMETRY` is no longer consulted — it mutes only the factory's own events.jsonl, an independent plane), and the `/ai-factory` session-start step tells the operator to fix it before running (set the env — `telemetry/claude-code-telemetry.env.example`, KI-E28 — or put it in the host's `.claude/settings.local.json` `env`). The collector→Prometheus path itself was already correct (verified by pushing a synthetic OTLP metric end-to-end); the gap was purely session opt-in with no warning. |
| KI-E31 | **`fold` accepts the Workflow harness envelope directly.** The runtime wraps the factory's return as `{summary, agentCount, logs, result:{mode,cycle,results:[…]}}`, so a raw task-output file carries the fold payload under `.result`. `cmdFold` read `results.results` at the top level → `undefined` → it silently folded **0** items, forcing the operator to hand-extract `.result` into a file every cycle. `unwrapResultEnvelope` (pure, in `ledger.mjs`, selftest-covered) now unwraps that one shape; a direct results file (array, or an object already carrying `.results`) passes through unchanged. `fold <task-output-file>` now Just Works. Review fix: a ZERO-result payload (crashed/empty-run envelope, or an unexpected shape) no longer folds with success affect — loud error, exit 1, no cycle bump, no ledger write. |
| KI-E32 | **Acceptance-surface lint (KI-E22) no longer flags references/citations/exclusions.** The lint nudges files named in `acceptance` but absent from `files[]`. It fired on every run for files the item names to *model on*, *cite* (`SomeEndpoint.cs:739`), or *exclude* (`do NOT touch SomeHelper`) — none of which the file-lock should serialize. `acceptanceSurfaceGaps` now skips a token whose immediate context carries a strong reference cue (`modeled on`, `same … uses`, `File.cs:NN` citation, `do not touch`, `non-goal`, `rules out`, `instead of`, …). Deliberately conservative: an actively-named edit target (`ItemsController clamps …`, `modify X`, `in X`) still surfaces, so genuine "you forgot file X in the lock-set" gaps stay visible. Review fix: the cue set was TIGHTENED — bare `existing`/`see`/`per` and trailing `uses`/`does`/`already` suppressed everyday ACTIVE phrasings ("the existing X must clamp", "X does not clamp", "X uses raw Skip; fix it"); clause stops (`;:!?`) now end a cue's reach, and a bare `TypeName.cs:NN` citation is recognized (previously only pathed citations were). Both directions selftest-pinned. |
| KI-E29 | **A CLOSED dependency whose fix is uncommitted provides no code to dependents.** Worktrees branch from HEAD, and fixes stay UNCOMMITTED on `factory/<id>` branches (KI-E1) — so a dependency being `CLOSED` in the ledger does *not* put its code in HEAD until the human commits it. A dependent scheduled before that commit builds from a HEAD missing the dependency's code (live: `HOST-ITEM-A` depended on CLOSED-but-uncommitted `HOST-ITEM-B`; the fixer correctly re-derived HOST-ITEM-B's missing method fix inside its own worktree, which then tripped a false lock-set/scope block — KI-E30). `group` now WARNs when a picked item depends on a CLOSED item that still has a live worktree (proxy for "not yet committed to HEAD"); it does not exclude (nothing is clobbered — the owner decides whether to commit the dependency first for a clean base). Distinct from KI-E14, which is about the *main tree's* uncommitted overlap. Review fix: the check reads the LEDGER ROW's recorded `worktree` path (pure `ledger.closedDepsWithLiveWorktree`, selftest-pinned) instead of an assumed `state/worktrees/<id>` dir — SWEEP-closed deps are covered too, and gc (which nulls the field) correctly ends the warn. |
| KI-E30 | **`scopeViolation` conflated a lock-set deviation with a product-scope red-line.** The shared gate schema's `scopeViolation:true` is meant for the one hard-stop — a `product-scope.md` red-line (tax/purchase-fee/SAR/gov-report/shipping). But nothing told gates that touching a file *outside the item's declared `files[]` touch-set* is NOT that, so the developer gate set `scopeViolation:true` for a justified 1-line companion fix outside the lock-set, and `factory.js`'s KI-L57 guard (non-APPROVED + scopeViolation → hard block) framed it to the owner as a "product-scope violation" — a false-premise block on green, fully-reviewed code (live: `HOST-ITEM-A`). Fixed in the shared gate prompt: `scopeViolation` means a product-scope red-line ONLY; a touch-set deviation is a normal `finding` (note whether justified) and the verdict rests on its merits. The lock-set-overlap *observation* remains valuable — it stays a finding, it just no longer masquerades as a red-line hard-stop. Follow-up (same live case): when the hard stop DOES fire, `factory.js` echoes the flagging gate's own headline into the block reason AND the queue note keeps that raw reason verbatim — bracketed after the decision-framer's headline when framing succeeds, bare otherwise (review fix; previously the framer's paraphrase could displace the gate headline from the note) — so a mislabelled block is self-evident from the queue. |
| KI-E28 | **Dashboard cost panels need session-side OTLP.** The Grafana dashboard's two cost panels (`Claude Code token rate by type`, `Prompt-cache hit ratio`) read `claude_code_token_usage_tokens_total`, which the **Claude Code session** emits — not the factory exporter. The collector was already wired to accept it (`otel-collector.yaml` metrics pipeline has an `otlp` receiver; :4318 exposed), but nothing configured the session, so the panels were permanently empty with no cue why. Fixed as a setup step, not code: `telemetry/claude-code-telemetry.env.example` + a `telemetry/README.md` section give the exact env (`CLAUDE_CODE_ENABLE_TELEMETRY=1`, `OTEL_METRICS_EXPORTER=otlp`, `OTEL_EXPORTER_OTLP_ENDPOINT` → the collector's host HTTP port). Also documented: the `Span-derived stage latency p90` panel is live-only (spans push only while the exporter runs — AD-13), whereas the exporter-histogram `Stage duration p95` panel is the always-populated duration authority. Every `factory_*` panel needs none of this. |
| KI-E27 | **Multi-source issue ingestion.** `driver ingest` pulls work-items into `state/normalized/<source>.json` (which `merge-graph` folds) from `--github owner/repo` (`--issues` csv or `--label`/`--state`/`--limit`, via the `gh` CLI), `--json` (gh-issue array or ready work-item passthrough), or `--markdown` (a `- [ ]` checklist). The mapping is the pure lib `_workflow/lib/ingest.mjs` (selftest-covered — label→severity/theme, section extraction, id shape); the driver does the I/O (KI-E2 split). **Honest-acceptance invariant:** an ingested item is never `auto` — `blocked` triage when no acceptance section is parseable, at most `escalate` when an "Expected behaviour" section is lifted (a human still confirms it). A raw issue rarely states a checkable acceptance + regressionTest, so ingestion seeds the queue and a human/`bmad-spec` authors the contract before it schedules; it never fabricates a green light. Replaces the SETUP.md §6 "no adapter ships" gap. Review fixes: the invariant is ENFORCED at the command too — `enforceIngestTier` clamps `--json` passthrough tiers (auto/missing → escalate/blocked; selftest-pinned) so merge-graph's auto-default can never see an ingested item, and merge-graph now prints a VERIFY problem line when a hand-authored file's tier defaults to auto; flags are validated loudly up front (`--severity`/`--id-prefix`/`--issues`), gh failures exit 1 with the stderr reason, a --limit-sized result warns of likely truncation, `--out` is sanitized to a plain in-dir name, malformed `--json` elements are counted + skipped instead of crashing; the markdown adapter ingests UNCHECKED boxes only (a checked `[x]` is done work — skipped, count reported) with CONTENT-HASHED ids (stable under backlog edits; positional ids re-attached ledger state to different tasks), and `ingestReport.other` guards the "none are auto-runnable" line. Provenance cites the actual matched heading; fenced code no longer truncates section lifts; severity/theme label boundaries hardened (`not-critical`, `P10`, `docker`). |
| KI-E26 | **Host data must never enter the engine repo.** `SETUP.md` promises "everything under `state/`, `reports/`, and `queue/` is generated per host at runtime and is gitignored, so no project data ever enters this repo" — but `queue/decisions.md` was **tracked**, and `state/{ledger,findings-graph,PROGRESS}` + `reports/*` were merely untracked (so `git add -A` in the mount swept them in). Operating the factory in a host therefore dirtied a tracked file with that host's data: the decision queue quotes owner framings verbatim, which routinely name customers, internal issue ids and production detail. `queue/decisions.md` is now untracked (`queue/.gitkeep` ships instead, matching `state/`/`reports/`) and the per-host runtime artifacts are gitignored, making the shipped promise true. Nothing reads these as input — `setup/init.mjs` scaffolds the dirs and `driver init`/`escalations` regenerate the files on every host. |
| KI-E25 | **Telemetry stack identity is per-host, not just per-port.** The factory mounts in any number of host repos, but `telemetry/docker-compose.yml` pinned `container_name: factory-*` and the compose project `name:`, so a SECOND host on the same machine could not start its stack at all (duplicate container names — and `docker compose -p` does **not** override `container_name`), and the project name scopes the `prometheus-data`/`grafana-data` volumes, so a renamed-by-hand host would silently share host A's data. Both are now `${FACTORY_COMPOSE_PROJECT:-ai-factory-telemetry}` / `${FACTORY_CONTAINER_PREFIX:-factory}`, defaulting to the historical names (existing single-host stacks unaffected). Host B sets both in `.env` alongside the ports — see `telemetry/README.md`. Verified by running two stacks concurrently. Prometheus scrapes by **service** name (`exporter:9464`), never `container_name`, so inter-service DNS is unaffected. |
| KI-E53 | **`escalations` rendered "(no note)" for every ESCALATED/BLOCKED item, always** — the human decision queue's headline reason was structurally silent. `cmdEscalationsSync` reads `r.note` (the ledger row's static field) per item, but nothing in `driver.mjs` ever assigns `ledger.items[id].note` — only per-transition `history[].note` entries carry real reasons (e.g. the landing note on the `GATED -> ESCALATED` hop). The function already defines a correct `lastNote(r)` walk (last note-bearing history entry) for the separate "retry-exhausted FAILED" section a few lines below, but the main per-item line used the dead field instead. Live-caught 2026-07-28 (a real production item, real production run): `queue/decisions.md` showed the item ESCALATED with `- (no note)` despite the ledger history carrying "auto-drafted + fully verified; awaiting human sign-off before integrate (blast-radius)" one hop earlier. Fix: the per-item line now calls the same `lastNote(r)` helper instead of `r.note \|\| '(no note)'` — zero new code, just routes to the already-correct logic sitting right there. Confirmed live: re-running `escalations` after the fix rendered the real reason. |
| KI-E54 | **Every fold-time transcript re-read (`verify-raw.txt`/`integrate-raw.txt`/`verify-red-raw.txt`/`baseline-raw.txt`) hardcoded `readFileSync(p,'utf8')` with ZERO encoding defense.** A producer that emits UTF-16 instead of UTF-8/ASCII — e.g. a dispatched subagent's `\| tee <file>` command whose shell resolves to PowerShell, where `tee`/`Tee-Object`/`>`/`Out-File` all default to UTF-16LE — silently mis-decodes into mojibake under the hardcoded `'utf8'` read; every `FACTORY::` marker regex in `lib/verify.mjs` then finds nothing, collapsing to `hasData:false`/`"no-machine-evidence"` with no error or warning anywhere in the fold path. Live-caught 2026-07-28 (a real production item, real production run, OpenCode-driven session on Windows): a genuine `FACTORY::RED::1` proof existed in the test-author subagent's own transcript (self-reported: "Failed: 1, Passed: 0 ... row count 0 vs expected 1"), but its teed `verify-red-raw.txt` landed as UTF-16LE bytes (confirmed via hex dump: `FF FE` BOM, null-interleaved bytes) and read back unparseable — triggering a false deterministic fold-time override to FAILED on real, correctly-produced evidence, which then burned a resultId before the true root cause was isolated. Fix: `decodeTranscript(buf)` — a pure, dependency-free BOM-sniffing decoder in `lib/verify.mjs` (UTF-16LE/BE BOM -> correct decode incl. byte-swap for BE; UTF-8 BOM -> stripped; no BOM -> unchanged `'utf8'` decode, byte-identical to the old behaviour on the common path) — wired into BOTH of `driver.mjs`'s fold-time read sites (the shared `readIf` helper and the inline `baseline-raw.txt` read) in the same change. Confined to `driver.mjs`/`lib/verify.mjs` — the OpenCode port (`_workflow/opencode/*.mjs`) never re-reads these files from disk (it parses the in-memory transcript immediately after `spawnSync(..., {encoding:'utf8'})`, before writing to disk), so it was never exposed to this class at all; the fix is purely a canonical-engine hardening, not a port-specific patch. |
| KI-E55 | **The intermediate strengthening of KI-E51's comment policy (2026-07-30) — defined here because agent briefs, lint headers, and selftest assertion names cite the ID.** KI-E51 barred narrative comments in the NEW test file only; KI-E55 extended that bar to ALL files a fix touches, while still permitting one narrow class of comment: a line stating a genuinely non-obvious invariant or provider quirk. It shipped as agent-brief text and was superseded the SAME day by the owner's absolute directive — KI-E57: zero new comments, no exceptions, edited pre-existing comments reverted verbatim (now the host-configurable `noNewComments` policy; see KI-E57). This row exists so the KI-E55 references in `build-test.sh`, `comment-lint.mjs`, the four agent briefs, and the selftest assertion names resolve instead of dangling. |
| KI-E56 | **`test-author.md` hardcoded "xUnit + FluentAssertions" as THE testing convention — factually wrong for at least one real target repo.** Live-verified 2026-07-30 across this factory's actual multi-repo production work: one real target repo uses NUnit (`[Test]`, `Assert.That(...)`, `Assert.EnterMultipleScope()`) throughout every one of its test files, while another uses xUnit (`[Fact]`) with FluentAssertions (`.Should()`) throughout every one of ITS test files — two real repos this same factory targets, two different frameworks, and the brief named only one of them as if it were universal. Since neither repo (nor any repo this factory has been pointed at so far) ships a `code-style.md` this claim could have been sourced from, a test-author session with less direct scrutiny than a controlling session gives could plausibly write xUnit-shaped assertions into an NUnit project (or vice versa) — a compile-breaking or at minimum idiom-violating mistake with no gate specifically positioned to catch a wrong-framework choice before build. Fix: `test-author.md` no longer names a specific framework/assertion library at all — it now requires reading a sibling test file in the SAME target project FIRST and matching its actual framework/assertion/fixture idiom exactly, every time, with the two confirmed real conventions cited as the concrete example of why the assumption is unsafe. |
| KI-E57 | **KI-E51/KI-E55's comment policy still allowed a narrow "legitimate non-obvious constraint" comment through — the owner's actual rule is stricter: ZERO new comments, no exceptions, full stop.** Live-caught 2026-07-30 (a real production item, immediately after the KI-E55 fix shipped): the owner reviewed the resulting PR and issued an explicit, repeated, all-caps directive that NO comment may ever be added by this factory's output — not even one meeting KI-E55's "states a real invariant/provider-quirk" bar — and that any PRE-EXISTING comment a diff had edited/reworded must be REVERTED to its original text (not rewritten to stay accurate), even when that means the reverted comment now describes stale/superseded behavior — an explicitly ACCEPTED trade-off, not an oversight. A live worktree audit under the new rule found and removed 13 comment additions in one already-"KI-E55-clean" real target repo's diff that KI-E55's narrower bar had not flagged as violations, confirming the two bars are materially different, not just a wording tweak. Fix: `fixer.md`/`test-author.md`/`gate-developer.md`/`review-code.md` all upgraded from "no NARRATIVE comments" to "no comments, full stop, including reverting edited pre-existing ones verbatim" — the KI-E55/KI-E51 IDs are kept (this is a strengthening of the same rule, not a new class) and this row exists to make the escalation and its date explicit for anyone reading KI-E55 and assuming it is still the operative bar. **HOST-CONFIGURABLE (this same change), not a universal engine absolute:** the zero-comments directive is the ORIGINATING host's rule — another host's own engineering rules REQUIRE certain comments (divergence call-site tags, dependency-justification comments, Arrange/Act/Assert markers) and treat EF migrations as the canonical change mechanism (the KI-E58 rule), so shipping the absolutes unconditionally broke every host but the originating one. `config/factory.config.json` now ships `"policies": { "noNewComments": false, "noSchemaChanges": false }` — both OFF in the public engine; a host enables them in its gitignored `config/factory.config.local.json` (the KI-E17 overlay seam); `lib/policy.mjs`'s `loadPolicies()` is the single loader; the driver prints the effective policy state at every group/sweep; and the briefs' policy sections are conditional — a HOST POLICY block is injected into prompts only when the policy is enabled. |
| KI-E58 | **No brief anywhere in this factory ever told a fixer/planner that a DB/schema change (a migration, a new/renamed/removed column or table) is out of bounds** — the only existing hard-stop class was `product-scope.md` (a business/legal red-line), which schema changes are not, so a fixer proposing an "additive, nullable column on a shared entity" to fully close a correctness finding had no rule telling it that was off-limits. Live-caught 2026-07-30 (a real production item): a real, gate-surfaced MEDIUM finding whose most complete, textbook-correct fix required persisting a correlation token on a shared entity's table — which the owner explicitly refused ("DB - not allowed") when asked to choose between that and accepting the residual risk as analyzed/documented. Fix: `fixer.md` and `planner.md` both gain an explicit HARD STOP (mirroring the product-scope.md one in shape, but a DISTINCT class — narrower/expected, not a business red-line): no migrations, no schema changes, ever, even a seemingly-safe additive nullable field on a shared entity; when only a schema change would fully close a finding, implement the best fix possible within the EXISTING schema and record the residual gap as an accepted, documented trade-off (an architecture-doc entry — never a code comment, per KI-E57) rather than a scope-stop. **HOST-CONFIGURABLE (this same change):** like the comments rule, this is the originating host's directive, not a universal engine absolute — other hosts treat EF migrations as the CANONICAL change mechanism — so it now ships as the `noSchemaChanges` policy: OFF in the public engine's `config/factory.config.json`, enabled per host in the gitignored `config/factory.config.local.json`, loaded by the same `lib/policy.mjs` `loadPolicies()` seam (KI-E57), printed with the effective policy state at every group/sweep, and injected into prompts as the conditional HOST POLICY block only when enabled. |
| KI-E59 | **KI-E57's absolute no-comments rule was prompt-only — nothing MECHANICALLY enforced it, so a slip past the fixer/gate-developer/review-code prompts was only ever caught by luck (or, live, by the owner reading the PR by hand).** The owner's next instruction made the remaining gap explicit: beyond cleaning up the PR comments already landed, the ask was for MECHANISMS that prevent comments going forward, not just more prompt rules — the mechanism half is exactly this gap. KI-E51/KI-E55/KI-E57 are all agent-brief text, and the KI-D12 LeftoverScan precedent already proved prompt-only rules are insufficient alone under load. Fix: a deterministic detector `lib/comment-scan.mjs` (`findComments`/`classifyCommentLine`, mirroring `lib/leftover-scan.mjs`'s shape — scanning the worktree diff's ADDED lines + untracked new files) wired as a THREE-layer mechanical gate, all three layers running only when the host policy `noNewComments` (KI-E57) is ON. Hardened against real-world false-positive classes in this same change: (a) pure MOVES/re-indents are suppressed — an added comment line whose trimmed text byte-matches a REMOVED line anywhere in the same diff is a relocation, not a new/reworded comment (previously a file-scoped-namespace conversion hard-failed with impossible compliance); (b) string literals are stripped before matching — killing the `"//"`-inside-a-literal, protocol-relative-URL, base64 and csproj `Include="**/*.cs"` glob false positives; (c) the scan is extension-aware — `.md`/`.txt`/`.json` are not scanned, `<!-- -->` counts only in markup files, and `#` (shell/yaml/ps1/py), `--` (sql), `@* *@` (razor) and `(* *)` (F#) are covered where they are real comment syntax; (d) untracked files are read via `readFileSync` — the old `execFileSync('cat')` was a silent no-op on Windows, the very platform the gate was born on; (e) a scan failure reports an explicit `FACTORY::COMMENT-SCAN-ERROR` marker, never a silent count 0. Unlike LeftoverScan there is deliberately NO classify step anywhere — the enabled policy admits zero legitimate exceptions, so any hit is an immediate hard violation, checked BEFORE leftover-scan in both runtimes: (1) native `factory.js` — a new fail-open `comment-probe` haiku call (`COMMENT_SCHEMA`) runs `build-test.sh comments` and reports the deterministic count back VERBATIM (no judgment), FAILing the item pre-band on count>0, same fail-open posture as `leftover-probe` (a malformed/unavailable probe never sinks an item alone — layer 3 is the backstop); (2) the opencode port `runtime.mjs` — fully mechanical, ZERO LLM, importing `findComments` directly (no bash dependency) and treating a scan error as gate-unavailable, NEVER as clean, immediately FAILing on any hit before leftover-scan even runs; (3) fold-time — a WARN-only backstop in `driver.mjs` (`findComments`, mirrors the KI-D12/F2 posture exactly: a detection aid, never a fold-blocker) re-greps every folding CLOSED/INTEGRATED result's worktree independent of any LLM verdict; this should never fire, since layers 1/2 and the KI-E57 gate-band prompts all already check it, but an unmissable WARN exists if it ever does. `verify/build-test.sh comments <worktree>` / `_workflow/comment-lint.mjs` is the CLI surface, mirroring `leftovers`/`leftover-lint.mjs` in shape — and `build-test.sh` now handles `comments` (and `leftovers`) BEFORE the `build-test.local.sh` host-override seam, so a pre-existing host override can no longer silently neutralize the gate (previously an old override's usage error parsed as count 0 → a false APPROVED); count markers are parsed anchored + last-match, with the exit code consulted. Selftest-pinned: detector unit tests (hit/no-hit, move-suppression, string-literal and extension-routing cases), static wiring checks across all three layers, and a REAL exec-smoke run of `factory.js`'s actual pipeline logic proving both the short-circuit ordering (a comment-probe hit FAILs before the leftover-probe call ever runs) and the clean CLOSED/gate-recorded happy path. |
| KI-E60 | **KI-E56 fixed ONE symptom (a hardcoded test-framework assumption) but left the root cause standing: every agent brief in `agents/*.md` is ONE universal, shared set of role instructions, and this factory targets multiple real, materially different repos — the very repos KI-E56 describes (one NUnit, one xUnit+FluentAssertions) also differ in naming conventions, comment norms, mocking libraries, and dozens of other real idioms that KI-E56's narrower "read a sibling test file" workaround does not reach.** Fix: a new, purely-ADDITIVE per-repo overlay — `agents/repo-profiles/<target>.md`, one optional file per real target repo, auto-derived from that repo's own recent merged PRs (profiles exist for all four real target repos this factory's hosts point it at, and more land as research covers further repos — but they are HOST-LOCAL data, never engine content: `agents/repo-profiles/` is gitignored except `README.md` + a fictional `_example.*.md` template, because a real profile distills a private repo's conventions and must never enter the engine repo, per KI-E26) — is read alongside the universal `agents/*.md` brief and injected as a clearly-labelled "REPO-SPECIFIC STYLE PROFILE" block AFTER it, never in place of it: the universal briefs remain the default/authoritative source for EVERY repo, and this must never regress — a target with NO profile file behaves EXACTLY as it did before this mechanism existed, the same silent-no-op best-effort target-keyed-lookup posture `buildDocMap`/`solutionFor` already use (a missing file is normal, never an error, never required). Wired everywhere a role prompt is composed — since a review fix in this same change, that includes the SWEEP and SELECT lanes (the first cut injected on group/recover only, contradicting this row's own "wired everywhere" claim): `lib/promptpack.mjs`'s `readRepoProfiles()` mirrors `readRoleBriefs()` exactly (dir-listed, `.md`-only, capped, `{}` on any failure); `driver.mjs` inlines the map into the batch on the group, sweep, and select lanes (`runArgs`, a sibling of the existing `briefs:` key) and separately in the `recover` regate builder (its own per-role prompt string, since that path never builds a `runArgs` object at all), because the sandboxed Workflow runtime (`factory.js`) has no filesystem access of its own to read the file directly (KI-E2); `factory.js`'s `compose()` reads the pre-inlined `A.repoProfiles[item.target]`; the opencode port `compose.mjs` (real fs access, KI-O1) instead reads `agents/repo-profiles/<target>.md` fresh off disk on every call, for EVERY role including probes *(correction, KI-E112: this row previously said "scoped to non-probe roles only exactly like its own brief-reading block". That was factually wrong — the profile block sits OUTSIDE compose.mjs's `!/-probe$/` branch, and both the port's own comment and its selftest assert the opposite. The brief-reading block IS probe-scoped; the profile block is not.)*. Both runtimes cap an injected profile at the SAME exported `PROFILE_CAP` (review fix, this same change: the native path silently truncated at 12000 chars — 3 of the first 4 live profiles lost 30-53% of their content — while the opencode path injected them whole). Every injection carries the same override notice: a profile's own observed facts win over generic assumptions for THIS repo, but the host's `noNewComments` policy (KI-E57), when enabled, always applies regardless of what a profile's own "comment usage observed" section reports about a team's real-world habits. |
| KI-E61 | **KI-L65's main-tree contamination detection (KI-E41/E45/E50/E35) was WARN-only across four separate hardening passes and never found the actual trigger mechanism — because every pass treated cycle-35's incident as agent non-compliance ("the brief already forbids this") rather than re-examining whether a LATER prompt feature had reopened the exact ambiguity the brief's absolute WORKTREE/REPO-ROOT split was meant to close.** Live-caught 2026-08-02 (a real production item, ITEM-H18, cycle 56): the fold's KI-L65 check fired on `doc/data-flows/IdentityPortal.md` — present in the MAIN tree, uncommitted, changed. Root cause found by re-reading `lib/promptpack.mjs`'s `buildDocMap` (added 2026-07-18, *after* cycle 35's mainguard.mjs was written and its "detection, not another prompt rule" conclusion reached): the per-item DOC MAP feature hands out **bare-relative** paths — `doc/data-flows/<target>.md`, `<target>/CONTEXT.md`, `<target>/AGENTS.md` — with no anchor stated at the point of use, inside a prompt that separately says "read audit docs from the REPO ROOT." An agent correctly Reads a doc-map entry against REPO ROOT; when that SAME work item's acceptance criteria then requires EDITING that identical relative path (the dataflow.md doc-sync rule fires on most code-touching items), Edit naturally reuses the exact path string already in hand from the Read — a silent slide from "read-only reference" to "edited the main tree" with no rule ever knowingly broken. Retroactively explains ALL THREE prior incidents, not just this one: cycle 35's ITEM-H12/ITEM-H5 and cycle 48's ITEM-H16 (an "acceptance-named CONTEXT.md" — one of buildDocMap's own three candidate file types) are the identical shape. Two-part fix: (1) **prevention at the ambiguity** — `factory.js` and the opencode port's `compose.mjs` (verbatim-port parity) now render every DOC MAP entry REPO-ROOT-qualified, and any entry that is ALSO in the item's `files[]` touch-set gets an unmissable second line naming the exact worktree path to edit instead ("THIS path is ALSO in your files[] touch-set... do NOT Edit it. Your edit target... is: `<worktree>/<relpath>`"). (2) **auto-repair as defense in depth** — `mainguard.repairDirtyDrift` closes the loop KI-L65 deliberately left open ("repairing main is operator judgment"): splitDriftByStatus's OWN reasoning already proves dirty/uncommitted drift can only be agent contamination (the factory never commits, so a human's legitimate delivery always lands in the COMMITTED bucket, untouched) — there is no case where dirty drift is anything BUT a bug to fix, so both the fold-time and KI-E41 resume-time checks now restore a present-drifted file from HEAD or remove an absent-drifted stray immediately, and report what was auto-repaired (never silently). The mid-band KI-E50 `main-check` stays read-only/lock-free/report-only by its own explicit design (an in-flight lane's OWN uncommitted evidence would otherwise be at risk of a concurrent repair) — auto-repair applies only at the two points (fold, resume) where the item generating the drift is provably not still running. Behaviorally selftest-pinned: `repairDirtyDrift` restore/remove/failure-is-non-throwing cases, source-text parity between `factory.js` and the opencode port's docMap block, and driver wiring pins that both call sites actually invoke the repair (not just report it). |
| KI-E62 | **A mixed `group` batch (two or more disjoint similarity cliques) got NO batch-pattern stamp for ANY item, even when each clique on its own would have passed the strict all-pairs test — `batchPatternFor(picked)` is deliberately all-or-nothing (`similarity.mjs` § "STRICT all-pairs test"), so batching e.g. a doc-drift item alongside two pagination-clamp items silently stripped the sibling-convergence benefit from BOTH pairs, not just the item that broke the whole-batch clique.** Identified as a candidate in an internal parallelism/reuse analysis (never built until 2026-08-02). Fix: `similarity.mjs` adds `perCliqueBatchPatterns(items)` — clusters first via the pre-existing `clusterBySimilarity`, then calls `batchPatternFor` on each cluster independently, stamping only the clusters that qualify (≥2 members AND pass the strict all-pairs test within that sub-cluster). `driver.mjs`'s `cmdGroup` now builds a `Map<itemId, pattern>` via `perCliqueBatchPatterns(picked)` (replacing the single `batchPatternFor(picked)` call) and each item's `batchPattern` field is looked up per-item (`batchPatternById.get(wi.id)`) instead of forwarding one shared value; `--pattern`/`--no-pattern` operator overrides are unchanged (a forced pattern still stamps every item; `--no-pattern` still stamps none). Strictly additive by construction: when the WHOLE batch is one clique, `clusterBySimilarity` collapses to exactly one cluster (every pair being mutually similar makes them transitively connected), so `perCliqueBatchPatterns` reproduces the IDENTICAL stamp `batchPatternFor(items)` already produced — the pre-existing whole-batch-clique behavior cannot regress. Selftest-pinned: clique members get the same stamp text `batchPatternFor` would produce for that sub-clique alone, singletons and cross-clique items stay unstamped, the 4-item mixed-batch stamps exactly its one qualifying clique, and the whole-batch-is-one-clique parity case matches `batchPatternFor` byte-for-byte; driver wiring pins confirm `cmdGroup` calls `perCliqueBatchPatterns(picked)` and forwards via `batchPatternById.get(wi.id)`. **Follow-on caught in the same pass:** `cmdSuggest`'s own batch-pattern preview line (the advisory text shown before the `group --ids ...` command it prints) still called the retired whole-batch-only `batchPatternFor(batch)` and would print "none (mixed shapes — group will not stamp)" for a `pick(false)` fallback batch (file-disjoint, not all-pairs similar) even in cases where `group` would now actually apply a per-clique stamp to a qualifying sub-clique within that exact batch — the preview and the real behavior it exists to predict would have silently diverged the moment KI-E62 shipped. Fixed by switching the preview to `perCliqueBatchPatterns(batch)` and reporting three distinct states: no qualifying sub-clique ("none"), the whole batch qualifies ("AUTO, whole batch"), or a genuine partial ("AUTO, per-clique (KI-E62): N/M member(s) would be stamped"). This also left `batchPatternFor` itself with zero remaining call sites in `driver.mjs` (only `perCliqueBatchPatterns`, which lives in and calls it from `similarity.mjs`, is used directly) — the now-dead import was removed. **Second follow-on, more serious, live-caught:** the `batchPattern` → `batchPatternById` rename left ONE bare `batchPattern` reference far below the rename site, inside the `run_prepared` telemetry `temit(...)` call (`attrs: { ..., batchPattern: !!batchPattern, ... }`) — a `ReferenceError` that crashed EVERY real `driver.mjs group` invocation (dry or not) the instant it reached that line, i.e. the single most heavily-used command in the factory. Source-text selftest pins on the rename sites did NOT catch this — they assert substrings exist, they never execute `cmdGroup`. Caught only by an actual `driver.mjs group --max N --dry` smoke run performed as a final check before considering the change done — the lesson generalizes: any `driver.mjs` change that renames or removes a variable MUST be live-smoke-tested with a real (`--dry` is sufficient) invocation of the affected command, not just selftest-source-pinned, because Node throws at the FIRST executed reference to an undefined binding, and a purely textual pin cannot see execution order or reachability. Fixed to `batchPattern: batchPatternById.size > 0` (true when the batch produced at least one per-clique stamp) and selftest-pinned so this exact bare-reference shape cannot silently regress. |
| KI-E63 | **A `group` batch got zero benefit from the 167-and-growing CLOSED corpus: a fixer facing e.g. a doc-drift or pagination-clamp item had no way to discover that a gate-APPROVED instance of the exact same change-shape already shipped, so every item re-derived its approach from scratch even when a proven, reviewed template already existed.** Identified as a candidate in an internal parallelism/reuse analysis ("cheapest high-leverage reuse … immediately monetizes the CLOSED corpus", never built until 2026-08-02). Fix: `similarity.mjs` adds `bestClosedPrecedent(item, closedCandidates)` — a pure, one-item-vs-pool scan (no clustering needed) over already-CLOSED siblings, matched the same way batch-pattern matching is (same `theme` + `similarSigs`), preferring the MOST RECENTLY closed qualifying match. `driver.mjs`'s `cmdGroup` builds the candidate pool once per `group` call by cross-referencing `ledger.items` (`state === 'CLOSED'`) against `byId(graph)` for `theme`/`title`/`target`, looks up a precedent for each picked item, and stamps `item.precedent = {id, target, title, fixJson?, worktree?}` — **but only when at least one of `state/items/<id>/fix.json` or `state/worktrees/<id>/` is ACTUALLY present on disk right now** (`existsSync`-checked at stamp time). This existence gate is load-bearing, not defensive theatre: investigating this candidate found that NEITHER artifact class is durably guaranteed — worktree `gc` is explicit-opt-in (`driver.mjs gc --yes`), and on this very mount only 6 of 167 CLOSED items still had a `state/items/<id>/` directory at all (the rest were evidently cleared by an operator or process outside the factory's own code, which never deletes `state/items/`) — so a precedent stamp pointing at a dead path would be worse than no stamp (an unfollowable instruction). Silent-no-op when nothing qualifies, matching the `buildDocMap`/`solutionFor` convention (KI-E60): never an error, never required. Rendered into the prompt by `factory.js` and ported verbatim to `opencode/compose.mjs` (KI-O1 parity) as a `PRECEDENT` block, explicitly framed as READ-ONLY reference material in a *different* item's directory ("never Edit it, never touch its files") — the same ambiguity lesson KI-E61 already codified for the DOC MAP block, applied here even though the risk is lower (a precedent path can never coincide with the current item's own `files[]` touch-set). Selftest-pinned: the pure function's most-recent-match / cross-theme-exclusion / self-id-exclusion / empty-and-null-pool behavior; `cmdGroup` wiring (candidate-pool construction, the existence gate, per-item forwarding); source-text parity between `factory.js` and `compose.mjs` for the `PRECEDENT` block and its read-only framing. Independently verified against real ledger/graph data (166 of 167 CLOSED rows have a live graph entry; a synthetic item shaped like the real ITEM-H10 correctly resolved ITEM-H10 as its precedent with both `fix.json` and the worktree confirmed present) and via multiple live `driver.mjs group --dry` invocations (default, `--no-pattern`, `--pattern`, themed filters) with zero crashes. |
| KI-E64 | **`graph-audit`'s KI-E16 shared-file-gap lint has carried the wrong ledger filename since its introduction (2026-07-20): `LEDGER_PATH = '_bmad-output/tech-debt/STANDARDS-LEDGER.md'` — missing "DIVERGENCE" — a path that has NEVER existed on disk (only `STANDARDS-DIVERGENCE-LEDGER.md` does). Every item whose acceptance names a ledger entry/anchor was flagged `SHARED-FILE-GAP` regardless of whether its `files[]` already correctly carried the real path, because `!(wi.files||[]).includes(LEDGER_PATH)` could never be false against a constant nothing could ever match — and `graph-audit --fix` would have silently APPENDED the nonexistent path into files[] instead of the real ledger, worsening rather than fixing the lint's own target.** Found live 2026-08-02 while merging a new batch of 45 standards-divergence-ledger-derived work items (a large batch-prep pass): a fresh `graph-audit` run reported 47 shared-file gaps — 45 of them on the brand-new items, whose `files[]` demonstrably ALREADY contained the correct `_bmad-output/tech-debt/STANDARDS-DIVERGENCE-LEDGER.md` path (independently confirmed via direct JSON inspection: 45/45 items matching the lint's own trigger regex already had the real path in `files[]`), plus 2 more on pre-existing, unrelated items across several other target repos that had been silently mis-flagged since the day this lint shipped. Fix: corrected the constant to `_bmad-output/tech-debt/STANDARDS-DIVERGENCE-LEDGER.md`; also widened the detection regex from the literal `STANDARDS-LEDGER` to `STANDARDS-(?:DIVERGENCE-)?LEDGER` (the OR'd `ledger entr(?:y\|ies)`/`ledger anchor` fallbacks already covered most real prose, which is why the bug went undetected for two weeks — the DETECTION side mostly worked via those fallbacks; only the PATH-COMPARISON side was structurally broken). Re-running `graph-audit` after the fix: shared-file gap count dropped from 47 to 0 (every real reference, old and new, already has the correct path — confirming this was a pure false-positive bug, not a real backlog of un-appended ledger references). Selftest-pinned: the corrected `LEDGER_PATH` constant's exact value. |
| KI-E65 | **A fold result's `toState` and the last element of its `transitions` array are two independent fields with no structural link — `foldResults` walks `transitions` as the primary source of truth and only falls back to `[toState]` when `transitions` is empty, so a result that sets `toState:'FAILED'` while leaving `transitions` ending in the skeleton's default success path (`'...,CLOSED'`) silently CLOSES the item instead of failing it.** Live-caught 2026-08-03 during the cycle-57 recovery pass for the `SDL-*` standards-divergence batch: `recoveryFoldSkeleton` pre-fills a new recovery's `transitions` array with the full `RED..CLOSED` success chain (correct for the common case where the fold-prep step only needs to fill placeholders when the recovery succeeds), and a fold-prep agent instructed to "set `toState: 'FAILED'`" on a still-failing item (ITEM-30 — one re-gate returned `CHANGES_REQUIRED`, `adjudicator: UPHELD`) correctly wrote `toState` but — never told to also truncate `transitions` — left it unchanged. Folding it walked the row straight through to `CLOSED`, silently recording a genuinely-still-broken item (a newly-introduced flaky test in `RefundCompletedConsumerTests.cs`) as done. Caught only because the operator independently cross-checked the folded ledger state against the recovery note's own prose (`grep`-visible contradiction: note said "toState is FAILED, not CLOSED" while `ledger.items['ITEM-30'].state === 'CLOSED'`) — nothing in the fold pipeline itself would have surfaced it. Corrected by hand (`canTransition` has no legal `CLOSED->FAILED` or `CLOSED->READY` edge for `reset`/a corrective fold to use — a narrow, disclosed, one-off ledger hand-edit was the only path back to a true state, logged as an `OPERATOR CORRECTION` history entry on the row). Fix: `lib/ledger.mjs` adds `reconcileToStateAndTransitions(r)` — a pure function called in `cmdFold` for every result BEFORE the deterministic-verify pass and `foldResults` (both of which also read `transitions`/`toState`). No-ops when the two already agree (or either is absent). When they disagree: if `toState` appears earlier in `transitions`, truncate there (the array over-ran its own stated ending); if `toState` does not appear at all (the live shape above), walk backwards from the end of `transitions` for the last state that is a legal jumping-off point for `toState` per `canTransition` (skipping `CLOSED`/`INTEGRATED`, neither of which can reach `FAILED`; landing on the last `ACTIVE` state, e.g. `REAUDITED`, which can), truncate there, and append `toState`. Every correction is reported loudly (`⚠ TOSTATE/TRANSITIONS MISMATCH ... AUTO-CORRECTED`, same posture as KI-E61's dirty-drift auto-repair) — never a silent fix-up. Selftest-pinned: all four no-op shapes (null result, no `transitions`, no `toState`, already-consistent), the mid-array-truncation case, the live ITEM-30 shape's exact backward-walk-and-append output, and an end-to-end regression pin that runs the exact live-bug shape through `reconcileToStateAndTransitions` + the real `foldResults` and asserts the row lands on `FAILED`, not `CLOSED`. |
| KI-E66 | **Cache-hit-rate telemetry did not exist ANYWHERE in the factory's own event stream — and its intended source (two Grafana panels, KI-E28) had been permanently empty on every host observed to date, including this one, with no cue why beyond a preflight message.** `claude_code_token_usage_tokens_total` (session-side OTLP) is the ONLY place cache-read/creation/input/output token typing exists at all; per KI-E33 it requires `OTEL_METRICS_EXPORTER=otlp` + an OTLP endpoint, which this host had never actually set (`CLAUDE_CODE_ENABLE_TELEMETRY=1` alone was present) — independently confirmed live by querying Prometheus directly: zero `claude_code_*` metric names registered despite the `telemetry/` compose stack (grafana/prometheus/exporter/otel-collector) running continuously for 25+ hours. Separately, true PER-ITEM token measurement was investigated and found not available at all: the Workflow runtime's `budget` global exposes only a whole-turn/whole-workflow output-token counter (`budget.spent()`, the existing KI-E23 `usage` event's basis) with no per-call or per-item split, and a direct empirical test (dispatching a marker-tagged subagent and inspecting the local session transcript) confirmed a Workflow-internal `agent()` call's own token cost is not independently recoverable from outside the sandboxed runtime either — corroborated by this factory's own multi-month development history never achieving better than a whole-run total despite KI-E23 explicitly wanting per-item granularity. Fix, two parts: (1) **cycle-scoped bridge** — `lib/token-usage.mjs` (`parseTokenUsageVector`, `cacheHitRate` — the SAME ratio already authored in the dashboard's "Prompt-cache hit ratio" panel, `cacheRead / (cacheRead + cacheCreation + input)`, reused rather than inventing a second definition — `tokenUsageSummary`, `buildTokenUsageQuery`) + `driver.mjs`'s `queryPrometheusTokenUsage` (best-effort `fetch`, zero new deps per AD-4, 5s timeout, never throws) wired into `telemetry-report` (not the hot fold path — AD-1's "runs to completion with the stack down" stays true): on success it emits a `token_usage_snapshot` event (`source:'driver'`) into `events.jsonl` so the durable AD-9 record carries it, not just a live Grafana view; on any failure (unreachable, empty result, malformed response) it resolves to nothing gathered — a bare 0%/0-token result is never fabricated (the exact KI-E40 silent-miscount lesson, applied here before it had the chance to repeat). (2) **disclosed per-item apportionment** — since real per-item measurement is unavailable, `apportionTokensByCallShare` divides each cycle's REAL, measured output-token total (KI-E23) across that cycle's items by their REAL, measured call-count share (`item_folded.attrs.cost`, already accurate) — rendered under a column/section explicitly labelled "estimate — NOT a measurement" in both the report and this row, never presented as measured data. Also fixed as a prerequisite, same session: this host's `setup/install.sh telemetry-up --shell-env --yes` had written the marker-guarded shell-profile block + merged the project `settings.local.json` env block at some earlier point, but the RUNNING session predated both (KI-E33's own documented "session predates the settings edit" class) — re-run to confirm both are now in place for the next session; `driver.mjs preflight`'s `cost telemetry: NOT gathered` message is exactly the correct, honest signal for a still-old session and needs no further fix. `renderTelemetryReport` gains "## Session token usage & cache hit rate" and "## Per-item apportioned tokens" sections; `aggregateEvents` gains `itemCallCounts` (cycle-keyed, so a retried item's later-cycle calls never dilute an earlier cycle's apportionment) and `tokenUsageSnapshots`. Selftest: 780 → 802 (`lib/token-usage.mjs` pure-function coverage incl. the exact dashboard formula match, the range-vector latest-sample case, and every zero/empty edge case; `aggregateEvents`/`renderTelemetryReport` integration incl. the cycle-keyed apportionment and the honest NOT-gathered fallback when no snapshot exists). |

| KI-E67 | **An item's own artifact prose can read as confidently DONE while the deterministic fold verdict is FAILED, with nothing surfacing the contradiction — a concrete instance of the factory's single largest recurring rejection driver (agent self-report vs machine-verifiable evidence, KI-E19/E38/E43/E51/E54) that cost real investigation time to disentangle.** Found live 2026-08-03 while auditing ITEM-22 (`FAILED — deterministic override: "integrate transcript: 406 new suite failure(s) beyond baseline 0"`): its `state/items/ITEM-22/` directory carries a non-canonical `VERIFICATION-REPORT.md` — a confident, 10-point "✅ COMPLETE — All acceptance criteria met, zero leftovers, build passes... Ready for merge... No additional changes required" — directly contradicting the FAILED state. (A prior hypothesis that this file was CONTAMINATION from an unrelated item, per a same-session research pass, was independently checked and disproven: `findings-graph.json` confirms ITEM-22's own real title/acceptance IS the MarketplaceAdmin orphan-count-header feature this file describes — the file is genuine, if non-canonically-named, output for THIS item, not debris. Likewise, that same research pass's claim that a specific recent commit "accidentally committed real generated secrets into k8s" was checked directly against the commit's actual diff and found FALSE — the commit only edited placeholder text (`REPLACE_WITH_32_PLUS_RANDOM_CHARS` → `..._OR_FAIL_VALIDATION`) and a config key rename; HEAD still carries only placeholders. Both corrections are recorded here so neither false lead is chased again.) Fix: `lib/narrative-check.mjs`'s `detectNarrativeVerdictContradiction(toState, textsByFile)` — pure, deliberately conservative (fires ONLY on toState FAILED/ESCALATED, matching ONLY a small set of strong, unambiguous completion phrases: "✅ complete", "all acceptance criteria met", "ready for merge", "no additional changes required", "zero leftovers", a "Status: ... complete" line) — wired as a `cmdFold` WARN-only detection aid (same posture as F2/KI-D12/KI-E59: never a fold-blocker, the deterministic override remains the verdict) that scans EVERY `.md` file already sitting in a FAILED/ESCALATED result's `state/items/<id>/` directory, canonical or not — deliberately not limited to the canonical artifact list, since the live case was exactly a non-canonical file. Selftest-pinned: fires only on FAILED/ESCALATED (never CLOSED, where upbeat language is simply an accurate description), each marker phrase individually, a non-matching narrative producing no hit, and the live ITEM-22 shape end-to-end. Selftest: 802 → 816. |

| KI-E68 | **`cmdGroup`'s per-run agent-concurrency default was a bare hardcoded `2` in `driver.mjs`, completely disconnected from `config/factory.config.json`'s `concurrency` block — the block's `throttled`/`normal`/`max`/`default` tiers and its own guidance note ("2-3 under active throttle, 5-8 when quiet") were pure unread documentation, never consulted by any code path.** Found live 2026-08-03: the operator, watching cycle-58's 12-item batch stall in the review-gate band (11/12 items already through rootCause/red/fix/build, ~60+ independent review/gate agent calls queued across them) at a global concurrency of 2, asked why parallelism was so low and to raise it. `run-script.js`'s inlined `const CONC = A.concurrency || 2` traced straight back to `cmdGroup`'s `runArgs.concurrency: flags.conc ? parseInt(flags.conc,10) : 2` — a second, independent magic number never reconciled with the config's own documented tiers. Fix: `cmdGroup` now defaults to `cfg.concurrency.default` (raised 3→6, i.e. the config's own `normal` tier) with a `6` literal fallback only if the field is ever absent; `--conc` still overrides per-invocation for an explicit active-throttle/quiet judgment call. This closes the doc-vs-code gap in the SAME direction the operator asked for (6) rather than just picking a second disconnected number. In-flight run recovery: the ALREADY-CLAIMED cycle-58 batch's generated `state/run-script.js` had its inlined `"concurrency":2` hand-edited to `6` and relaunched via `Workflow({scriptPath, resumeFromRunId})` — the runtime's own cache-by-(prompt,opts) resume semantics replayed all 62 already-completed agent() calls for free and only the queued-but-not-started review-gate calls picked up the new ceiling; no worktree/claim/fold state was touched or lost. Selftest: 816 → 819 (3 source-text pins: `cmdGroup` reads `cfg.concurrency.default` rather than a bare literal, the old disconnected hardcoded-2 default is gone, and `config.concurrency.default` itself reads 6 — mirroring the KI-E14/KI-E29 cmdGroup source-pin style since `cmdGroup` itself has no pure-function harness to execute directly). |

| KI-E69 | **A killed Workflow run's per-stage artifacts (plan.md, test.json, fix.json) are already durably written to `state/items/<id>/` the moment each role completes — independent of the KI-L40 end-of-item `result.json` checkpoint, which only fires if the item's WHOLE lifecycle resolves — yet the sanctioned relaunch ("relaunch the same run-script verbatim", SKILL.md § Recovery) re-ran plan → test-author → fixer from scratch for every relaunch candidate regardless of how much of that already-expensive generative work was sitting on disk, complete and untouched, in the item's own worktree.** Witnessed live 2026-08-03 (the same cycle-58 batch KI-E68 raised concurrency for, now killed by a sudden host shutdown before ever reaching a checkpoint): `resume` reported 12 CLAIMED items with 50/42/32/19/13/11/10/10/8/7/7/0 changed files already sitting in their worktrees — for the items with real progress, the ONLY thing between them and a fold was re-running the downstream review/gate band, yet the existing recovery path would have discarded plan/test/fix entirely and paid full price again. Investigated and found a real correctness trap along the way: `state/items/ITEM-H6/verify.json` (a real on-disk artifact) carries rich `{result, errors, warnings, ...}` objects for `build`/`targetedTest`, but `runItem()`'s own code reads them as plain pass/fail STRINGS (`if (!/^pass/i.test(String(verify.build)))`) — `String({result:"pass",...})` evaluates to `"[object Object]"`, which would have silently misclassified a genuinely-passing build as FAILED. `test.json`/`fix.json`, by contrast, were directly verified to carry plain, schema-matching values for every field `runItem()` actually reads. Fix, scoped to what is provably safe: `lib/prior-attempt.mjs`'s `loadPriorAttempt(itemDir, sinceMs)` reads back `test.json`/`fix.json` (freshness-gated: mtime must be at or after the item's CURRENT claim timestamp from ledger history, never a stale prior cycle's leftovers) and reconstructs a safe stand-in for `plan` (`{recommendScopeStop:false, recommendEscalate:false}` — the ONLY two fields `runItem()` reads from it — correct by construction whenever `test.json` also exists, since either flag would have short-circuited the item before test-authoring ever ran). `verify` is deliberately NEVER reused — always re-run fresh, which is also the safety net that independently re-confirms the reused fix still builds/passes on this worktree. Everything from the editorial pass onward (editorial, edge-scan, acceptance-scan, the full gate band, refute, reaudit, integrate) is completely unchanged and always re-runs fresh, preserving the existing, explicitly-stated design principle that gates re-adjudicate the worktree on every relaunch. Wired behind an explicit opt-in: `driver.mjs resume --reuse` (a plain `resume` is byte-for-byte unchanged) regenerates the relaunch candidates' `run-args`/`run-script` with `item.priorAttempt` populated per item, then re-emits the launcher via the existing `emitLauncherScript` (no duplication of `cmdGroup`'s pick/worktree/dep-check pipeline — the ALREADY-CLAIMED items' fully-built objects already live in the original run-args.json; this only enriches and re-serializes them). `runItem()` in `factory.js` consults `item.priorAttempt.{plan,test,fix}` at exactly those three call sites and nowhere else. `res.priorAttemptReuse` is always present on the result (an empty array IS the "nothing reused" signal, never silently absent) and rides the `item_folded` telemetry event so reuse is measurable, never silent. Known, disclosed gap: the opencode runtime port (`_workflow/opencode/`, KI-O1) does not yet consult `item.priorAttempt` — out of scope here since the batch this fixes runs on the primary Workflow-based runtime; a future port pass should mirror the same three call sites. Selftest: 819 → 838 (`lib/prior-attempt.mjs` pure-function coverage incl. the fresh/stale mtime boundary, malformed-JSON isolation, and the plan stand-in's dependency on test.json also being present; source-text pins confirming `runItem()`'s three call sites, the `--reuse` opt-in gate, and the `item_folded` telemetry wiring). |

| KI-E70 | **`parseVerifyRaw`'s `lastMatch` on `FACTORY::TEST::FILTER::RESULT` assumes every occurrence in a transcript is a RETRY of the SAME test class (cycle-8's fail-then-pass precedent) — but a runner legitimately filtering SEVERAL DIFFERENT test classes in one verify pass (not a retry loop) hits the identical marker shape, so the deterministic override's "targeted regression test did not pass" message named no class at all, sending the reader hunting for the wrong test.** Live on ITEM-H1, 2026-08-07 (the cycle-58 relaunch batch): `verify-raw.txt` shows 5 sequential `FILTER::START`/`FILTER::RESULT` pairs for 5 DIFFERENT classes (`ClassATests`, `ClassBTests`, `ClassCTests`, `ClassDTests`, `ClassETests`) — the first 4 passed, the last (`ClassETests`) failed on a real `EntityFrameworkCore.Storage.RelationalConnection.OpenInternalAsync` exception against a `FACTORY::REALINFRA::Testcontainers-postgres` container. The agent's own schema-validated `targetedTest` return field genuinely said "pass" (truthfully describing the FIRST/nominally-"targeted" class), so the in-run check (factory.js) let it through to gates; only the fold-time deterministic override caught the LAST filter's exit=1 — correctly, but reported it as "targeted regression test did not pass" with no indication it wasn't even the same class the runner named as its target. Fix: `parseVerifyRaw` now also collects every `FACTORY::TEST::FILTER::START $target :: $filter` class name (build-test.sh's own emission format) in order and, when the LAST `FILTER::RESULT` fails, pairs it with the LAST captured class name (1:1 by emission order — build-test.sh always emits exactly one START before its RESULT/SUMMARY per filter invocation) into a new `targetedFailClass` field; `verdictFromParse`'s reason string appends `(<ClassName>)` when available, degrading to the old unqualified message when no START marker exists (legacy/bare transcripts). A same-class retry (the original cycle-8 shape) is unaffected — the class name is simply identical across attempts, so the message just becomes more specific, never wrong. Selftest: 838 → 844 (the ITEM-H1 multi-class shape end-to-end, a same-class-retry-with-name case, and the no-START-marker-present degrade-gracefully case). |

| KI-E71 | **`leftover-raw.txt` — the KI-D12 leftover-scan probe's OWN canonical artifact (`factory.js`: `res.artifacts['probe:leftover-scan'] = 'state/items/' + id + '/leftover-raw.txt'`) — was missing from `lib/telemetry.mjs`'s `STAGE_ARTIFACTS`, so `nonCanonicalArtifacts()`/`resume`'s KI-E42 debris detector false-positived "NON-CANONICAL artifacts from the dead attempt" on it for EVERY item that reached that stage, live run or dead, diluting the signal for genuinely-improvised files sitting alongside it.** Found live 2026-08-07 monitoring the cycle-58 relaunch batch: every `resume` check from the item's leftover-scan onward flagged `leftover-raw.txt`, even for items with fresh sub-2-minute-old writes elsewhere in the same directory (an actively-running item, not a dead one). Investigated `leftover-final.txt` (flagged alongside it on the same items) as a second candidate for the same fix — found ZERO references anywhere in `factory.js` or `agents/*.md`; it is genuine agent improvisation and correctly stays flagged, so only `leftover-raw.txt` was added to `STAGE_ARTIFACTS` (mapped to stage `probe:leftover-scan`, matching the label already used in `result.json`'s `artifacts` map). Selftest: 844 → 846 (`nonCanonicalArtifacts` no longer flags `leftover-raw.txt` while still flagging `leftover-final.txt`; `stageForArtifact('leftover-raw.txt')` returns `probe:leftover-scan`). |

| KI-E72 | **`preflight`'s `dotnetAvailable()` shells `dotnet --version` directly in the CONTROLLER session's own process env, but the REAL verify path every subagent uses (`verify/build-test.sh`) auto-delegates to a per-host `verify/build-test.local.sh` PATH shim (KI-E17 override seam, `SETUP.md § 6`) when one exists — so a controller session whose OWN shell never sources `~/.zshrc` (the non-interactive-shell class documented right in the shim's own header comment) reports `dotnet: ABSENT — build/test verify cannot run on this host` while every real verify call in the actual factory run succeeds via the shim.** Found live 2026-08-07: `driver.mjs preflight` reported ABSENT on a host that had already closed 180+ items that same session — independently confirmed the SDK/dotnet binary genuinely exists at `~/.dotnet/dotnet` (10.0.302) and `verify/build-test.local.sh` was present, executable, and correctly exporting `PATH`/`DOTNET_ROOT` before delegating. Fix: `lib/preflight.mjs` adds `shimAvailable(shimPath)` (pure, injectable — existence + executable-bit check) and `dotnetAvailable()` now returns true when EITHER the raw PATH probe succeeds OR the host shim resolves; `preflight()`'s return gains `dotnetViaShim` so `cmdPreflight`'s message distinguishes "available (on PATH)" from "available (via host shim — not on this session's own PATH, but every real verify call resolves it)" instead of collapsing both into a bare boolean. Live-verified against this host post-fix: `dotnet: available (via verify/build-test.local.sh host shim...)`. Selftest: 846 → 851 (`shimAvailable` pure coverage: executable/non-executable/nonexistent/empty-path cases via real temp fixtures, plus `dotnetAvailable(shimPath)` with an injected shim). |

| KI-E73 | **`cmdResume`'s checkpoint-visibility check hardcoded `cyc = ledger.cycle + 1`, even though its own comment said "same rule as reconstruct" — `cmdReconstruct` had ALREADY been fixed under KI-L63 to derive the relevant cycle from on-disk evidence (the MAX cycle among unfolded checkpoints), but that fix was never actually shared, so `cmdResume` silently drifted back to the naive +1 guess. A `resume --reuse` relaunch (KI-E69) keeps each item's OWN claim cycle rather than bumping to a new one, so a same-cycle checkpoint — the NORMAL shape for that recovery path — was invisible to `resume`'s in-flight listing.** Live-witnessed across the whole 2026-08-07 cycle-58 relaunch monitoring session: 7 separate items in that batch each finished with a complete, valid `result.json` and sat done-but-unfolded for tens of minutes apiece — `resume` never once flagged any of them as checkpointed (unlike a pre-existing, correctly-working case earlier the same session, where `resume` DID immediately flag one item's checkpoint because that checkpoint's cycle number happened to be strictly newer than the then-active cycle). Every occurrence required a manual `driver.mjs reconstruct` to discover. Fix: `deriveUnfoldedCycle(itemsRoot, ledger)` — the exact KI-L63 derivation logic — extracted out of `cmdReconstruct` into `lib/ledger.mjs` (pure, now unit-testable; it was previously inlined directly in `cmdReconstruct`'s body with zero test coverage of its own) and imported by BOTH `cmdResume` and `cmdReconstruct`, so the two call sites share one implementation and cannot drift apart again. Live-smoke-tested per the KI-E62 lesson (a driver.mjs change that moves/renames a binding must be exercised with a real invocation, not just selftest-pinned): `resume`, `reconstruct`, and `group --dry` all run cleanly post-change. Selftest: 851 → 858 (the exact same-cycle bug shape end-to-end, the already-folded exclusion, the KI-L63 multi-item MAX-wins case, a malformed-JSON item skipped gracefully, and 2 source-text pins confirming both call sites use the shared function with no re-inlined copy). |

| KI-E74 | **`decisions-digest`'s "Rule-together bundles" grouped parked owner-decisions ONLY by exact `target` (service) match, so the SAME underlying question recurring across DIFFERENT services was invisible to it — even though `cluster.mjs`'s `clusterBySimilarity` (theme + title-keyword overlap) already detects exactly this pattern for the SWEEP report.** Found live 2026-08-07: manual investigation of the 36-item parked queue found 7 items across 6 services (`ITEM-1`, `ITEM-20`, `ITEM-40`, `ITEM-41`, `ITEM-42`, `ITEM-44`, `ITEM-48`) were all the identical "build a shared `Shared.SharedTypes` assembly, or ratify permanent per-service duplication?" question — a single owner ruling would close all 7, but the digest scattered them as unbundled singleton rows because none of the 7 shared a `target`. Fix: `cmdDecisionsDigest` now ALSO runs the parked rows through the existing `clusterBySimilarity`/`sharedLabel` (already imported, already used for `suggest`/`group` batch-pattern stamping — no new classifier written), filtered to clusters spanning ≥2 DIFFERENT targets (same-target clusters stay owned by the pre-existing `byTarget` bundle, avoiding duplication). **Precision caveat surfaced during verification, not shipped silently**: live-testing against the real queue found the automated cluster (13 items / 10 services) is LARGER and more thorough than the 7-item manual count — it correctly also caught `ITEM-46` (a genuine match missed by hand) — but ALSO chain-merged 4 tangential items (`ITEM-6`, `ITEM-7`, `ITEM-10`, `ITEM-50`) that only share generic boilerplate phrasing ("owner ruling", "permanently unavailable" — common across this whole owner-decision batch's titles) with ONE other cluster member, not real subject-matter overlap; `clusterBySimilarity`'s union-find is chain-transitive (A~B~C merges even when A is not directly similar to C), which is an accepted tradeoff for its original SWEEP-triage purpose (a human reads every member individually before writing the canonical fix) but is a materially stronger, WRONG claim for decision-bundling ("one ruling closes all of these"). Rather than build a bespoke stricter clusterer, the section is worded as a candidate requiring verification — mirroring the existing KI-E36 "possibly already DELIVERED in HEAD — verify" posture — rather than an assertion; a partial ruling (some ids in the bundle, not all) is documented as the normal expected outcome. Selftest: 858 → 860 (2 source-text pins: the shared-clusterer wiring + cross-target filter, and the candidate/verify framing). |

| KI-E74B | **`VERIFY_SCHEMA` has always had a `note` field the runner MAY populate with an honest caveat, but `factory.js` captured the runner's structured return into a local `verify` variable and never read `verify.note` again anywhere — no gate, no review, no report. `review-pack.md` (the ONE artifact every review-role agent is told to read) is a pure git-diff snapshot with ZERO narrative capacity by design (its own generated header says "INPUT curation for reviewers, not an authored judgment"), so a runner's self-aware warning had NO channel to reach the review band at all.** Live on ITEM-H1, 2026-08-07 (the cycle-58 relaunch): the runner's own `verify.json` wrote, verbatim, "Build+targeted-test green here does NOT mean round 2's findings are resolved — they are not, and are unaddressed in this worktree as of this pass" — naming 5 specific unaddressed findings (a Development-boot crash, an unguarded malformed-BaseUrl DI-construction crash, an unbounded retry loop, retry-catch gaps, a timing-oracle amplification) from an EARLIER review round that no fixer round had ever touched. All 8 gates/reviews subsequently APPROVED anyway — none of them had any way to see that note, since it was never surfaced. The item would have folded CLOSED had an UNRELATED mechanism (KI-E70's deterministic transcript override, triggered by a completely different failing test class) not independently caught it. Fix: `factory.js` now sets `item.verifyNote = verify.note` immediately after the runner call (before any early return), and `compose()`'s review-role branch renders it prominently — "VERIFY-STAGE NOTE (from the runner's own verification pass — READ THIS, do not approve past it unverified)" — right after the REVIEW PACK instruction, framed as a hypothesis to independently re-verify (KI-L35 posture: never a conclusion to copy forward, since it may describe a stale/historical state). `agents/runner.md` gains an explicit instruction (mirroring the pre-existing fix-manifest-cross-check bullet's shape) to check `feedback.md`/`last-failure.md` for named findings on an unchanged worktree and say so plainly in `note` — "a green note that omits a known-unresolved finding you had evidence of is a worse failure than reporting a false fail". Partial parity: `opencode/compose.mjs`'s render-side is mirrored (the two `isReviewRole` branches are structurally identical, confirmed by the pre-existing KI-E60 parity precedent), but the SETTING half is a disclosed gap — `opencode/runtime.mjs` drives verification via external `mech verify` step-dispatch calls, not `factory.js`'s direct `await call('runner', ...)`, so there is no single insertion point to mirror without a deeper, untested-tonight architectural change; a future port pass should wire it (same disclosed-gap posture as KI-E69's opencode note). Selftest: 860 → 864 (source-text pins: the exact threading line runs before any early FAILED return, the render sits inside the review-role branch specifically — not the shared prefix every role sees — and the opencode render-side mirror). |

| KI-E74C | **KI-E67's narrative/verdict contradiction detector only fires in ONE direction — a FAILED/ESCALATED verdict contradicted by confident-DONE prose. The MIRROR direction (a CLOSED verdict contradicted by an artifact that ITSELF admits something is not resolved) had no detector at all, despite being the exact shape of the ITEM-H1 near-miss (KI-E74B): the runner's own verify.json admitted 5 findings from a prior review round were unaddressed, and all 8 gates approved anyway — only an UNRELATED mechanism (KI-E70's deterministic transcript override, on a completely different failing test class) happened to catch it.** This is a detective backstop for when KI-E74B's preventive fix (surfacing the runner's note directly in every review-role prompt) isn't enough on its own — an agent still misses the surfaced note, or a similar admission lands in some other artifact never explicitly prompted. Fix: `lib/narrative-check.mjs` gains `detectUnresolvedCaveatOnClose(toState, textsByFile)` — same pure, WARN-only, conservative shape as KI-E67 (mirrored exactly: fires ONLY on `toState === 'CLOSED'`, only on STRONG unambiguous non-resolution admissions modeled directly on the ITEM-H1 phrasing, never a bare appearance of "unresolved" alone — a legitimately-accepted LOW-severity deferral correctly does not trip it, verified against a crafted fixture). Wired into `cmdFold` right beside the existing KI-E67 loop, scanning every `.md` file PLUS `verify.json` (the proven real-world source of this admission) — deliberately broader than KI-E67's `.md`-only scan, which stays unchanged to avoid any risk to an already-shipped, working check. Live-verified against the exact ITEM-H1 text before wiring: the marker correctly fires on `toState:'CLOSED'` and correctly stays silent on `toState:'FAILED'` (the same admission on a FAILED result is an accurate description, not a contradiction) and on a clean control text. Selftest: 864 → 876 (all 4 marker phrases independently pinned, the live ITEM-H1 fixture end-to-end, the CLOSED-only gate, the legitimate-deferral non-false-positive case, and 3 driver.mjs wiring pins: the detector call + WARN tag, the CLOSED-only gate, and the verify.json inclusion). |
| KI-E81 | **Auto-generate the "died on exactly one late-pipeline stage, gates already APPROVED, nothing to remediate" recovery prompt — the manual work this session did 7 times by hand** (2026-08-16: four items each needed only a fresh `re-auditor`; seven items each needed only a fresh `integrator`) after a spend-limit outage killed the band mid-run. Every one required the controller to: read `last-failure.md`'s prose by eye to find the stage-reached line, manually pull the finding's title/acceptance/files from `state/findings-graph.json`, hand-write near-identical role-brief-inlined prompt boilerplate, and remember the exact `integrate-raw.txt` tee-path contract — real, repeatable, mechanical work with no tooling behind it. `driver.mjs recover <id>` already auto-generates a `regate-<role>.md` prompt for a structured DISSENT (a gate that said CHANGES_REQUIRED) but had NOTHING for a pure infra death, where `dissentersFrom` finds zero dissenters (the stage never ran at all — there's no verdict to disagree with) and the README fell back to a bare "no structured dissent found — read last-failure.md" pointer. Fix: `lib/recover.mjs` gains `missingStageFrom(transitions, gates)` (pure) — deliberately narrow, recognizing only the two cleanest, most common shapes: last transition `REAUDITED` -> only `integrator` missing; last transition `REFUTE_OK` -> only `re-auditor` missing, with the exact lens set read DIRECTLY off the already-recorded `gates.reaudit` string (e.g. `"code=NULL"` -> `['code']`) rather than re-deriving factory.js's theme/band-based `reauditLenses()` selection logic a second time (which the Workflow-runtime/driver.mjs split, KI-E2, makes awkward and risky to share safely) — any other shape (died before any gate ran, or at GATED with refuter/reaudit both still missing, or a multi-lens reaudit with no recorded string) returns `stage:null` and falls back to a normal re-group, matching the judgment calls this session made by hand for the harder cases. The data source is a NEW `state/items/<id>/last-failure.json` structured sidecar — `cmdFold`'s existing loop (which already writes `last-failure.md` for every FAILED result, purely from the in-memory `r` object) now ALSO dumps `{transitions, gates, infraSuspect, note}` as JSON right next to it; deliberately NOT `result.json` (KI-L40's end-of-lifecycle checkpoint), because the checkpoint-writer agent is exactly as likely to die to the SAME spend-limit outage as everything else in the band — most items in this session's live incident had no `result.json` at all for that reason, but `last-failure.md`/`.json` are written by the DRIVER from data it already holds, independent of any agent surviving. `cmdRecover` now calls `missingStageFrom` whenever `dissentersFrom` finds zero dissent, and when it recognizes a shape, writes `state/items/<id>/recovery/recover-stage-<role>.md` — the SAME role-brief-inlining machinery the dissent path already uses, reusing the pre-existing `solutionFor(target)` resolver (services.json + `<Target>/<Target>.sln` convention) for the integrator's build command rather than a second, divergent lookup, and ending with the same parseable `VERDICT: ...` line convention this session's hand-written prompts used throughout. Surfaced in both the generated README.md (a new step "1b") and the CLI summary output, never silently only on disk. Enrichment only, never a gate: a null/unrecognized shape changes nothing about the existing dissent-only behavior (confirmed via a live `cmdRecover` smoke test against a real pre-existing FAILED item with no `last-failure.json` yet — identical output to before this change). Deliberately does NOT auto-generate a `refuter` prompt or handle a multi-lens FULL-band re-audit death — this session judged those not worth the added complexity/risk for a single item and folded it into a normal re-group instead; a future pass could extend `missingStageFrom` to that shape using the same `gates.reaudit`-string-reading trick. **This host's KI-E76 (recovery-gate-freshness mtime check) has not been ported here** — a sibling fix, KI-E80, extends KI-E76 with a content-hash freshness fallback; since E76 isn't present in this repo, E80 is a disclosed gap here rather than ported (see the source project if/when E76 lands). `missingStageFrom`'s full decision table (single-lens, multi-lens, GATED-death, pre-gate-death, empty/undefined-input never-throws cases) plus wiring pins confirming `cmdFold` writes the sidecar in the same pass, `cmdRecover` gates the auto-detection behind zero-dissent, reuses `solutionFor`, and surfaces the generated path in both outputs are covered by the lib self-test. |
| KI-E82 | **`main-check <id>` (KI-E50) already existed but required the caller to already know which ids to suspect — and fold's own KI-L65 auto-repair only ever inspects items in the CURRENT fold batch, never a sweep of everything the factory has ever touched.** Live 2026-08-16, same session as KI-E81: a 17-item fold's auto-repair correctly caught and restored exactly 1 stray main-tree file — the one matching an item IN that fold's batch (`ITEM-H23`) — but a manual `git status` sweep of the REAL working tree immediately afterward found 6 MORE contaminated files (5 modified + 2 new untracked) sitting there, leaked by items from TWO EARLIER, already-folded rounds this same session (`ITEM-M23`; another service's item), each independently confirmed byte-near-identical to its own worktree's copy (an agent writing outside its worktree, the exact KI-L65 shape) before being restored to HEAD by hand. Neither that fold nor any later one ever re-checks an item once its OWN fold has happened — the contamination sat undetected in the actual application repo for two full rounds. Fix: `cmdMainCheck` gains an `--all` flag (and a bare `main-check` with no ids ALSO sweeps, matching the live incident where nobody thought to pass any ids at all) that lists every item directory carrying a claim-time `main-snapshot.json` — i.e. every id the factory has EVER claimed, any cycle, not just the current batch — and checks all of them in one read-only pass. Deliberately reuses the EXISTING `driftAgainstSnapshot`/`splitDriftByStatus` decision logic unchanged (committed-drift vs dirty-drift classification is untouched and already correct — live-verified against the real 97-item history: `ITEM-H21`/`ITEM-H22`/etc. correctly read as "committed drift (human delivery)" since the owner had already reviewed and committed those, while the 6 real contamination files, before this fix's manual cleanup, would have read as `⚠ MAIN-DRIFT`) — this is purely a wider id list, not new classification logic, so the well-tested KI-E50/KI-L65 semantics carry over exactly. Live-verified end-to-end on the real repo post-fix: `main-check --all` swept all 97 claimed items, zero `⚠ MAIN-DRIFT` remaining (confirming the manual cleanup was complete) alongside the expected `committed drift` rows for genuinely-already-delivered items. Operational note for future sessions: run `main-check --all` periodically during a multi-round session (not just relying on each fold's own narrower auto-repair) — cheap, read-only, and the only way to catch contamination from a round that already closed. Selftest: 1010 → 1016 (wiring pins: flags threaded through the dispatch, `--all`-or-bare-call triggers the sweep, the id set is derived from actual on-disk snapshots rather than guessed, and the sweep does not reimplement drift classification). |
| KI-O4 | **`_workflow/opencode/`'s binding is controller-agnostic, not OpenCode-specific — GitHub Copilot verified as a second controller with zero new code.** Nothing in `runtime.mjs`/`compose.mjs`/`routing.mjs`/`schemas.mjs`/`buildtest.mjs` actually depends on OpenCode; the CLI just emits a generic `{mechanical:"<step>"}` / `{agents:[...]}` protocol over stdout and reads/writes plain files (`state/items/<id>/opencode-progress.json` kept as the checkpoint's on-disk name for historical continuity with KI-O1 — cosmetic only, not OpenCode-specific behavior). VS Code Copilot Chat in agent mode and the GitHub Copilot coding agent both have a terminal/run tool, so the exact same `init`/`next`/`submit`/`mech`/`status`/`finalize` sequence documented in `_workflow/opencode/README.md`'s "Usage protocol" applies to them verbatim — see that README's new "Using this binding from GitHub Copilot" section. **New fidelity gap, specific to any subagent-less controller (Copilot included):** unlike OpenCode's `Task` tool, Copilot has no documented "spawn an independent subagent" primitive, so a Copilot controller session must play every role (`architect`/`developer`/`qa`/`security`/`po`, the review-flow roles, etc.) itself, sequentially, in one conversation — this weakens `PLAN.md`'s "the review stage is separate adversarial subagents, never nested in the doing agent" invariant, since the reviewer is no longer independent of the implementer's own reasoning trail. Disclosed and accepted, not fixed by this change — same posture as KI-O1's "no per-call model tiering" gap. Also added: `.github/copilot-instructions.md` (this repo, direct) and a host-installable `copilot-assets/copilot-instructions.md` template that `setup/init.mjs` copies to a host repo's `.github/copilot-instructions.md` (opt out with `--no-copilot-assets`), mirroring the existing `claude-assets/` → `.claude/skills/` install step exactly (same `copyTree()` helper, same `*.factory-new` no-clobber behavior on a locally-edited host file). |
| KI-E83 | **The `FACTORY::RED::` marker's disk-authoritative validity was checked in exactly ONE place: `deterministicVerifyOverride`'s P1 check at FOLD time — AFTER the entire expensive gate band (4-5 gates + refuter + reaudit + integrator) had already run and approved the item.** The test-author's OWN self-reported `test.red` field IS checked in-run, immediately after that call — but KI-E10 already proved, for the SAME shape of marker (`FACTORY::REALINFRA::`), that a self-reported field can diverge from what the agent's own artifact shows on disk (an earlier item, cycle 26). Nothing had applied that same lesson to the RED marker. Live 2026-08-16, same session as KI-E81/E82: `ITEM-H24` (9 gates APPROVED) and `ITEM-H26` (8 gates APPROVED) each burned a full band before being rejected at fold on `FACTORY::RED::0` — a vacuous regression test that passes on both old and new code, present since the ORIGINAL fixer round and caught by none of the 17 gate/review agents across both items combined, only by the fold-time grep neither of them ever saw. Fix: `factory.js` gains a `RED-PROOF MARKER PROBE` (KI-E83) — the KI-E10 realInfra-marker-probe SHAPE applied verbatim to this sibling marker: a cheap haiku agent runs `grep "FACTORY::RED::" <worktree-item-dir>/verify-red-raw.txt | tail -1` (mirroring `parseRedRaw`'s own "last match wins" semantics — a retry-appended transcript is handled the same way both places) and reports `markerFound`/`exitCode`/`line` back verbatim, no interpretation. Wired in immediately after `res.transitions.push('GREEN', 'BUILT', 'TESTED')` — the EARLIEST point the marker can exist — strictly BEFORE the edge-scan/acceptance-scan/leftover-scan/registration-drift-scan pre-band probes and the entire gate band. `markerFound:false` or an exit code that doesn't match the item's contract (`exitCode !== 0` for a normal item — vacuous; `exitCode === 0` for a `verificationOnly` item — KI-L55's INVERTED contract, the pinning/coverage test must PASS on the current tree) fails the item immediately, `finish('FAILED', ...)`, before a single gate-agent token is spent. The fold-time P1 check is explicitly RETAINED unchanged as the deterministic backstop — this is a fail-fast optimization layered in front of it, never a replacement (a null/unavailable probe fails OPEN, proceeding to the band exactly as every sibling pre-band probe already does, per the established fail-open posture). New `RED_PROOF_SCHEMA` also added byte-identical to `opencode/schemas.mjs` for the mechanical schema-parity selftest gate (Fix #20). *(Port status, corrected by KI-E112: this passage disclosed the probe as "NOT yet ported (a future port pass should wire it)" and cited a `KI-E75` that has never existed as a row in this registry — a dangling reference. The gap is now CLOSED: `afterVerify` enforces the RED-proof contract mechanically, and the KI-E103 runtime-parity gate makes a re-opened gap a selftest failure rather than a prose footnote.)* Selftest: 1016 → 1022 (three exec-smoke behavioral proofs, not just source pins: a vacuous `exitCode:0` on a normal `.cs` item fails BEFORE any gate/review/refuter/reaudit/integrator call — the actual token-savings claim, verified against the real `calls` list the harness records, not assumed; a missing marker fails identically; and the `verificationOnly` inversion, proven on a genuine `.cs` item since the obvious `SMOKE-VONLY` fixture turns out to be a config/YAML-only item — `codeChange:false` — so the probe correctly never even runs for it, a live discovery while writing the test, not a bug in the shipped code). |

| KI-O5 | **OpenCode was a first-class RUNTIME concern and a zero-class INSTALLATION concern: `setup/init.mjs` installed a host pointer for Claude Code (`.claude/skills/`) and, since KI-O4, for Copilot (`.github/copilot-instructions.md`), but an OpenCode controller sitting in a host repo had NO installed pointer at all** — `_workflow/opencode/` was reachable only by a session that already knew to go looking for it, and the only documents that mention the binding (`CLAUDE.md`, `.github/copilot-instructions.md`) are either not loaded by OpenCode or describe a different controller's affordances. Fix, mirroring the KI-O4 precedent: a third asset dir `opencode-assets/` with two halves, because they have two ownership stories. (a) `opencode-assets/root/**` are factory-owned FILES that the existing `copyTree()` installs onto the HOST ROOT with the identical no-clobber contract (byte-identical → silent no-op, absent → created, locally-edited → `*.factory-new` sibling + warning): `AGENTS.md`, `.opencode/ai-factory.md` (the always-on rules), `.opencode/skill/ai-factory/SKILL.md` (the controller manual, rewritten around `runtime.mjs` rather than the `Workflow` tool, and mandating one independent `Task` subagent per role — the invariant KI-O4 records Copilot as structurally unable to honour). `AGENTS.md` is the one asset a host is genuinely likely to already own, so the shipped copy is deliberately a short POINTER and the substance lives at `.opencode/ai-factory.md`, a factory-owned path that cannot collide — a host that keeps its own `AGENTS.md` still gets the full rule set, via `instructions`. (b) `opencode-assets/opencode.config.json` is a FRAGMENT that is **merged** into the host's own `opencode.json`, not copied over it: that file carries the host's model/provider/MCP settings. `lib/hostinstall.mjs`'s `mergeOpencodeConfig` does it, and it is a real function rather than an object spread at the call site for one specific reason — **opencode evaluates the LAST matching permission pattern, and a JS spread keeps an existing key in its ORIGINAL insertion position on overwrite**, so `{...host, ...factory}` would leave a factory rule installed by a PREVIOUS run stranded behind a broad host rule declared after it (e.g. `git *: allow`) and the invariant would be silently off. `appendRulesLast` drops factory-owned keys from their old positions and re-appends the whole block at the tail; the reordering is REPORTED, never silent. A host config that is not strict JSON, or whose top-level `permission` is the bare-string form (which applies to EVERY tool, including ones the factory names no rules for — expanding it would silently narrow read/glob/grep/list/task posture), is REFUSED with the factory block written as `*.factory-new`, the same no-silent-damage posture as `install.sh`'s `settings.local.json` guard. **Policy shape, deliberately not a blanket deny:** `opencode.json` is repo-wide, not factory-session-scoped, so denying mutating git outright would also block ordinary OpenCode-assisted development in the host repo — which the factory's own rules explicitly permit ("implementing something yourself, outside the factory, is fine", `copilot-assets/copilot-instructions.md`). So bare `commit`/`add`/`checkout`/`restore`/`stash`/`reset`/`clean`/`push` (with and without `-C`) are **`ask`** — normal work costs one keystroke and "the human authors every commit" becomes a prompt that must be answered — while the same verbs aimed at a factory worktree (`git -C *worktrees* ...`, one pattern covering both `/` and `\` separators), deleting `state/STOP_REQUESTED.md` (`rm`/`del`/`Remove-Item`), and editing `state/ledger.json` are hard **`deny`**. `git worktree add` is deliberately NOT caught: `lib/worktree.mjs:58` invokes it WITHOUT `-C`, and every pattern is anchored on `git -C ` or a bare mutating verb. Also added: `setup/install.mjs`, the Node twin of `install.sh` for hosts with no usable bash (Windows without Git Bash/WSL, locked-down CI images) — same contracts (strict `vX.Y.Z` resolution via the shared `pickLatestReleaseTag`, selftest gate on install AND upgrade, rollback on a red upgrade, transactional removal of a partial install, vendored-mount refusal, explicit-`--host`-beats-self-location), plus a `controllers` command that re-installs host assets only. It delegates ALL host-side wiring to `init.mjs`, so the three controller seams have exactly one implementation and cannot drift; telemetry is explicitly NOT reimplemented (docker + shell profiles, no meaningful Windows story) and the gap is disclosed in `--help` and in `install`'s final line rather than silently omitted. **Live-caught during verification, before shipping:** `readFileSync(p,'utf8')` does not strip a UTF-8 BOM and `JSON.parse` rejects one, so a host `opencode.json` saved by PowerShell 5.1's `Set-Content -Encoding utf8` / older Visual Studio / Notepad read as "unparseable" and took the REFUSING-to-touch path — the factory's OpenCode policy would then have silently never installed on precisely the Windows hosts `install.mjs` exists to serve; `stripBom`/`parseJsonFile` fix it, and a malformed-JSON case is pinned alongside so the tolerance can never swallow a real parse failure into a clobber. **Accepted limitations:** the permission patterns are matched against the command string, so `git -C <path> worktree list` and similar read-only `-C` forms that happen to contain a mutating verb as a later word can prompt spuriously (safe direction, one keystroke); and an `ask` rule is only as strong as the human answering it — it is a visible decision point, not an enforcement boundary. Selftest: 898 → 972 (the reordering invariant stated directly, idempotency of a re-merge, per-tool string-shorthand promotion vs top-level bare-string refusal, host-setting preservation, BOM tolerance + malformed-JSON throw, the fragment's key set against opencode's `additionalProperties:false` Config, the ask/deny split per verb, the `git worktree add` exemption, all six asset paths, the OpenCode skill's required frontmatter and its `runtime.mjs`-not-`Workflow` framing, init.mjs's step-4c wiring, and install.mjs's parity contracts). |

| KI-E86 | **Ported from a host-mount session (2026-08-24, originally found on the Azathoth marketplace mount): concurrent items run in SIBLING worktrees on the SAME host under `group --conc N`, and by default `dotnet build`/`dotnet test` leave persistent MSBuild worker processes running for reuse by the NEXT invocation — a well-documented source of intermittent cross-invocation contention/staleness under concurrent CI-style builds, independent of the code under test.** Fix: `export MSBUILDDISABLENODEREUSE=1` once, at the top of `verify/build-test.sh`, right after `set -uo pipefail` — covers every dotnet invocation in the script (present and future) without touching each call site individually. Zero-risk: only changes whether MSBuild worker processes are cached between invocations, no correctness impact. No selftest delta (a shell-script environment export has no pure-function surface to pin; verified by inspection that the export precedes every `dotnet` call in the file). No findings against this entry from the 2026-08-25 multi-lens review (see KI-E87's entry). |

| KI-E87 | **Ported from a host-mount session (2026-08-24) — PlanCommitmentScan: no stage in the pipeline cross-checked the diff against the PLANNER's OWN stated commitments — every stage checked the diff against the ITEM's external acceptance criteria (KI-E18) or general code quality, a different axis, so a plan/execution drift was invisible until the re-audit's holistic pass, at full band price.** Origin evidence: a host-mount session's own batch burned a FULL band (editorial x2, gate:architect, gate:developer, gate:qa, review:code/adversarial/edgecase/testreview, refuter) before the re-auditor — the LAST stage in the pipeline — caught that `plan.md` explicitly said "**MUST include the brownfield note**" while the delivered diff never included it. Fix, mirroring the proven KI-E18/KI-D12 shape: (1) hoist `plan` to function scope in `runItem()` (`let plan = null` before the `if (R.planner)`, assigned inside it) — `null` for a mechanical item (planner skipped), naturally gating the new probe off for that whole class at zero cost. (2) New pure deterministic pre-filter `hasPlanCommitmentLanguage(text)` (`lib/plan-commitment.mjs`, inlined byte-identically into factory.js per the KI-E2 split) detects explicit "MUST"/"must include/add/update/…"/"required to" language in `plan.approach`+`plan.blastRadius` — a coarse gate (not a clause splitter) since commitment language in free prose is sparse and irregular, unlike the acceptance field KI-E18 splits. Ships with the `[\s-]+` separator refinement already folded in (not `\s+`-only) — the origin session found live that a hyphenated title-case compound ("Must-cover checklist") defeats a whitespace-only pattern; proven against the exact specimen before widening. (3) New pre-band probe "PLAN-COMMITMENT SCAN" (`PLAN_COMMITMENT_SCHEMA`, cheap haiku), positioned AFTER the acceptance-scan and BEFORE the comment-scan: reads the plan's commitment language + `review-pack.md`, judges `honored`, with ONE bounded fixer amend + ONE re-probe on a confirmed gap (identical safety-valve shape to acceptance-scan) before a still-unhonored commitment fails the item PRE-BAND. Fail-open throughout: a null/malformed probe never sinks the item (verified: exactly one `finish('FAILED', ...)` call site in the whole block). `opencode/schemas.mjs` gets `PLAN_COMMITMENT_SCHEMA` for the mechanical schema-parity selftest gate, mirroring the KI-E83/`RED_PROOF_SCHEMA` precedent exactly — schema-parity only: the opencode runtime's own routing/dispatch of this probe is NOT yet ported. Selftest: 974 → 1020 (combined KI-E87+E88+E89 delta — see KI-E89 below): `hasPlanCommitmentLanguage`'s positive/negative/word-boundary cases incl. "mustard"/"mustache" false-fire guards and the "Must-cover" hyphen-separator case; the factory.js inline-copy byte-parity pin; full pipeline wiring pins (schema, gate key, the `plan` hoisting, the gate condition, the bounded-amend-then-reprobe shape, the single fail path). **Multi-lens review + fixes (2026-08-25, run against both this repo and the origin host-mount session, all findings cross-ported):** a 4-lens agent review (correctness, cross-repo consistency, test quality, doc accuracy) found 13 real, independently-verified issues across the KI-E86..E89 port (all 13 confirmed by an adversarial second pass). Four landed here: (1) **the "must + verb" pattern was a CLOSED 14-word verb allowlist** — tested against 14 realistic commitment sentences a planner would actually write, 13 of 14 were silently missed because their verbs weren't on the two-incident-derived list. Since this function is the SOLE gate deciding whether the probe runs at all, every miss meant zero plan-vs-diff cross-check. Widened to `\bmust[\s-]+\w+` (any word, not a fixed list). (2) **The KI-E69 cross-session plan-reuse stand-in carried NO text fields at all**, so on any relaunched item this scan silently, permanently never fired regardless of what the real on-disk `plan.md` promised. Fixed: the stand-in now also carries the raw `plan.md` text verbatim as `approach`. (3) **The PLAN-COMMITMENT AMEND note-only dead end** — see the KI-E18 entry above; the identical fix applied here too (and to this repo's own KI-E18 acceptance-scan, which had the same bug). (4) **`meta.description` was already accurate here** (this repo's copy of that field already named the plan-commitment scan stage — only the origin host-mount session's copy was missing it, fixed there). Also reconciled a cross-repo finding: the KI-E87 probe's LLM-facing prompt text had been independently rewritten in each repo rather than ported byte-for-byte, undocumented as an adaptation (unlike KI-E88's explicit one) — now documented in-line as an accepted adaptation (prompt prose has no byte-parity convention anywhere in this file; only deterministic logic does). Selftest: 1020 → 1046 (combined with the KI-E88/E89 fixes below — see those entries). |

| KI-E88 | **Ported from a host-mount session (2026-08-24), adapted — the picker (`driver.mjs suggest`) was item-COUNT-blind to item-COST: a FULL-band item runs the full 5-gate opus panel (architect/security/po + developer/qa) plus an opus-routed planner/testAuthor/fixer; LIGHT skips 3 of those 5 gates and the planner entirely, roughly a 4x gate-panel difference, and the printed item count gave zero signal for which a given batch would land.** Adaptation note: the origin session's fix targeted a mixed-batch severity-bucketed fallback section of `suggest` that does not exist in this repo — this repo's `suggest` is entirely similarity-cluster-based (`clusterBySimilarity` + clique/co-batch pick, owner directive 2026-07-04). Ported the reusable half verbatim (`lib/band.mjs`, a driver-side Node-importable port of factory.js's canonical `bandFor()`/`BAND_FULL_THEMES` — the Workflow-runtime definition itself cannot be imported, KI-E2 split; byte-identical to factory.js's copy, pinned) and re-implemented the DISPLAY half against the actual cluster-based shape: a whole-pool band-mix line right after `suggest`'s existing summary line, plus a per-cluster band-mix tally appended to each cluster's own header (`#n [theme] ... (band mix of the N-item pick: X LIGHT / Y FULL)`). Purely additive/read-only — the clustering, the clique/co-batch pick logic, and the `group --ids` recommendation are all unchanged. Selftest: see KI-E89 below for the combined final count — `bandFor`'s full priority-order unit tests, a `BAND_FULL_THEMES` byte-parity pin against factory.js, a `bandFor` function-body byte-parity pin, and driver wiring pins for both the whole-pool and per-cluster band-mix lines (targeting this repo's actual `cmdSuggest`, not the origin session's fallback shape). **Fix (multi-lens review, 2026-08-25 — see KI-E87's entry for the review itself):** two test-quality gaps. (1) No case combined a `BAND_FULL_THEMES` theme WITH `fixType:'mechanical'` in one call — `bandFor`'s own P5 safety comment names exactly this interaction ("a mechanical authz/HMAC/tenant-filter edit is still a security change whose load-bearing reviewer must NOT be dropped"), but nothing pinned it: a future edit swapping the order of the two `if`s would have silently downgraded a security-themed mechanical item from FULL to LIGHT with zero test failures. (2) The `BAND_FULL_THEMES` array-parity extraction had no null-guard before its `eq()` comparison, unlike the sibling `bandFor`-body check. Both closed with regression tests. |

| KI-E89 | **Ported from a host-mount session (2026-08-24) — `main-check`'s per-item snapshot loop (incl. its own KI-E82 `--all` widening, already present in this repo) could only detect drift on a path that was part of some CHECKED item's claim-time snapshot; a brand-new leaked path no item ever declared had no snapshot to diff against and was structurally invisible to it, even under the `--all` sweep.** Origin evidence: a host-mount session's batch validation found real leaked contamination (one tracked-file edit + two new untracked paths) sitting clean through both the closing item's own auto-repair and a fresh `main-check --all` sweep — a manual `git status --short` was the only thing that caught it, exactly the KI-E82 motivating shape one layer deeper (E82 widened WHICH ids get checked; this closes what happens when NO id's snapshot covers the leaked path at all). Fix: new pure helper `unclaimedMainDrift(dirty, mountRel, claimedPaths)` (`lib/mainguard.mjs`, alongside its KI-E14 sibling `dirtyMainPaths`) — given the raw dirty/untracked state of the main tree, the factory's own mount prefix (its bookkeeping — ledger/reports/queue/state — is ALWAYS legitimately dirty during active use and is excluded, never reported as noise), and the union of every path any CHECKED item has claimed (accumulated from every checked id's snapshot `files[]` keys, regardless of drift status — the exact ceiling of what KI-E82's own widened loop is even capable of seeing), returns whatever main-tree dirt remains unaccounted for. `cmdMainCheck` calls it once after its existing per-id loop (unmodified from KI-E82: still `flags?.all` triggers the full-history id sweep) and prints a distinctly-labeled `⚠ MAIN-DRIFT unclaimed (KI-E89)` warning — still read-only/warn-only, no auto-repair (an unclaimed path has no snapshot to prove what "repair" would mean). Ported here as an UNMODIFIED port (not an adaptation) — this repo's `cmdMainCheck` already had the exact `(rest, flags)` + `--all`-sweep shape the origin session built KI-E89 against. Verified via a unit-level real-git-repo fixture (mirroring the origin session's own CLI-level live repro of the identical incident shape) — a synthetic leaked directory under an already-tracked sibling path correctly surfaces, while a claimed file and the mount's own bookkeeping churn both stay silent; `main-check --all` also smoke-run live against this repo's own (currently empty) ledger — confirmed it reaches and exits the new code path cleanly (no claimed items exist here yet to demonstrate a live positive-fire the way the origin session did on its own populated ledger). Selftest: 974 → 1020 (KI-E87 + KI-E88 + KI-E89 combined delta: `unclaimedMainDrift`'s pure predicate cases, the live-repo-fixture proof, and driver wiring pins for `cmdMainCheck(rest, flags)`'s actual KI-E82-widened shape; `bandFor`'s coverage per KI-E88; `hasPlanCommitmentLanguage`'s coverage per KI-E87). **Fix (multi-lens review, 2026-08-25 — see KI-E87's entry for the review itself):** three real correctness gaps, all zero-test-coverage before this pass. (1) **`claimedPaths` was scoped to only the explicitly-requested id(s)** on a targeted (non-`--all`) call — correct under `--all` but wrong on the far more common single-id call every item's own mid-band Verify stage makes: any OTHER item's legitimately snapshot-backed but not-yet-committed change sitting in main was misreported as unclaimed/leaked contamination. Fixed by computing the full claimed-id inventory unconditionally and seeding `claimedPaths` from THAT, decoupled from which id(s) this invocation is targeting for its own drift re-hash. (2) **`unclaimedMainDrift`'s `dirs` branch never consulted `claimedPaths` at all** — a directory an item legitimately declared in its own files[] was unconditionally, permanently reported as unclaimed, directly contradicting this function's own header comment. Fixed by mirroring `filesOverlapDirty`'s reversed-direction check. (3) **`dirtyMainPaths` stripped only the OUTER quotes** git wraps around a path containing a quote/backslash/non-ASCII byte, leaving the C-style `\NNN` octal escapes literally in the string — verified live against real git output (a backslash, an embedded quote, and a UTF-8 filename all previously came out mangled). Fixed with a small, narrowly-scoped decoder that only activates on an already-quoted path. Selftest: 1020 → 1046 (combined with KI-E87/E88's fixes above — see those entries). |
| KI-E90 | **Ported from a host-mount session (2026-08-28) — `group` had no signal for same-`target` batch-pairing risk.** The KI-E14 file-lock check only compares DECLARED `files[]`, but a fixer may legitimately touch a file OUTSIDE its declared lock-set when "strictly required" (fix.json's own documented escape hatch). Origin evidence: two same-`target` items in one batch had ZERO declared `files[]` overlap and both cleared the KI-E14 check, yet one item's fixer discovered mid-run it also needed a file the OTHER item held, and FAILED on a pure batch-scheduling collision neither item's own content caused. Fix: new pure helper `sameTargetPairs(picked)` (`lib/ledger.mjs`, alongside its KI-E29 sibling `closedDepsWithLiveWorktree`) returns `[{target, ids}]` for every target with 2+ picked items; `cmdGroup` prints a `KI-E90 WARN` after the existing file-lock/KI-E29 checks. WARN, don't exclude (KI-E29 posture) — same-target pairing is often fine, nothing is clobbered by it; the operator judges whether to split the batch. Ported here as an UNMODIFIED port (this repo's `cmdGroup` already had the exact same file-lock + KI-E29 shape the origin session built KI-E90 against). Selftest: 1046 → 1052 (pure-function cases + a `sameTargetPairs(picked)`/`KI-E90 WARN` wiring pin). |
| KI-E91 | **Ported from a host-mount session (2026-08-28) — no check mechanized `standards-evolution.md §3.4`'s own documented "ripple check"** (grep for the tag, confirm the ledger anchor exists, confirm the entry isn't self-contradictory) — left to a human/reviewer's memory, caught (if at all) only by a full expensive adversarial review + adjudication. Origin evidence: a doc-drift-themed item authored the SAME anchor slug as a top-level entry in TWO sibling ledger files (a root ledger + a subrepo-scoped ledger) with materially different Created dates / Standard citations / Legacy-sites content for what is presented as the SAME divergence, and separately asserted a `standards-evolution:` call-site tag was present in five files that grep proved did NOT carry it — both caught only after a full band + adjudication, and the same duplicate-entry content later leaked into an UNRELATED item's main-tree contamination the same cycle (a stale claim-time snapshot happened to expose it). *(This repo has a single `_bmad-output/tech-debt/STANDARDS-DIVERGENCE-LEDGER.md`, not the origin session's root+subrepo pair — the duplicate-anchor check still applies to any two ledger files a host configures, and degrades to a no-op when only one exists.)* Fix: new pre-band haiku probe `ledger-anchor-probe` (`factory.js`, mirrors the `leftover-probe`/KI-D12 STEP-1-mechanical/STEP-2-classify shape — this repo has no `registration-drift-probe`/KI-E77 at all, so that origin-session precedent doesn't exist here to mirror) — gated on declared `item.files` naming a `*STANDARDS-DIVERGENCE-LEDGER.md` path; new engine-owned `build-test.sh ledger-anchor` subcommand (joining the `leftovers|comments` dispatch case) backed by pure module `lib/ledger-anchor.mjs` + CLI wrapper `ledger-anchor-lint.mjs` finds candidates deterministically (added/edited anchor headings, cross-checked against every configured ledger file for the same slug at the same heading level, plus any claimed `standards-evolution:` call-site tag cross-checked against the actual file); the haiku probe judges each candidate for material disagreement. `LEDGER_PATHS` in `ledger-anchor-lint.mjs` currently holds this repo's SINGLE ledger path (`_bmad-output/tech-debt/STANDARDS-DIVERGENCE-LEDGER.md` — no subrepo-scoped sibling), so the duplicate-anchor half always finds zero candidates here (correct degrade, not a bug — verified by a dedicated selftest case); the false-tag-claim half stays fully active. Fail-open (only acts on an explicit boolean), FAILS the item pre-band on a hit. **Port status**: `LEDGER_ANCHOR_SCHEMA` is exported from `opencode/schemas.mjs` and, since KI-E112, the opencode runtime DISPATCHES this probe (mechanical `build-test.sh ledger-anchor` STEP-1 in `mech leftover`, gated on the item's declared files naming a `*STANDARDS-DIVERGENCE-LEDGER.md` path, then a `ledger_anchor_classify` agent step mirroring `leftover_classify`). *(Correction, KI-E112: this row previously claimed "this repo has no `schema-parity` selftest gate forcing opencode runtime parity". That was FALSE — a mechanical schema-parity gate does exist in `opencode/_selftest.mjs` and is run by the main suite. The accurate statement was that no RUNTIME-parity gate existed; KI-E103 then built one, and KI-E112 closed the gap this row was disclosing.)* Selftest: 1052 → 1079 (pure-function coverage for all six exported `lib/ledger-anchor.mjs` functions incl. a CPS-H-15-shaped duplicate-anchor repro and the single-ledger no-op-degrade case, plus `build-test.sh`/factory.js pipeline wiring pins; two pre-existing KI-D12/KI-E59 literal-string wiring pins updated for the extended `leftovers|comments|ledger-anchor)` case pattern). |
| KI-E92 | **Ported from a host-mount session (2026-08-28) — mechanical fixer/test-author routing (`fixer.mechanical`/`test_author.mechanical`) re-routed `claude-sonnet-5` → `claude-sonnet-4-6`** (owner directive, in response to the origin session's own batch close-rate regression). Origin ledger cost-bucket evidence confirmed `claude-sonnet-4-6` was the mechanical fixer/test-author default before a routine current-gen swap to `claude-sonnet-5` — never shown worse; the only documented revert on that route was an unrelated `claude-haiku-4-5` experiment. Updated in all three places routing is defined in this repo: `config/model-routing.json` (`fixer.mechanical`/`test_author.mechanical`), `factory.js`'s inline `RT` table (`fixerMech`/`testMech`), the pinned selftest assertion, AND `_workflow/opencode/routing.mjs`'s mirror copy. |
| KI-E93 | **Ported from a host-mount session (2026-08-28) — `test-author.md` had no explicit self-check for MULTI-target acceptance criteria.** When a finding's acceptance names several controllers/methods/files needing coverage, nothing forced the test-author to enumerate and cover every one rather than a representative sample. Origin evidence: an item failed the identical pre-band acceptance-scan twice in a row — its acceptance named three controllers needing HTTP auth-matrix test coverage, and both attempts covered only one of the three. **Fix**: `agents/test-author.md` gains a new numbered "MULTI-TARGET COVERAGE SELF-CHECK" step — enumerate every named target explicitly and check the acceptance text off target-by-target against `testFiles` before returning. Prevention, not detection. |
| KI-E94 | **Ported from a host-mount session (2026-08-28) — `fixer.md` had no self-check for a fix that touches one instance of a repeated pattern (a switch arm, a method overload, a near-identical sibling) while leaving other instances of the identical defect untouched, nor for verifying a fix's own writes are actually consumed by anything.** Origin evidence: one item fixed one arm of a remediation switch while the next arm — the same defect class, a write nothing reads — shipped unfixed; a separate item's actual money-correctness fix was inert dead code, papered over with an unescalated "known limitation" comment, caught only by a unanimous multi-reviewer rejection. **Fix**: `agents/fixer.md` gains a new numbered "SIBLING-PATTERN SWEEP" step (grep for every other instance of the touched pattern before finishing, fix each one carrying the identical defect) plus a "DEAD-CODE SELF-CHECK" (before finishing, name the specific downstream reader of every write/increment/cache-set the fix adds or relies on). |
| KI-E95 | **Ported from a host-mount session (2026-08-28) — `fixer.md`'s existing DOC-CLAIM (KI-E11) and COUNT-CLAIM (KI-E51) self-checks catch a phantom path or a wrong count, but nothing caught a fully plausible-sounding, wholly INVENTED config key / default / behavior, nor a stale claim left standing elsewhere in the same document being corrected.** Origin evidence: a new AGENTS.md-style doc invented a config key name, its default (backwards), and its behavioral semantics (nonexistent) — asserted confidently in five places, shipped with a fully green test run because nothing in the diff tested the documentation's own accuracy against source; a separate item left a stale claim standing elsewhere in the same doc file it was correcting. **Fix**: `agents/fixer.md` gains a "NO-INVENTION SELF-CHECK" (every factual prose claim must trace to an actual grep/read in this worktree, never recalled from a plausible-sounding convention; if you can't find where a behavior lives, say so instead of guessing) and an "ADJACENT-CLAIM RE-CHECK" (re-read the whole surrounding section — not just the edited line — for other claims about the same subject that may also now be wrong; re-verify any exact line-number/text citation against current file content immediately before finishing). |
| KI-E96 | **Ported from a host-mount session (2026-08-28) — `code-style.md`'s CancellationToken rule ("ALWAYS pass CancellationToken through the entire async call chain") had no self-check in `fixer.md` at all** — the violation compiles cleanly, which is exactly why it keeps shipping. Origin evidence: a fix accepted a `CancellationToken` parameter but never forwarded it at multiple call sites — and the SAME gap, at the SAME call sites, survived from one review round into the next uncorrected. **Fix**: `agents/fixer.md` gains a "CANCELLATIONTOKEN CHAIN SELF-CHECK" — when a touched method accepts a `CancellationToken`, grep its body for every downstream `Async(...)` call and verify the token is threaded to every one, not just the first/obvious call. |
| KI-E97 | **Ported from a host-mount session (2026-08-29) — `REALINFRA_SIGNAL`'s bare `concurren` catch-all (the safety net that forces `needsRealInfra:true` even when the graph declares `realInfra:false`) matched plain-English uses of "concurrent" with zero relation to a real concurrency defect, forcing an unnecessary Testcontainers demand onto items that never needed one.** Origin evidence — 5 live false-positive incidents: "~10 concurrent 100MB downloads cause OOM" (a volume/OOM concern, not a race), two "do not group **concurrently** with X" file-lock batch-scheduling notes (matched purely because the word appears), "file-lock will serialize against any **concurrent** … work" (scheduling prose), and one item with TWO separate occurrences — "…or that they are **concurrent**)" and, missed by the first-pass fix since `.test()` only needs one survivor, a bare throttle-cap aside "…SemaphoreSlim cap (e.g. max 10 **concurrent**)." with no following noun. **Fix**: narrowed the catch-all with a preceding are/is lookbehind, a following-noun exclusion list (downloads/requests/clients/users/sessions/connections/tabs/calls/work), a with/to/against-preposition exclusion, and a close-paren exclusion (`(?!\))`) for the dangling-adjective throttle-cap shape — WITHOUT losing any of 11 confirmed-genuine concurrency-defect matches, several of which depend ENTIRELY on this regex since their own graph entry declares `realInfra:false`. Also carries forward two earlier, never-before-ported refinements found alongside this fix: hyphen-vs-space normalization (`optimistic[\s-]+concurren`, `unique[\s-]+(constraint|index)`) and an explicit `concurrencyexception` alternative. Verified against a full findings-graph sweep on the origin session (not just the curated incident list) — the rest of the realInfra-matching population all matched via genuine, non-catch-all alternatives. One latent, unconfirmed risk NOT fixed on the origin session (no live incident, and fixing it risks a new false negative): the identical "concurrently (parenthetical)" scheduling-note idiom, phrased with a parenthetical instead of "with X" — noted, not chased without evidence. |
| KI-E98 | **Ported from a host-mount session (2026-08-29) — neither `detectNarrativeVerdictContradiction` (KI-E67) nor `detectUnresolvedCaveatOnClose` (KI-E74C) understood negation** — a phrase preceded by "No"/"Not"/"Zero"/"None"/"Never"/"Nothing" asserts the OPPOSITE of what the bare marker text implies, but neither detector's match logic ever looked at what preceded the match. Origin evidence: an item's own `verify.json` wrote "No findings from the prior round remain unaddressed." — a genuine, correct completion claim on a `CLOSED` result — and `detectUnresolvedCaveatOnClose`'s `findings…remain unaddressed` marker fired anyway, reading a correct claim as an admission of failure. **Fix**: `narrative-check.mjs` gains a shared `isNegated(text, matchIndex)` helper (a 25-char lookbehind window tested against `/\b(no|not|zero|none|never|nothing)\s*$/i`), applied to BOTH detectors' match loops — symmetrically, even though only the `detectUnresolvedCaveatOnClose` direction has a confirmed origin-session live incident, since it is the identical underlying weakness. Care was taken NOT to over-suppress: the real ITEM-H1 admission this detector exists to catch ("Build+targeted-test green here does NOT mean round 2's findings are resolved — they are not, and are unaddressed…", the KI-E74B/KI-E74C citation) still fires — its outer "does NOT mean X" negates a different clause than the inner "findings…remain unaddressed" marker the regex actually matches on, so a naive blanket negation scan could have silently broken the very detector KI-E74C was built to add. |
| KI-E99 | **Ported from a host-mount session (2026-08-29) — `doclint.mjs`'s `findMissingClaims` (F2) flagged a line HONESTLY stating a path does not exist YET — a common, legitimate "deferred/planned/Wave N" prose pattern — as if it were a fabricated-existence claim, the opposite of the false-existence claim the linter exists to catch.** Origin evidence: a doc-drift fix's own added `CONTEXT.md` line — "**No K8s manifest yet**: `k8s/base/services/a-service.yaml` is Wave 5 (deploy gates the full sprint)." — a true statement about the current tree, flagged anyway because the linter only checked "does this path resolve", never whether the surrounding prose asserts existence or absence. Non-blocking (WARN-only), but exactly the kind of noise that degrades a WARN channel's trustworthiness (the same rationale KI-E67/KI-E74C were built on). **Fix**: `findMissingClaims` gains a `NOT_YET_EXISTS_RE` line-level skip (`no…yet`, `not yet`, `does(n't/ not) exist`, `yet to be/exist`, `to be built/created/added/implemented`, `not (yet) built/created/implemented`, `is wave \d`) checked BEFORE claim extraction on each added line — deliberately line-granular (a genuine phantom-path claim on a DIFFERENT line in the same call still catches; only the matching line itself is skipped). |
| KI-E100 | **Ported from a host-mount session (2026-08-29) — `removeWorktree` (worktree.mjs) drops the worktree directory + git's worktree-admin entry but leaves the `factory/<id>` branch ref standing at its stale creation commit** — `addWorktree`'s own fallback path (`git worktree add -b` failing "already exists" → falls back to plain `git worktree add <path> <existing-branch>`) then silently attaches the NEXT worktree for that id to the OLD branch tip instead of cutting a fresh one from current HEAD. Origin evidence, confirmed via direct operational experience: refreshing worktrees that had drifted far behind HEAD required a MANUAL `git branch -D factory/<id>` after `worktree-remove` before a subsequent `group` created a genuinely fresh worktree — silent staleness, not a crash, so it would otherwise go unnoticed until a fix landed on top of months-old code. **Fix**: new `pruneStaleBranch(branch, repoRoot)` in `worktree.mjs`, called immediately after `removeWorktree` at BOTH call sites (the CLI `worktree-remove` subcommand — branch resolved via `listWorktrees()` before removal, since the info is gone once the worktree is gone — and `gc`'s CLOSED-item sweep, which already has `r.branch` from the ledger). Deletion is gated on `git merge-base --is-ancestor <branch> HEAD`: per the factory's own "never commits" invariant, a `factory/<id>` branch has, by construction, zero commits beyond its base, so one that IS an ancestor of HEAD carries no unique value and is safe to discard; one that is NOT (an unexpected commit landed on it, which the hard rule forbids but this function must not blindly trust) is left standing with a reason string, never silently discarded. |
| KI-E104 | **The fold's P9 root-cause check ("a diff that touched ONLY tests greened the test, it did not fix the bug") ran ONLY at fold — after the entire gate band had been spent.** This is the identical economics KI-E83 fixed for its sibling P1, and P9 is strictly cheaper to hoist: no marker, no transcript, no agent judgment, just the worktree's own `git status`. **Fix**: new `verify/build-test.sh rootcause` subcommand (joining the engine-owned `leftovers\|comments\|ledger-anchor` dispatch, before the host-override seam) backed by `_workflow/rootcause-lint.mjs`, which IMPORTS `lib/worktree.mjs`'s `changedFiles` and `lib/verify.mjs`'s predicate rather than re-deriving either — there is no second definition to drift from what the fold applies. A cheap haiku relay probe (`ROOTCAUSE_SCHEMA`, no classify step, mirroring KI-E59's comment probe) reports the markers verbatim; `nonTestCount === 0` FAILS the item pre-band. Gated exactly as the fold gates P9: code items, never `verificationOnly` (KI-L55), only when the item predicted a non-test touch-set. Fail-open on a null/malformed probe, and a probe that could not run records `SKIPPED` on the gates map — silence never reads as clean (KI-E20/KI-E41 posture). **Two real defects found while building it, both fixed here.** (1) `ROOTCAUSE_SCHEMA` initially used a `count` field, which collided with `COMMENT_SCHEMA`'s `count` in `_execsmoke.mjs`'s schema-property-sniffing stub — where zero means GOOD and here it means FAILURE. The exec-smoke caught it by driving every lane to FAILED. Renamed to `nonTestCount`: an identically-named field with inverted polarity is a footgun for any shape-keyed consumer, not just the harness. (2) The shared `isTestPath` predicate (hoisted here out of two byte-identical copies in `touchedRootCause` and `debrisFiles`) carried `/i` on its FILENAME arm, so `[^/]*Tests?\.cs$` matched any source file merely ENDING in those letters — `Contests.cs`, `Manifests.cs`, `Protests.cs` were all classified as tests. That is a live false-positive in two shipped deterministic checks: P9 would read a fix touching only such a file as "tests-only" and FAIL a correct fix, and `debrisFiles` (which never flags a test file) would let a same-named scratch file through. The filename arm is now case-SENSITIVE on `Test` (.NET test types are PascalCase by universal convention); the DIRECTORY arm stays `/i` as the backstop for repos not using PascalCase filenames, and both directions are pinned. Also fixed: three selftest pins on the `build-test.sh` case pattern were pinned to the exact alternation literal and broke on every lint added to it — re-pinned on MEMBERSHIP, because a pin that fails for a purely additive correct change trains readers to edit pins reflexively. One of them (`indexOf('leftovers\|comments)')`) had been silently VACUOUS since KI-E91 extended the pattern: it returned -1, and `-1 < indexOf(...)` is trivially true, so the "lints dispatch before the host override" assertion had been passing for the wrong reason and would no longer have caught a regression. |
| KI-E105 | **KI-L41's retry economics were one-sided: they could only EXTEND the budget, never shorten it.** `isStrictlyNarrower` grants a bonus round when the blocking-finding set shrinks; nothing anywhere detected the opposite. An item whose findings were flat or GROWING round over round still consumed its full `maxItemRetries` allowance — and a band is the most expensive unit of work the factory has (a FULL band is ~5 opus gates + review.code + review.adversarial + refuter + multi-lens re-audit; KI-E88 measures the gate panel alone at ~4x LIGHT). Spending band three on a trajectory that has twice failed to make ANY numerical progress is close to pure waste, and it delays the human review the item evidently needs. The comparison data already existed and was simply unused: `applyConvergenceBonus` persists `row.convergence` on EVERY fold, not only on rounds it grants a bonus for. **Fix**: `isNotConverging` + `applyStallDetection` + `isStalled` in `lib/convergence.mjs` (reading the same field the bonus path writes, so the two directions cannot disagree about what "the prior round" was), a new `maxStallRounds` config knob (default 2, `0` disables), and a single parking decision — `escalateExhausted` now parks on EITHER stop condition, with the transition note naming WHICH one, so scheduling and parking can never diverge. Ordering subtlety handled explicitly: `applyConvergenceBonus` overwrites `row.convergence` as it goes, so the driver snapshots the prior summary BEFORE the bonus pass and hands it to the stall pass. **Deliberately conservative in three ways**, because a false stall parks work that would have closed: it is the strict complement of PROGRESS rather than of `isStrictlyNarrower` (a round that reduces findings while max severity worsens earns no bonus yet is NOT a stall — real progress was made); it requires both rounds to carry comparable gate data, so a pre-gate failure never counts; and it needs N CONSECUTIVE stalled rounds, with any progress resetting the streak to zero. |
| KI-E106 | **`acceptance` is the contract every downstream stage grades the diff against — and nothing BLOCKED on its quality.** The acceptance-scan splits it into clauses, `gate-po` signs off against it, the re-auditor confirms it, KI-E22 lints its named surfaces — yet it is hand-authored and the only check was KI-E22's advisory WARN. That asymmetry is expensive in a specific way: a badly-scoped item does not fail cheaply, it buys ~15-25 agent calls of expensive argument, fails, and re-bands up to `maxItemRetries + maxBonusRounds` times. KI-E18's own row records acceptance-clause coverage gaps as 4/4 of one cycle's band FAILs, and KI-E27 concedes outright that a raw ingested issue "rarely states a checkable acceptance + regressionTest". **Fix**: `lib/readiness.mjs` — a deterministic pre-`group` gate checking three things an author can satisfy unambiguously: the acceptance yields >= 1 clause the KI-E18 splitter would actually probe (reusing that SAME splitter, so the gate and the probe cannot disagree about what "checkable" means), a `regressionTest` description exists and is not a placeholder (`n/a`/`TBD`/`?` are ABSENT, not stated), and `files[]` is non-empty (an empty lock set silently disables the KI-E14 lock, makes the KI-E22 lint vacuous, AND short-circuits the fold's P9 to PASS). `cmdGroup` REFUSES to schedule unready items, printing every problem at once in stable order, with an explicit `--force-unready` override. **Blocking, unlike the KI-E29/KI-E90 scheduling warns, and the distinction is deliberate**: those flag risks that may not bite and that a different batch composition could avoid, whereas an unready item is defective in ITSELF and no scheduling luck rescues it. **Scope bound, deliberately narrow**: it judges only what can be decided MECHANICALLY about the item's own text — never whether the acceptance is *correct* or *well-scoped*, which is an authoring judgment. A gate that guessed at that would reject real work and train operators to reach for the override reflexively, which is how a blocking gate degrades into noise. `blocked`-tier items are skipped (they await an owner ruling and are not scheduled anyway). |
| KI-E107 | **The report could say which models were called and how items ended, but not the one number that governs cost: what share of the work went to items that never closed.** `agg.models` and `agg.outcomes` were both derived from the SAME `item_folded` event and never crossed. Given six prior entries about this factory's own numbers being wrong or absent (KI-E40 rendered a 100%-passing gate as 0%; KI-E46 read a 45% recovery rate as 14%; KI-E48 had two structurally dead sections; KI-E44 lost usage on a kill; KI-E66 found cache telemetry never existed; KI-E33 found session telemetry silently off), the absence of a yield figure meant no cost argument could be settled with evidence. **Fix**: `agg.callsByOutcome` + `renderCallsByOutcome` (pure, selftest-pinned arithmetic) render a per-outcome table with items, calls, share AND calls-per-item — that last column is what exposes a FAILED item costing more than a CLOSED one — plus an explicit yield line and a FAILED-spend line. Call counts are the unit because KI-E66 established per-item TOKENS are structurally unrecoverable inside the Workflow runtime; this is a call-weighted proxy, labelled directional, never a bill. **"Wasted" deliberately means FAILED only**: an ESCALATED item reached a human with its analysis intact and a BLOCKED one surfaced a real scope question — both are the system working, and the footnote says so rather than letting a reader infer a harsher number. Report-side only; no emit path touched (KI-E7 — telemetry is observational, never evidentiary). |
| KI-E108 | **Nothing distinguished a genuinely-failing test from an intermittently-failing one**, and the cost of that confusion is a whole band: a flaky failure re-bands the item, while a flaky PASS is worse — KI-E65 records an item folded CLOSED while carrying a newly-introduced flaky test, caught only because the operator cross-checked the note against the ledger by hand. **Fix**: `flakeSuspects` in `lib/verify.mjs` uses the one flake signal that is deterministic and free — the SAME test class appearing with BOTH a passing and a failing filter result in one transcript, re-paired START->RESULT by the identical 1:1 emission contract KI-E70 already relies on. Wired into the fold as a named WARN over both the verify and integrate transcripts. **Deliberately ADVISORY, and this bound is the design**: a fail-then-pass sequence is ALSO the legitimate cycle-8 retry shape (fix, re-run, green) that `lastMatch` intentionally resolves as last-wins, so promoting this to a blocking signal would fail the very shape the engine explicitly supports. It only NAMES the class, letting a human tell "retried after a fix" from "this test is unstable" — pinned in both directions, including an assertion that a retry-shaped transcript still PASSES `verdictFromParse` while still being named. |
| KI-E109 | **Main-tree contamination had SEVEN detection passes (KI-L65/E41/E45/E50/E35/E82/E89) and ZERO prevention** — because KI-E50 explicitly parked the choice: "prevention (sandboxing main read-only vs failing the lane fast) remains a host-owner policy decision the engine deliberately does not make." That left the decision permanently unmade rather than available. **Fix**: `failLaneOnMainDrift`, a third entry in the existing `lib/policy.mjs` seam (the same KI-E57/KI-E58 mechanism, not a bespoke flag), OFF in the shipped engine. When ON, the `⚠ MAIN-DRIFT` marker the KI-E50 verifyHint ALREADY has the runner paste verbatim into its own note fails the lane immediately — before the gate band, and long before the fold's KI-L65 check — instead of only warning. Adds no new detection: it only decides what the factory DOES with a signal it already collects. Default OFF is byte-for-byte the historical warn-only behaviour, so no host's posture changes without an explicit opt-in. |
| KI-E134 | **Ported from a host-mount session (2026-09-04) — three fixes shipped together in direct response to that session's own KI-E133 incident review: a real PREVENTION mechanism for main-tree contamination (not just another detector), a real fix for plan-drift (not just another downstream scan), and a real correctness bug found while investigating both.** **(i) `build-test.sh` worktree-boundary guard.** Every contamination incident that session traced to the same root cause: `build`/`red`/`filter`/`suite` resolve their `$target` against whatever the CALLING shell's cwd happens to be, and because every factory worktree AND the main tree are full checkouts of the SAME repo, a wrong-cwd invocation frequently does NOT error (no MSB1009) — it silently SUCCEEDS against the wrong tree's copy of that path, giving the caller no signal anything is wrong. A new `_guard_worktree_path` helper refuses (exit 65, loud stderr `FACTORY::WORKTREE-GUARD::REFUSED`) unless the resolved target lives under `state/worktrees/<id>/`. Deliberately scoped to ONLY the `dotnet`-invoking subcommands (`build`/`red`/`filter`/`suite`) — the read-only git-diff lints do not share the silent-wrong-tree failure mode and a synthetic/placeholder path is a legitimate way to exercise their wiring, confirmed live when the first unscoped draft of this guard broke exactly such a fixture in `opencode/_selftest.mjs`'s own mechanized "mech leftover" simulation. Ported here for the `build`/`filter`/`suite` cases and the `red` case, matching this repo's `verify/build-test.sh` shape as of the port (this repo does not yet carry the KI-E129/KI-E132(ii) self-tee `_redout` mechanism the origin session's `red` case also touched, so only the guard call — not the redout positional-arg fix — applies here). **(ii) `pathToken`/`phantomManifest` object-shape bug.** NOT ported: the origin fix patches an object-shape bug in `pathToken`/`phantomManifest` (KI-E118), and neither exists yet in this repo's `lib/verify.mjs` — recorded here only so a future KI-E118 port carries this fix from the start rather than reintroducing the same bug. **(iii) Plan-drift prevention at the source, ported to BOTH runtimes.** Two items in that session's own batch died in `KI-E87` PROSE mode with "no evidence in the diff after one bounded amend" — PROSE mode's `hasPlanCommitmentLanguage` prefilter is a coarse keyword heuristic, weaker than STEP mode's per-step evidence check, and `steps` stayed intentionally optional in `PLAN_SCHEMA` (KI-E101), so a planner could describe substantive commitment-language work in prose and never decompose it, silently routing into the weaker check. Fix: when a FRESH plan's own approach/blastRadius carries commitment language (`MUST`/`required to`) but `steps` came back under a 2-entry floor, ONE cheap bounded follow-up call (`PLAN_STEPS_NUDGE_SCHEMA`) asks specifically for the missing decomposition — the same "one bounded amend" shape used everywhere else in this pipeline, never a second full plan, and skipped entirely for the common case at zero extra cost. Ported to `_workflow/opencode/runtime.mjs` as its own phase (`plan-steps-nudge`) per this repo's own Fix #20 (`opencode/_selftest.mjs` mechanically deep-equals every `factory.js` `*_SCHEMA` against a same-named `schemas.mjs` export), so shipping the schema without the runtime wiring would have been a mechanically-caught partial state. **Verification (origin session):** real behavioral tests via the origin repo's `exec-smoke` harness, not source-text pins; origin selftest 1626 → 1654 passed, 0 failed. This repo's own selftest is re-verified green after the port (see below). |
| KI-E133 | **Ported from a host-mount session (2026-09-04) — SEVERITY: CRITICAL, a subagent violated KI-E1 ("NO mutating git, ever") and ran a destructive `git reset`/`clean` against the MAIN repository, not a worktree, mid-Workflow.** During a 13-item batch on the origin host, `git reflog` on the HOST repo showed a `git reset --hard` with NO corresponding `driver.mjs` code path (audited: every git invocation in `_workflow/*.mjs` is either read-only or `-C <worktree>`-scoped), combined with an unscoped `git clean -fd`. **Blast radius:** every uncommitted, tracked, main-tree change standing at that moment was wiped back to HEAD — that session's own in-flight tooling fixes (silently reverted mid-batch, so the latter half of the same batch ran against the re-broken tooling, reproducing a KI-E132(ii)-class litter-file bug live a second time), the ledger (silently un-recording prior recoveries), and every regenerated report. **Attribution: circumstantial, not proven** — one runner agent had file activity in the reset's exact window and a repeat wrong-cwd fingerprint, but no agent's own self-reported artifacts across all 13 items mention running `git reset`/`clean`; the full transcript set was not read in full to avoid a context-overflow, so this is not a closed investigation. **Recovery (origin session, verified complete):** `state/items/` is `.gitignore`d and untouched by `reset`/`clean` (respects the ignore by default) — every per-item checkpoint, recovery-fold JSON, and gate verdict survived intact; the tooling fixes were re-applied, `driver.mjs reconstruct` + `fold` rebuilt the ledger identically to its pre-incident state, and the origin selftest reconfirmed green after the redo. **No prevention fix shipped in the incident itself** — the existing contamination-DETECTION net (KI-E61 auto-repair, KI-E89 unclaimed-drift, KI-E41/KI-E45 main-guards, and this repo's own KI-E109 `failLaneOnMainDrift`) is what made it recoverable, but all of it is detect-after-the-fact; nothing stopped a subagent from running the command in the first place. KI-E134(i) (above) is the prevention fix this incident produced. **Process mitigation adopted (not a code fix):** commit controller-authored factory-tooling changes immediately after verifying them (selftest green), BEFORE launching a Workflow batch — `git reset --hard`/`clean -fd` cannot touch what is already committed. A durable technical fix (sandboxing/restricting what a subagent's Bash tool can execute against the HOST repo root) is a real gap this incident exposes but is outside what a prompt-level or driver.mjs-level change can close — flagged for the operator, not resolved. |
| KI-E132 | **Ported from a host-mount session (2026-09-04) — two RED-evidence-integrity bugs found investigating a batch fold: a data-staleness bug that had ALREADY wrongly FAILED several fully-approved closes, and an off-by-one in the KI-E129 self-tee fix.** **(i) `fold`'s P1 `verificationOnly` check trusted the EMBEDDED `r.verificationOnly` flag** (a copy captured whenever the checkpoint/result object was produced) while re-deriving the RED evidence itself fresh from disk (`verify-red-raw.txt`) — a freshness mismatch. When an item is re-claimed for a later, redundant re-verification pass, that pass's fresh `test.json`/`verify-red-raw.txt` overwrite the very files P1 reads, but nothing updates the embedded flag, which came from an earlier (often `reconstruct`-recovered) checkpoint. Fold then reads: embedded flag = stale `false`, disk evidence = a genuine verificationOnly `exit=0` re-confirmation — and misreads the second as a vacuous test on the first. Live on the origin host: three items with a COMPLETE, fully-gate-APPROVED (or fully re-verified) closure each folded to FAILED by this bug alone. **Fix:** P1 now re-reads `test.json` fresh (`readIf`, same freshness tier as `verify-red-raw.txt`) and recomputes `verificationOnly` with the exact formula `factory.js` uses to set it (`test.verificationOnly===true && !test.red`); on disagreement with the embedded flag, the on-disk value wins (it is the live attestation) and a diagnostic line names the mismatch. The corrected value is written back onto `r.verificationOnly` so later checks that also read it (P9 root-cause-touch, the `filesChanged` guard) inherit the fix automatically. **This repo's own `fold` already carried this exact re-derivation pattern independently** (its `KI-L55`/`KI-L66` handling, immediately below the ported code) — the port slots in alongside it without conflict, since the two check different freshness windows (embedded-vs-disk here; contradicted-flag-vs-genuine-red there). **(ii) `build-test.sh red`'s KI-E129 self-tee off-by-one:** on the origin host, `_redout="${3:-}"` read the SAME positional slot already assigned to `filter` two lines above, so the "optional 3rd arg" was never actually optional — it silently equalled the dotnet filter STRING on every `red` invocation, and since a filter never contains `/`, the transcript got `cp`'d to a file literally named after the test filter in whatever directory happened to be current: harmless litter inside a worktree, real main-tree contamination when cwd was the repo root. **NOT ported**: this repo's `verify/build-test.sh` does not yet carry the KI-E129 self-tee `_redout` mechanism at all (see KI-E134(i) above), so there is no target for this specific off-by-one here — recorded so a future KI-E129 port carries the fix from the start. |
| KI-E113 | **`.claude/` was never gitignored in the engine repo, so a routine `git add -A` would have committed per-host session settings, a full checkout of the repo inside itself, and a generated copy of tracked content.** Found 2026-09-02 while preparing a commit: `git status` showed `?? .claude/` — 124 files, 1.3 MB, untracked and NOT ignored. This is the KI-E26 class ("host data must never enter this engine repo"), and the original sweep enumerated every sibling (`state/`, `reports/`, `queue/`, `config/factory.config.local.json`, `verify/build-test.local.sh`, `agents/repo-profiles/*`, `telemetry/`) but missed this one. **Why it was missed is the interesting part, and is recorded so the distinction is not re-lost:** `.claude/` is the ONE path that IS legitimately tracked in a *host* repo — its `.claude/rules/*.md` are the acceptance rules the factory enforces, and `worktree.mjs`'s KI-L67 sparse-checkout exists specifically to strip that tracked copy out of each worktree so the ~55k-token rules baseline is not paid twice. So the path does not read as runtime state at a glance. In the ENGINE repo it is never engine content, for three independent reasons: (1) `.claude/settings.local.json` is per-host session config — KI-E33 and KI-E52 both write the OTLP env block, including endpoints, into precisely that file; (2) `.claude/worktrees/<branch>/` are REGISTERED git worktrees (confirmed via `git worktree list`: one sat on `feature/recovery-auto-gen-mainsweep-redprobe` with its own `.git`), so committing one is actively BROKEN rather than merely noisy, and the Claude Code worktree feature recreates them — ignoring the current contents once would not have held; (3) `setup/init.mjs` INSTALLS the controller skill into `.claude/skills/`, so running `init` against this repo (dogfooding) generates a copy of the tracked `claude-assets/` source, and committing the copy would let it silently drift from the original it was generated from. **Fix**: `.claude/` added to `.gitignore` with the engine-vs-host distinction spelled out at the ignore line itself (that distinction is the whole reason for the miss, so it belongs where the next reader will be). Purely preventive — nothing under `.claude/` was tracked, so no `git rm --cached` was needed. Selftest pins BOTH directions, by intent rather than by current directory contents: `.claude/` is ignored, AND `claude-assets/` — the shipped source `init.mjs` installs FROM — is NOT, so a future over-broad pattern that swallowed the engine's own assets fails loudly. |
| KI-E112 | **Every remaining disclosed-but-unfixed gap, closed — the `UNPORTED` set in `opencode/stage-parity.mjs` is now EMPTY.** KI-E103 built the runtime-parity gate and honestly recorded four stages the port did not implement; KI-E104 added a fifth. A disclosed gap is better than a hidden one, but it is still a gap: on every one of them an item could pass in the port that canon would have failed. **Stages ported.** (1) **RED-proof (KI-E83)** — `afterVerify` now re-reads `verify-red-raw.txt` from DISK via `parseRedRaw(decodeTranscript(...))`, applying canon's exact contract including KI-L55's INVERTED `verificationOnly` polarity (a pinning test must PASS, exit 0). The port previously trusted the test-author's self-reported `test.red` until fold time — the precise divergence that let ITEM-H24 and ITEM-H26 each burn a FULL band with 9 and 8 gates APPROVED. `parseRedRaw` had been imported and unused since the port was written. (2) **Root-cause touch (KI-E104)** — `nonTestChanged(changedFiles(worktree))` through the SAME `lib/verify.mjs` derivation the fold's P9 uses, gated identically, recording `mech:rootcause-touch` and announcing `SKIPPED` on an unreadable worktree. (3) **Debris (KI-D1)** — `debrisFiles` was not even imported; a teed factory artifact or scratch file reached the gate band. (4) **Plan-commitment / plan-step (KI-E87 + KI-E101)** — the full trio (probe, ONE bounded amend, one final re-probe) mirroring the acceptance trio, with both STEP and PROSE modes, sharing canon's existing `probe:plan-commitment-scan` gate key and stamping the mode into the headline. This required retaining the planner's output on `progress` (the port's analogue of KI-E87 hoisting `plan` to function scope in canon) — without it the stage would have dispatched and silently found nothing to check, which is why the selftest pins the retention AND pins that the acceptance phase actually advances INTO the new stage: a stage nothing routes to is dead code that looks implemented. (5) **Ledger-anchor (KI-E91)** — mechanical STEP-1 via the engine-owned `build-test.sh ledger-anchor`, gated exactly as canon gates it (only when the item declares a `*STANDARDS-DIVERGENCE-LEDGER.md` path, so a free no-op for hosts without one), then a `ledger_anchor_classify` agent step mirroring `leftover_classify`. **Non-stage gaps closed in the same pass**, each individually small and each a real divergence: the KI-E74B `verifyNote` channel is now POPULATED rather than being a dead render (canon fills it from the runner AGENT's self-caveat; this port has no runner agent, so the faithful analogue is the caveats its own DETERMINISTIC pass produced — a check that could not run, a KI-E108 flake-suspect class — which unlike agent prose cannot be self-serving); `integ.branch` now reaches `res.branch`, so the folded row carries the branch the human is meant to review and commit, the one hand-off the factory exists to produce (KI-E1); the host's configured `gateSet` is finally READ (both `gateRolesFor` call sites passed `null`, silently ignoring a host that customised `config/factory.config.json` — harmless only while that config happens to equal the hardcoded default, which is exactly what made it a latent trap rather than a visible bug); and the dead `touchedRootCause`/`appendRaw` imports are removed. **Three false statements in this registry and its docs, corrected rather than left standing.** KI-E91's port-status note claimed "this repo has no `schema-parity` selftest gate" — false; that gate exists in `opencode/_selftest.mjs` and is run by the main suite. The accurate claim was that no RUNTIME-parity gate existed, which KI-E103 then built. KI-E60 claimed the port scopes repo profiles "to non-probe roles only exactly like its own brief-reading block" — factually wrong; the profile block sits OUTSIDE `compose.mjs`'s `!/-probe$/` branch, and both the port's own comment and its selftest assert the opposite. `CLAUDE.md` still said "currently 972 asserts". Also refreshed: `factory.js`'s own `meta.description` (it omitted comment-scan, ledger-anchor and root-cause — the file was describing a pipeline it no longer ran) and `claude-assets/skills/ai-factory/SKILL.md`'s stage list (it omitted six stages, so the Claude-Code controller's own manual understated what a run does). Selftest: 1347 → 1368; opencode 187/0. **Remaining gaps are structural only, and all three are inherent rather than unimplemented**: per-call model tiering (KI-O1 — `Task` accepts no model parameter), Copilot's lack of independent subagent dispatch (KI-O4), and `infraSuspect`/KI-L53 (the submit protocol has no null-agent state to express — every key requires a real submit). |
| KI-E111 | **The three residual failures KI-E102 disclosed as "host-capability gaps, not engine defects" were re-examined on the owner's instruction to fix them — and TWO of the three turned out to be genuine engine defects that the Windows environment merely made visible.** Assuming they were environmental was the wrong call, and this row records the correction. **(1) `shimAvailable` could never return true on Windows — a byte-for-byte recurrence of the KI-E72 false-negative it was WRITTEN to fix, one platform over.** It tested `statSync(f).mode & 0o111`, but NTFS has no execute attribute and Node synthesises `mode` from the read-only flag alone, so that expression is 0 for EVERY file — verified against this repo's own shipped `verify/build-test.sh`, which reports mode `0o666`. Consequence: `dotnetAvailable()` and `preflight()` reported "dotnet: ABSENT — build/test verify cannot run on this host" on ANY Windows host relying on the KI-E17 shim, which is exactly the live 2026-08-07 symptom KI-E72 exists to prevent. It matters concretely because the shim is never executed via its mode bit on Windows anyway: `_workflow/opencode/buildtest.mjs` spawns `build-test.sh` through Git Bash, which runs a `.sh` regardless of NTFS attributes. **Fix**: on win32 the honest test is existence-of-a-FILE (an `isFile()` guard keeps the relaxation from degrading into "always true", and a directory is still rejected); the POSIX path keeps the mode check byte-for-byte unchanged. The selftest now asserts the REAL contract per platform rather than skipping. **(2) `writeJsonAtomic`'s `renameSync` had no Windows retry — and it is the engine's ONLY durable-write primitive.** `rename` over an existing target is atomic-and-always-succeeds on POSIX, but on Windows it is a `MoveFileExW` that fails with a transient sharing violation (EPERM/EACCES/EBUSY) whenever anything momentarily holds the destination open — antivirus, the search indexer, a concurrent reader. KI-E102 recorded the live symptom (an intermittent `EPERM … opencode-progress.json.tmp -> opencode-progress.json` that passed on re-run) and speculated it "may be a real portability defect"; it is. Because every ledger update, report, run-args write and opencode checkpoint funnels through this one function, a dropped write strands real state — the observed case loses an item's in-flight progress. **Fix**: bounded retry (5 attempts, 10/20/30/40ms backoff, ~100ms worst case) gated on win32 AND on those three codes only, so a genuine error still throws immediately and POSIX takes the pre-existing single-attempt path byte-for-byte. The backoff is a real synchronous sleep via zero-dep `Atomics.wait`, never a CPU-burning busy-wait, and is wrapped so a runtime that disallows it degrades to an immediate retry rather than throwing. A FAILED write now also removes its temp file — a stray `<path>.tmp.<pid>` is precisely the non-canonical debris KI-E42's relaunch scanner flags, so a failed write would otherwise have poisoned the next resume. **(3) The `bash -n` check was the only genuine host-capability gap — but it was reporting it dishonestly.** All three `bash -n` calls sat inside one try/catch whose failure message was "install.sh / release.sh / _e2e.sh all parse", so a host with no usable bash produced a scary FALSE failure implying the shipped release tooling has a syntax error. An errno check would not have separated the two either: on Windows `bash` typically resolves to the WSL launcher stub, which does not throw ENOENT but runs and exits non-zero. **Fix**: probe `bash -c "exit 0"` separately first; a usable bash runs the real syntax gate (now surfacing the actual stderr on failure instead of a bare boolean), an unusable one prints an announced SKIP naming the assert count — the same posture as this file's git-unavailable fixtures and KI-E102's own filename skip. CI runs `ubuntu-latest`, so the shipped scripts remain syntax-gated on every push. **Result: `node _workflow/lib/_selftest.mjs` now runs 1347 passed / 0 failed on Windows — green on that platform for the first time**, alongside the opencode suite's 187/0. |
| KI-E110 | **KI-E88's band-mix line told the operator the SHAPE of a batch but never its SIZE**, leaving its own "roughly a 4x gate-panel difference" as prose to be re-derived mentally each time. **Fix**: `lib/band-cost.mjs` projects a batch in AGENT CALLS — the exact unit `item_folded`'s cost map records and KI-E107's new yield table aggregates — so a projection can be compared against last cycle's real calls/item and calibrated, instead of quietly becoming folklore. A projection in a different unit from the measurement is uncheckable by construction; a currency or token figure would be strictly worse, since KI-E66 established per-item tokens are not recoverable and any such number would be fabricated. The per-stage counts are DERIVED from what `runItem()` actually dispatches (base stages + the band's role-gate panel + its review/refute/lens set + the planner only for non-mechanical items), and the rendered line states explicitly that it is a FLOOR — retries, amends, adjudication and re-gates all add calls. Under-promising is the safe direction for a planning aid: an operator surprised by a cheaper batch changes nothing, one surprised by an expensive batch has already spent it. `suggest` renders it against the ready pool using the SAME `bandFor` the scheduler uses, so projection and scheduling cannot disagree about a band. |
| KI-E103 | **The opencode/Copilot binding (KI-O1/KI-O4) had silently FORKED from `factory.js`: one drifted routing constant that made a class of items structurally unclosable in the port but closable in canon, five result-shape divergences that fed downstream consumers `undefined`, and no mechanism anywhere capable of detecting any of it.** Found 2026-09-02 by a directed parity audit (owner ask: "make sure copilot and opencode are on par"). **Root cause of the whole class: there was no RUNTIME-parity gate.** The only mechanical cross-check was SCHEMA parity, and its own documented workaround — export the schema, omit it from the `SCHEMAS` registry, add a `NOT yet ported` comment — is exactly how KI-E83/KI-E87/KI-E91 shipped unported while green. Anything with no schema surface (a guard, a phase re-order, a prompt hint, a routing constant) was structurally invisible. **The proof case is KI-E97**: it narrowed `REALINFRA_SIGNAL`'s bare `concurren` catch-all after five live false-positive incidents and added hyphen-normalised separators + `concurrencyexception` — in `factory.js` only. KI-E97's own row lists no opencode mirror (unlike KI-E92's, which explicitly lists all four sites), so the drift was unnoticed, not accepted. Re-verified live before fixing: the stale copy false-fired on all four documented incident shapes ("~10 concurrent 100MB downloads", "do not group concurrently with X", "serialize against any concurrent work", "max 10 concurrent)") — each then DEMANDS a Testcontainers marker the item's fix cannot produce, i.e. **unclosable in the port, closable in canon** — and false-negatived the hyphenated `unique-constraint` shape, losing its real-infra floor. **Fixes, six behavioural + one mechanism.** (1) `REALINFRA_SIGNAL` re-ported byte-identically. (2) `runtime.mjs`'s plan phase no longer overwrites `res.rootCauseFiles` with the planner's self-reported `plan.files` — it contradicted the port's OWN cmdInit comment and made the fold's P9 grade the diff against the planner's opinion of scope instead of the work item's, defeating the self-certification check P9 exists to be. (3) `gateDetails['probe:acceptance-scan']` now uses canon's `{verdict, headline, findings}` instead of the probe's raw `{covered, gaps}`: `lib/feedback.mjs` reads `d.verdict` and iterates `d.findings`, so the port had been rendering `## probe:acceptance-scan - undefined` and **silently dropping every un-evidenced clause** from feedback.md — the KI-L31 authoritative channel a reFix reads FIRST. The gate is also now recorded on the covered:true first pass (canon writes it unconditionally; the port only wrote it on the reprobe path, so a passing scan left no evidence it ran). (4) `res.gates['reaudit']` (the `code=ok edge-case=no` roll-up) is now recorded — its absence broke two consumers: `lib/recover.mjs missingStageFrom()` parses that exact string to derive which lens set a recovery must re-run, and KI-E40's telemetry classifier counts the all-`key=ok` family as ok, so a missing key read as 0%. (5) `gateDetails['adjudicator']` is recorded, and a P8 re-gate now writes `<key>:re-gate` instead of OVERWRITING the original dissent's detail — `lib/recover.mjs` filters `:re-gate` rows precisely because it expects both to exist. (6) `REFUTE_OK` is pushed when the refuter passes, not only after all lenses converge, so a refuter-passed/lens-failed item no longer ends its transitions at `GATED` — the shape `missingStageFrom()` keys on to auto-scaffold a re-auditor-only recovery. (7) **The mechanism, and the actual point of this entry**: new pure `lib/port-parity.mjs` (`extractFactoryRoles`/`extractPortRoles`/`namedConstantSource`/`parityGaps`) + a machine-checked manifest `_workflow/opencode/stage-parity.mjs` declaring every canonical stage as DISPATCHED, MECHANICAL (deterministic Node instead of an agent — `runner`, `marker-probe`, `comment-probe`, each of which is arguably STRONGER than the agent it replaces, having no self-report that can diverge from disk) or UNPORTED-with-a-reason (`red-proof-probe`, `plan-commitment-probe`, `ledger-anchor-probe`, and since KI-E104 `rootcause-probe`). The selftest fails on an undeclared stage, a stale declaration (declared-but-actually-dispatched), a dead one (declared-but-gone-from-canon), a contradictory one, a sub-40-char placeholder reason, and on any byte-drift in the shared constants — proven non-vacuous by asserting the extractor finds >=15 real roles and by an injected-stage simulation. Following KI-E59's own lesson (the owner's words: "MECHANISMS that prevent … not just more prompt rules"), this is a forcing gate, not a reminder — and it proved itself immediately: the very next stage added to canon (KI-E104's `rootcause-probe`) tripped it on the first run. `_workflow/opencode/README.md` also corrected: it had called `routing.mjs`/`compose.mjs` "**Verbatim** port" (both genuinely drifted) and claimed "the SAME contract (schemas, fold rules, artifact shapes)" while artifact shapes and gate keys differ (`reaudit-<lens>.md` vs `reaudit.md`, `mech:comment-scan` vs `probe:comment-scan`) — it now points at the machine-checked manifest instead of carrying a prose list nothing verified. **Left as disclosed, NOT fixed**: per-call model tiering (KI-O1 — `Task` has no model parameter) and Copilot's lack of independent subagent dispatch (KI-O4) are structural; `infraSuspect`/KI-L53 is structurally inexpressible here (the submit protocol has no null-agent state — every key requires a real submit); budget pre-stop (KI-C2) has no Workflow `budget` global to read. Selftest: 1181 → 1217. |
| KI-E101 | **The plan-vs-diff axis (KI-E87) could only fire when the planner happened to phrase its intent with "MUST"/"required to" — a plan describing the same work as ordinary narrative prose was structurally invisible to it, so on those items the axis silently did not run at all.** `hasPlanCommitmentLanguage` is a COARSE prose gate by design (its own header says so): it decides only "does this text contain SOME checkable promise", and the haiku probe then has to FIND the commitments in free text before it can judge them. That works — it caught the origin session's brownfield-note drift, and the KI-E87 review already had to widen a closed 14-verb allowlist after finding 13 of 14 realistic commitment sentences silently missed — but every widening is chasing the same root shape: the check's reach depends on the planner's word choice, not on the work. **Fix — structured STEP mode, additive, execution UNCHANGED:** `PLAN_SCHEMA` gains an OPTIONAL `steps` (string array; `agents/planner.md` asks for 2-8 ordered, individually checkable one-sentence steps, and warns the planner the list is machine-checked so an aspirational or deferred step is a self-inflicted failure — deferred work is routed to `ruleRisks`). New pure `normalizePlanSteps(steps, cap)` (`lib/plan-commitment.mjs`, inlined byte-identically into `factory.js` per the KI-E2 split, parity selftest-pinned) trims/collapses, strips a hand-written leading ordinal or bullet so the probe's numbering does not double up, dedupes case-insensitively, and merges the over-cap tail into the last entry exactly as `splitAcceptanceClauses` does — nothing authored is ever silently sliced away, since an unchecked step is the precise failure class this entry closes. Its length floor is **12, deliberately BELOW `splitAcceptanceClauses`' 20**: that splitter's floor discards punctuation noise from mechanically splitting a sentence, whereas an entry here is an explicitly authored unit of work, and 20 would silently drop genuine short steps ("Wire the DI seam" is 16, "Add the null guard" is 18). The 4c-bis block now runs **two modes through ONE probe/amend/re-probe/gate tail**: STEP when `normalizePlanSteps(plan.steps).length >= 2` (probes coverage step-by-step, the proven KI-E18 AcceptanceScan shape on the plan axis, with no phrasing dependency), PROSE otherwise (the unchanged KI-E87 prefilter + its verbatim original prompt). A single `axis` noun renders both modes' prompts/headline/findings/failure note; the mode is stamped into the headline (`[STEP mode]`/`[PROSE mode]`) so `gateDetails`/`feedback.md` is self-describing; both modes share the EXISTING `probe:plan-commitment-scan` gate key, because a second key would silently orphan the telemetry/recover/feedback consumers already keyed off it. **Backward compatibility is the load-bearing property**: `steps` is NOT in `required`, so a planner that omits it, a mechanical item (no planner at all), and a KI-E69 reused prior-attempt plan authored before this field existed all fall through to byte-identical pre-change behaviour — the silent-no-op posture `buildDocMap`/`solutionFor`/`precedent` already use. **SCOPE BOUND, recorded because the originating request was the opposite:** steps decompose CHECKING, never EXECUTION. The owner's initial proposal was a cheap-model worker implementing the plan in parts; that was rejected on this repo's own evidence and the rejection is why the bound exists — `agents/fixer.md`'s SIBLING-PATTERN SWEEP + DEAD-CODE SELF-CHECK (KI-E94), ADJACENT-CLAIM RE-CHECK (KI-E95) and CANCELLATIONTOKEN CHAIN SELF-CHECK (KI-E96) are all whole-item, cross-file consistency checks that a worker holding only its own slice structurally cannot perform, and they guard "fix-introduced defects" (KI-E51), the #1 rejection class; separately, the fixer is ~1 of 15-25 agent calls per item while a FAILED item re-runs the WHOLE band up to `maxItemRetries + maxBonusRounds` times, so raising the fail rate to cheapen one call is net-negative arithmetic (KI-E92 already re-routed that same route in the cautious direction after a close-rate regression), and haiku specifically is pinned OUT of the heavy roles for measured context-ceiling deaths (KI-L64/KI-L58/KI-L56; `worktree.mjs`'s KI-L67 header records the ~120k first-turn baseline against a 200k window). The one-fixer-per-item bound is therefore selftest-pinned BEHAVIOURALLY (an exec-smoke assertion counting fixer calls), not just by comment. Selftest: 1121 → 1181 on this Windows host (+60), including a second `execSmoke` run that executes the STEP-mode FAILURE path end to end — an un-evidenced step must fail the item pre-band after exactly ONE bounded amend and ONE re-probe, with the step text in the note, the gate band provably never dispatched, and sibling lanes unaffected. |
| KI-E102 | **`_workflow/lib/_selftest.mjs` crashed UNCAUGHT on Windows at ~line 987 of 2953, so roughly two thirds of the suite never ran on that platform and `node _workflow/lib/_selftest.mjs` — the command `CLAUDE.md` mandates be green after EVERY change — could not pass there at all.** The KI-E89 quote-path fixture wrote three filenames unconditionally: `café.cs` (legal everywhere), `weird"quote.cs` and `back\slash.cs`. `"` and `\` are RESERVED characters in a Win32 filename, so `writeFileSync` threw `ENOENT` at the second one and aborted the whole run — no assertion failed, nothing was reported, the process simply died mid-file. Found 2026-09-02 while running the mandated selftest before a KI-E101 change on a Windows host; the working tree was clean at HEAD, confirming it as pre-existing rather than change-induced. Notably this is the same platform KI-E59 already calls out as "the very platform the [comment] gate was born on", and the same class as KI-E59's own finding (d) — an `execFileSync('cat')` that was a silent no-op on Windows — so this is the second time a POSIX assumption in engine tooling has degraded silently on Windows specifically. **Fix**: the UTF-8 case stays unconditional (it exercises the octal-escape decode this fixture exists for and is legal on every platform); the two POSIX-only filenames are platform-gated behind `process.platform !== 'win32'` and ANNOUNCE their skip with an explicit count (`SKIP KI-E89 quote/backslash filename decode (2 asserts …)`), so silence never reads as "passed" — the same announce-never-skip-silently posture as KI-E41/E42 and the same `try/catch` + printed-SKIP convention the git-unavailable fixtures in this file already use. POSIX coverage is byte-for-byte unchanged and CI runs `ubuntu-latest`, so the decode assertions still gate every push. **Disclosed, NOT fixed here** (they were unreachable behind the crash and were BELIEVED to be host-capability gaps, not engine defects): with the suite now running to completion on Windows, 2 further failures surface there — `KI-E72 shimAvailable` (asserts a POSIX executable bit, which NTFS does not have) and `KI-E52 bash -n` (no `bash` on the host). A third, `PR#9: the opencode suite runs green`, was observed FAILING once and passing on subsequent runs, with `EPERM: operation not permitted, rename … opencode-progress.json.tmp -> opencode-progress.json` on the failing run — an intermittent Windows atomic-rename contention in the port's checkpoint writer that may be a real portability defect and is recorded here for whoever reproduces it. **CORRECTION (KI-E111, same day): that "host-capability gaps, not engine defects" judgment was WRONG for two of the three.** `shimAvailable` was a genuine engine defect — a recurrence of the very KI-E72 false-negative it was written to fix — and the EPERM rename was a genuine portability defect in `writeJsonAtomic`, the engine's only durable-write primitive. Only the `bash -n` case was a true host gap, and even that was being reported dishonestly. See KI-E111; all three are now fixed and the suite is green on Windows.** | further failures surface there — `KI-E72 shimAvailable` (asserts a POSIX executable bit, which NTFS does not have) and `KI-E52 bash -n` (no `bash` on the host). A third, `PR#9: the opencode suite runs green`, was observed FAILING once and passing on subsequent runs, with `EPERM: operation not permitted, rename … opencode-progress.json.tmp -> opencode-progress.json` on the failing run — an intermittent Windows atomic-rename contention in the port's checkpoint writer that may be a real portability defect and is recorded here for whoever reproduces it. |

| KI-E135 | **Ported from a host-mount session — documentation only, no code.** `standards-evolution.md § 3.1`'s rule-file-update requirement is structurally unsatisfiable from inside a work-item worktree whenever the cited standard lives under a target repo's own rules directory: every factory worktree sparse-checks out that path (KI-L67, "so the rules baseline is not paid twice"), so a fixer cycle scoped to the item's own branch can never stage the required companion rule-file edit — the Architect gate would fail it again for the identical reason every time. Not itself a code fix (the sparse-checkout exclusion is correct and intentional), but a process gap: nothing currently tells the controller/operator "this CHANGES_REQUIRED needs a MAIN-TREE edit, not a worktree recovery." Interim resolution on the origin host: the controller applied the rule-file edit directly in the main tree, then re-dispatched the delta re-gate. Durable fix, not yet built (there or here): an architect-gate finding whose `file` is a target-repo rules path should be mechanically distinguishable (e.g. a structured `routing: "main-tree"` field) so a recovery scaffold can surface it as a distinct step instead of leaving the operator to infer the routing from prose. Recorded here purely so a future engine session recognizes the shape if it recurs. |
| KI-E144 | **Ported from a host-mount session — MAIN-DRIFT FALSE-POSITIVE CROSS-CONTAMINATION, and this repo carried the exact same latent bug.** Live on the origin host, cycle 93: a 6-item batch spanning five UNRELATED services all FAILED identically on "main-tree contamination (KI-E109)". Root cause: `driver.mjs main-check <id>` prints up to TWO independent things — a per-item drift line scoped to that item's own declared files, and a SEPARATE, ALWAYS-appended, repo-wide sweep (KI-E89's "unclaimed" line) that fires on ANY stray path anywhere in the repo regardless of which id was asked about. The KI-E50 runner brief instructs pasting the whole `main-check` output verbatim into the item's own note; `factory.js`'s KI-E109 fail-gate then matched the bare substring `/⚠\s*MAIN-DRIFT/` — unable to distinguish "MY declared files drifted" from "an unrelated path exists somewhere else that nobody has claimed." **Confirmed this repo has the identical vulnerable regex at the identical call site** (`factory.js`'s `failLaneOnMainDrift` gate, and this repo's own `cmdMainCheck` prints the same two-line shape including the literal `MAIN-DRIFT unclaimed (KI-E89)` sweep) — a real, live, not-yet-triggered latent defect here too, only reachable when the OFF-by-default `failLaneOnMainDrift` policy is turned on, same as on the origin host. Fixed identically: the regex is anchored to the item's own id in the exact format `cmdMainCheck` only ever prints for a real per-item hit; the literal token `unclaimed` can never match a real item id. The KI-E50 runner hint was rewritten to explain the two-line shape explicitly. Verification: two new exec-smoke scenarios (an innocent item whose own note carries an unrelated item's real drift line AND the unclaimed sweep — must NOT fail; a guilty item whose own id's line is present — must still fail). Superseded in the same pass by KI-E144B below — see that entry for the final mechanism. Selftest 1405 → 1410 (E144); folded into KI-E144B's own count below since E144B replaces this fix's detection mechanism in the same file region. |
| KI-E144B | **Ported from a host-mount session — the KI-E144 id-anchored regex above was STILL prose-parsing, and broke a different way on the very next retry (same day, origin host).** Live there: a runner, re-run under the KI-E144 fix, correctly determined its own main-check was clean and wrote into its note something like `"No ⚠ MAIN-DRIFT <id> ( line... printed"` — quoting the exact marker format specifically to PROVE its absence. The anchored regex has no concept of negation: it matched the runner's own quoted disproof as if it were the real thing, and failed an item whose runner had explicitly, correctly said nothing was wrong. Owner feedback on seeing this pattern recur, verbatim: *"is that a good idea to do the regex find? this sounds unreliable. try to find a better way."* **Durable fix: stop parsing prose for a verdict at all.** `VERIFY_SCHEMA` gains `mainDriftOwnFiles` (boolean) — the runner's own closed yes/no answer to "did main-check print a real per-item drift line for MY OWN id" — set directly, never inferred from wording. `factory.js`'s KI-E109 gate now reads `verify.mainDriftOwnFiles === true` and nothing else; the id-anchored regex is retired, not layered on top. **General lesson, applied here as it was on the origin host**: grepping a schema-CONSTRAINED enum field (`build`/`targetedTest`, pattern `^(pass|fail)`) is safe — the model cannot express ambiguity in a field the structured-output validator already forces to one of two literal values. Grepping a FREE-TEXT field (`note`, `evidence`) for a marker is never safe, because the same vocabulary can express both "X happened" and "X did NOT happen," and a sufficiently careful, transparent runner is exactly the one MOST likely to write the negation form — the failure mode is triggered BY thoroughness, not carelessness. **NOT ported**: the origin fix's sibling `workreeGuardRefused` boolean (the same fix applied to a post-amend-verify hint) — that mechanism (KI-E125/KI-E136's post-amend re-verify) does not exist in this repo yet, so there is nothing to wire the field into; `opencode/schemas.mjs`'s `VERIFY_SCHEMA` still gains `mainDriftOwnFiles` for schema-parity (canon-only in behavior, per the established KI-E139/E141 schema-parity-only convention). Verification: three exec-smoke scenarios covering the full evolution — cross-contamination with `mainDriftOwnFiles=false` in a note that still contains unrelated drift text (must NOT fail), a genuine own-drift with the field `true` (must fail), and the exact negation shape reproduced directly (prose disproving drift, field correctly `false` — must NOT fail, unlike the live incident). Selftest 1405 → 1410 passed, 0 failed (KI-E144 + KI-E144B together); opencode 189/0 unaffected. |

| KI-E146 | **Ported from a host-mount session.** The KI-E89 unclaimed-drift sweep only ran when an operator remembered to invoke `main-check` by hand — `fold`, which runs every single cycle without fail, never looked for this class of contamination at all. Root cause of the origin host's KI-E144 cascade: a stray test file left by an earlier item sat undetected through that cycle's own fold — the per-item KI-L65 snapshot check only ever validates paths the CLAIMING item's own `files[]` declared, so a file nobody's snapshot claims is structurally invisible to it — then poisoned an entire separate, unrelated batch's main-check sweep the NEXT cycle. A full cycle of silence sat in between, during which nobody had a reason to run `main-check` by hand. **Fix:** `cmdFold` now also runs the KI-E89 `unclaimedMainDrift()` sweep on every fold, aggregating `claimedPaths` fresh from every item's own `main-snapshot.json` (not just the one item a manual `main-check <id>` call would have been scoped to) — read-only, WARN-only, wrapped in try/catch so a missing/corrupt items dir can never fail or half-apply a fold, same posture as every other fold-time detection aid already in this function. This surfaces the gap at the ONE checkpoint that always runs instead of the one that sometimes does. Deliberately still NEVER auto-repaired: `mainguard.mjs`'s own header comment is explicit about why — an unclaimed path could be agent contamination OR an operator's own unrelated work-in-progress sitting in the same tree, and there is no snapshot to prove which; auto-deleting the wrong guess would destroy real, uncommitted human work. Selftest 1410 → 1421 (11 new source-text-pin assertions, matching the same rigor already established for `cmdMainCheck`'s own KI-E89 wiring — a filesystem-fixture integration test was judged disproportionate for this class of never-blocking diagnostic aid). Confirmed KI-E89/`unclaimedMainDrift`/`dirtyMainPaths`/`cmdMainCheck` all already exist in this repo (ported earlier, KI-E86..E89) — no prerequisite gap. |
| KI-E147 | **Ported from a host-mount session — an operational finding, not a code change; recorded so a future contamination triage on THIS repo doesn't fall into the same trap.** On the origin host, KI-E146 confirmed its own value within minutes of shipping — and its FIRST live catch exposed a diagnostic trap: raw file mtime is NOT a reliable signal for which copy (main vs. worktree) is authoritative when contamination strikes an INTERMEDIATE pipeline step. A stray test file's mtime looked, by mtime alone, like the OLDER of two copies (main and worktree both fell "today", main's slightly earlier) — the natural assumption ("main gets an earlier draft, worktree gets the later, correct one") would have discarded it as safely superseded. Cross-referencing the item's OWN checkpoint artifacts proved the opposite: an earlier-stage checkpoint (e.g. `test.json`) was stale from an early attempt, while later-stage checkpoints (`verify.json`/gate-review files) were fresh from today — meaning the worktree's file had not been touched since the early attempt, and the real, later edit had landed in the main tree instead and never reached the worktree at all. A concrete instance of the still-architecturally-unsolved main-tree/worktree contamination mechanism (the origin repo's KI-E133/E136), striking ONE file at a mid-pipeline step while a LATER step in the same attempt correctly wrote the item's other touched file to the worktree — so the worktree ended up internally inconsistent: one file legitimately fresh, the other silently stale, with nothing about the worktree's own state flagging the split. **Lesson, applicable here too:** diagnosing which copy is authoritative requires cross-referencing the item's own checkpoint-artifact timestamps (`plan.json`/`test.json`/`fix.json`/`verify.json`/gate-review files), never a raw main-vs-worktree mtime comparison alone. No selftest applies — this is an investigative technique for a human/operator session, not a code path. |

| KI-E148 | **Ported from a host-mount session — documentation only, no code; a real, deliberately UNFIXED gap in KI-E146's claimed-path check, disclosed here rather than silently absorbed by porting E146 as if it were complete.** KI-E146's "claimed" check treats a path as accounted for if ANY item, ever, once snapshotted it — not whether the item that ACTUALLY dirtied it just now is among the claimants. Live on the origin host: one item cross-contaminated 3 shared k8s secrets files with its own config; the sweep caught the first (correctly unclaimed) but silently passed the other two, because FIVE unrelated, older items had legitimately claimed those exact same shared-secrets paths in their OWN past snapshots — a plausible situation any time several graph items share a config file across the whole ledger's lifetime. `claimedPaths` is a UNION with no provenance check: it answers "has anyone ever had a reason to touch this path", not "does today's dirty content belong to one of its claimants". **Not fixed there, and not fixed here — flagged, not patched.** A real fix needs per-path provenance (e.g., hash the claiming item's OWN copy of the path at ITS claim time and compare against the CURRENT dirty content, not just presence-in-any-snapshot), which is a real design task, not a one-line tweak — and is exactly the class of problem `isolation: 'worktree'` (KI-E149, ported below) would make structurally moot rather than requiring an ever-more-precise claim ledger. Recorded as a known, narrower gap in KI-E146 rather than deepened into a bigger detection mechanism. |

| KI-E149 | **Ported from a host-mount session — the single most significant standing gap this registry named (KI-E133/E136's own words there, "sandboxing what a subagent's own tool calls can reach is outside what a prompt-level or driver.mjs-level change can close") turned out to be wrong, for one class of the problem.** Every main-tree-contamination fix through KI-E148 above was DETECTION after the fact (main-check, the KI-E89/E146 unclaimed sweep) because nothing could PREVENT a subagent's Edit/Write call from landing at the wrong absolute path in the first place. On the origin host, prompted by the owner asking whether Claude Code hooks could help, research established the Agent/Workflow tool's `isolation:'worktree'` option does exactly this at the TOOL layer, with a scope narrower and stranger than hoped, confirmed via four independent LIVE tests against a real isolated agent: (1) an Edit/Write call targeting the shared main checkout — the EXACT failure mode behind the origin host's own KI-E133/E136/E144/E147/E148 incidents — is mechanically REJECTED before any write happens, with an explicit error naming "the shared-checkout path"; (2) an Edit/Write call targeting a DIFFERENT, unrelated git worktree of the same repo — including the very worktree the isolated agent is supposed to treat as its own workspace, `state/worktrees/<id>/`, which is a real, separate `git worktree` and therefore NOT "the shared checkout" — succeeds completely normally, so the factory's existing per-item worktree model needs ZERO redesign; (3) Read/Grep/ls anywhere, including the main checkout, are completely unaffected — full cross-service investigative context-gathering survives untouched; (4) Bash-tool writes (`tee`, `>` redirect, heredocs) to ANY path, including the main checkout, are ALSO completely unaffected — the guard is specific to the Edit/Write TOOLS, not a filesystem sandbox. (4) is a real, disclosed limitation, not a false sense of completeness: the origin host's own KI-E133 incident was a Bash `git reset`/`clean`, a vector this fix does nothing for. **Fix:** a new `needsWriteIsolation(role)` helper in `factory.js` scopes `opts.isolation = 'worktree'` to exactly the two roles that mutate a worktree's tracked SOURCE — `fixer` (and every bounded-amend call that reuses that role literal from acceptance-scan/plan-commitment-scan/docsync-scan/registration-drift-scan/leftover-scan/ledger-anchor-scan) and `test-author` — wired into BOTH the primary and the KI-D10 fallback-model `opts` objects in `call()` (a fix that only isolated the primary path would silently stop protecting the fallback the moment a primary model degraded). A new role-conditional prompt block (mirroring the existing KI-D7 pattern) tells the isolated agent what changed: Edit/Write now only reach its own worktree; the ARTIFACTS DIR instruction in the shared cache-strategic prefix (write your state/items artifacts EXACTLY here) names a path OUTSIDE that worktree, so anything written there now needs Bash (tee/redirect) instead of Edit/Write, which is a deliberate use of finding (4) as the SOLUTION rather than a gap. An explicit host-policy escape hatch (`A.policies.isolateWorktreeWrites === false`) is the only way OFF; unset/missing defaults ON, matching a safety feature's correct default. **This repo's own `_execsmoke.mjs` did not yet capture composed-prompt text at all (the origin host's KI-E117 feature was never ported here)** — extended it to do so (a small, purely-additive, zero-existing-test-risk change) so the ported behavioral tests below can actually verify the isolated agent SEES the hint, rather than silently checking `undefined`. Verification: 5 source-text pins (helper exists, correctly scoped, escape-hatch condition present, both opts sites wired, hint text present) plus 9 real exec-smoke behavioral checks against the live pipeline (every real fixer/test-author call in a codeChange smoke lane carries `isolation:'worktree'` AND sees the hint; every planner/gate/review call in the SAME lane carries neither; the escape-hatch policy actually turns both off). Selftest 1421 → 1435. |

| KI-E150 | **Ported from a host-mount session — precise per-item token attribution.** Only a run-wide token total existed (`budget.spent()`, KI-E23) at cycle end; answering "how many tokens did THIS item cost" was impossible below the whole cycle. Adds an atomic-claim design (a shared `lastSpentGlobal` checkpoint, advanced synchronously with no `await` between read and write, safe under `Promise.all` concurrency) that attributes `budget.spent()`'s marginal increase to the item whose call produced it. Two real bugs found on the origin host via a deliberately discriminating exec-smoke test (every mocked call contributes an identical, known token amount, so each item's own total is exactly verifiable rather than "the grand total roughly adds up"): claiming only on eventual `call()` success leaked a retry-exhausted call's real spend onto whichever call claimed next (fixed via a new `tryAgent` `onAttempt` callback, fired on both the success and the KI-D9 throw/catch paths); the checkpoint-writer calls (deliberately excluded from `tokensUsed` as bookkeeping overhead) never claimed at all, leaking their spend the same way (fixed with `claimTokensSilently()` — advances the shared checkpoint without attributing it to any item). A third, smaller mismatch on the origin host turned out to be a test-harness artifact: the exec-smoke mock resolved every call synchronously, letting `makeLimiter`'s `pump()` batch a whole concurrency-window's calls before any of its claims fired — fixed there by awaiting `agentOverride` in `_execsmoke.mjs` and giving the test's own override a real macrotask-crossing delay. **Ported with one confirmed adaptation, not a blind copy — since resolved.** At the time of this port, this repo had only `checkpointResult`/KI-L40 — `checkpointProgress`/KI-E137 had not been ported yet, so `claimTokensSilently` was wired into the ONE checkpoint call site that existed rather than two, and the ported test's checkpoint-label exclusion predicate checked for both the `:progress:` and `:checkpoint` label shapes even though nothing produced the `:progress:` one here. **KI-E137 has since landed** (see its own entry below) and its `checkpointProgress` call site was wired with `claimTokensSilently` from the moment it was added — the gap this note originally disclosed is closed; the `:progress:` half of the exclusion predicate is no longer dead weight. `ledger.mjs` accumulates `tokensUsed` across relaunch attempts (same posture as `cost`); `driver.mjs` threads it through `item_folded` telemetry and adds a tokens-per-closed-item stat plus a top-10-by-tokens report table. Verification: real behavioral exec-smoke coverage (not source-text pins alone) against this repo's own `smokeBatch()` fixture, ported byte-for-byte from the origin's already-debugged test and passing on the FIRST run here (no adaptation needed for the core token-accounting assertions, only for the checkpoint-site count noted above). Selftest 1435 → 1456 passed, 0 failed (re-run 3× to confirm the timer-based harness fix isn't flaky in this repo either). Opencode 189/0 unaffected. |

| KI-E137 | **Ported from a host-mount session — INCREMENTAL PROGRESS CHECKPOINTING.** `checkpointResult` (KI-L40) persists only the TERMINAL outcome, chained via `.then()` strictly AFTER `runItem()` fully resolves, so a Workflow killed anywhere in the pipeline between `fix` and `integrate` (10-25+ agent calls: the whole pre-band scan chain, then the entire opus gate band, then refute+re-audit) left `reconstruct` with NOTHING — discarding every already-paid-for stage no matter how close to done it was. **Fix:** `factory.js` gains `checkpointProgress(res, stage, extra)`, a sibling of `checkpointResult` (same KI-D8 provenance framing, same `CHECKPOINT_SCHEMA` write contract, a SEPARATE `state/items/<id>/progress.json` file so the terminal-checkpoint contract `reconstruct`/`fold` depend on is untouched), called at 4 phase boundaries: `post-verify` (before the pre-band scan chain begins), `post-preband` (after it, before the gate band), `post-gates` (after `GATED`, before refute+re-audit), `post-reaudit` (after `REAUDITED`, before integrate). `driver.mjs`'s `cmdResume` (per-item inflight line) and `cmdReconstruct` (both the zero-results and partial-results missing-item branches) now read it via the new `lib/progress-checkpoint.mjs` (`readProgressCheckpoint` + `summarizeProgress`, resultId-fenced exactly like `result.json`'s KI-B4 fold-idempotency check) and print the reached stage + gate tally + what remains, instead of a bare "no checkpoint — must re-run". Deliberately does NOT (yet) feed relaunch reuse — `progress.json` is advisory/forensic only in this change, same posture `lib/prior-attempt.mjs` already documents for why `verify` onward stays unreused. **Scoped narrower than the origin commit, deliberately:** the origin repo shipped E137 and KI-E139 (content-hash-fenced gate-band reuse) together in one commit, since E139's `post-preband` checkpoint carries a `reviewPackHash` E137 alone has no use for. This port lands E137 ALONE — the `post-preband` call here is the plain 2-arg form, with no hash argument, since there is no `PACK_HASH_SCHEMA`/pack-hash probe here yet to produce one; KI-E139 is a deliberately separate, later port. The `post-preband` checkpoint is also placed one scan earlier than the origin's exact spot, since this repo's own pre-band scan chain currently ends at `ledger-anchor-scan` (KI-E91) — it has no root-cause-touch-probe (KI-E104's PROBE form) yet to checkpoint after; this remains the correct "last thing before the gate band" position for this repo's actual pipeline. `claimTokensSilently` (KI-E150, already present in this repo) is wired into the new `checkpointProgress` call site from the moment it was added — no separate follow-up needed. Verification: real exec-smoke behavioral coverage over this repo's own 6-item `smokeBatch()` fixture — each of the 4 checkpoints fires exactly once per lane, AND, for one representative FULL-band lane, the four checkpoint calls interleave with the real pipeline calls in true RUNTIME order (post-verify < post-preband < gate band < po gate < post-gates < refute/re-audit < post-reaudit < integrator), passing on the FIRST run with no role-name adjustments needed. Plus full pure-function coverage of the read side (absent/fresh/stale-cycle/id-mismatch/malformed/unrecognized-stage-tag, all never-throw). Selftest 1456 → 1482 passed, 0 failed; opencode 189/0 unaffected (this change touches only `factory.js`/`driver.mjs`/a new `lib/` file, not `opencode/*`). |

| KI-E138 | **N/A in this repo — the trigger condition this fix addresses on the origin host never occurred here.** On the origin host, KI-E138 fixed a self-contradiction that KI-E136's own commit introduced: it added an `opencode/stage-parity.mjs` `UNPORTED` entry (`probe:post-amend-verify`, a synthetic gate-KEY label, never a literal `call(...)` role argument) without updating the sibling KI-E112 assertion that hard-pinned the set to `[]`, AND that entry was structurally undetectable by the existing port-parity extractor (`extractFactoryRoles` can only ever find literal `call('roleName', ...)` arguments, never a hand-assigned `res.gates['...']` string). Since this repo never received KI-E136 (post-amend-verify doesn't exist here — see that entry above), no such colon-qualified, extractor-invisible `UNPORTED` entry was ever introduced, so there is no self-contradiction to fix and no `extractFactoryGateKeys`/colon-qualified-routing mechanism to port. **KI-E139 below independently reopens the SAME class of question** (does adding an UNPORTED entry contradict a hard-pinned KI-E112 assertion?) for a DIFFERENT reason — `pack-hash-probe`, a genuine `call('pack-hash-probe', ...)` role the existing (non-colon-qualified) extractor already finds correctly — so the simpler fix (repin the KI-E112 assertion to the new expected set) was sufficient there without needing E138's fuller mechanism. Recorded here so a future port doesn't waste time trying to port a fix for a bug that provably can't occur in this repo's current state — revisit if a future colon-qualified gate-key entry (like the origin's `probe:post-amend-verify`) is ever needed. |
| KI-E139 | **Ported from a host-mount session — GATE-BAND REUSE, content-hash-fenced.** Building on KI-E137's checkpoints: on a relaunch, if a PRIOR attempt's `progress.json` proves the gate band already reached a fully-resolved `GATED` state (`post-gates` or `post-reaudit`) AND a FRESH hash of the JUST-regenerated `review-pack.md` matches the hash recorded at that prior `post-preband` checkpoint, the reviewable diff is provably byte-identical to what a full gate band already vetted — the recorded verdicts are adopted (`res.gateBandReused = true`, a visible telemetry flag) and the expensive opus gate band is skipped entirely. On ANY mismatch (a genuinely changed diff, a stale/corrupt checkpoint, or no eligible checkpoint at all) it falls through to a full, independent re-gate — byte-identical to pre-KI-E139 behaviour. The hash is never trusted from the prior attempt's own say-so; it is always re-derived, right now, against the live worktree via a new `pack-hash-probe` role (haiku, `PACK_HASH_SCHEMA`) — this is what lets the mechanism NOT weaken the "gates re-adjudicate the worktree on every relaunch" trust boundary: a skip only ever happens on mechanical proof, never an assumption. `factory.js`'s ENTIRE gate-band dispatch (role gates, method-flow reviews, dissent adjudication, PO gate) is wrapped in `if (!gatesReused) { ... }`; `methodFlows` is hoisted OUT of that block since the later Refute+Re-audit section also reads it, so it must exist regardless of which path ran. Wired into `cmdResume --reuse`, which now ALSO attaches a `progress.json` to `it.priorProgress` (id+cycle-fenced via `readProgressCheckpoint`, same convention as `result.json`'s KI-B4 fold-idempotency), alongside the pre-existing KI-E69 plan/test/fix attach. **Confirmed adaptation:** the origin repo ALSO wires this into a `cmdGroup`-based `KI-E121 CONTINUING` automatic re-claim-and-relaunch mechanism — that mechanism does not exist in this repo at all (this repo's `cmdGroup` only prints relaunch candidates for the operator to manually copy a command; there is no automatic re-claim to extend). `cmdResume --reuse` is the ONLY relaunch-reuse entry point this repo has, and it now carries the complete, correct KI-E139 wiring. New `pack-hash-probe` role declared `UNPORTED` in `opencode/stage-parity.mjs` (genuinely absent — this runtime's phase-driven state machine has no "prior attempt" concept to fast-forward from at all) with `PACK_HASH_SCHEMA` added to `opencode/schemas.mjs` for schema-parity; the KI-E112 hard-pin assertion is repinned from `eq(..., [])` to `eq(..., ['pack-hash-probe'])` in the SAME commit, per that assertion's own documented discipline (see KI-E138 above for why the fuller E138 mechanism wasn't needed for this repin). Verification: source-text pins (schema exists, reuse-eligibility condition, hash-comparison condition) plus 4 real exec-smoke behavioral scenarios proving ALL directions — a HIT (matching hash + eligible stage: zero gate-role calls dispatched, `gateBandReused===true`, the reused verdict lands on the final result, the item still reaches `CLOSED`), a hash MISMATCH (falls through to a full re-gate), a stage-too-early miss (`post-preband` only — falls through), and the pre-existing no-`priorProgress` baseline (already proven by KI-E137's own coverage, cited rather than re-proven). Selftest 1483 → 1496 passed, 0 failed (13 new assertions, matching the shape of the origin's own coverage); opencode 189 → 191 (schema-parity + UNPORTED-declaration checks). |

| KI-E140 | **Ported from a host-mount session — TASK-LIVENESS REMINDER.** A process mitigation for the still-unproven root-cause hypothesis behind main-tree contamination on the origin host: a stray/duplicate `runItem()` for the same id, from an incompletely-terminated PRIOR attempt whose task the controller never confirmed dead before relaunching into the SAME worktree path. The origin host verified live that a Workflow task does NOT appear to survive the session that spawned it — reassuring for the cross-session case, but the WITHIN-session case (a relaunch issued before a just-killed attempt's task is confirmed torn down) remains exactly the window that class of incident could not rule out, and the driver cannot check task liveness itself — only the controller session holds a `TaskOutput`/`TaskStop` handle. **Fix: a recording + surfacing mechanism, not a detector.** New `driver.mjs mark-launched --ids a,b,c --taskId <id> --runId <id>` writes `state/items/<id>/launch-meta.json` (atomic write, per-id skip-not-throw for a non-claimed id) — the controller runs this immediately after every real `Workflow` launch. `cmdResume`'s relaunch-candidate listing reads it back (mirroring KI-E41's MAIN-GUARD loop's exact shape and posture: warn loudly, per-id try/catch, never block) and prints the exact command to run (`TaskOutput <taskId> block:false`) before the relaunch line is copied. Best-effort by design: a missing `launch-meta.json` is silent, never a warning. **A real, live bug this port ALSO carried, caught and fixed in the SAME commit**: `launch-meta.json` (and, in the same pass, this repo's OWN `progress.json` from KI-E137 — a gap that had slipped through that earlier port uncaught) would otherwise be flagged by the pre-existing KI-E42 "non-canonical artifact" quarantine detector (`lib/telemetry.mjs`'s `CONTROL_ARTIFACTS` allowlist), meaning a real `resume --quarantine` run would sweep away the ONE file each mechanism exists to surface. Fixed by adding both filenames to `CONTROL_ARTIFACTS`. **Confirmed adaptation:** the origin host's reminder message also cites its own KI-E136 as "the leading, still-unproven, root-cause hypothesis" for the worktree-drift class this reminder guards against; that citation is dropped here since KI-E136 doesn't exist in this repo — the mechanism and its reasoning are otherwise unchanged. Verification: source-text coverage (matching this file's established convention for `driver.mjs`: it has no exports and a top-level `main()`, so no test spawns it as a subprocess) — `cmdMarkLaunched`'s no-op/skip/atomic-write/timestamp behavior, and the reader's silent-on-missing/exact-check-named/never-blocks posture, both pinned directly against the real source. Selftest 1496 → 1507 passed, 0 failed; opencode 191/0 unaffected (canon-only, no opencode relaunch-reuse concept to wire into). |

## Verified design properties (recorded so they are not re-investigated)

- The global agent limiter cannot deadlock — every `agent()` routes through one limiter; an
  item never holds a slot while awaiting another.
- Per-item results are isolated — a thrown item degrades to `FAILED`; siblings still
  complete and fold (the batch never rejects as a whole).
- A null/again-null gate result fails closed (`!== 'APPROVED'` → `FAILED`).
- `foldResults` is idempotent and self-heals a partial application (it re-walks from the
  current state rather than corrupting it).

## How to add an entry

Give it a stable `KI-<class><n>` id, one row stating the issue/constraint and its
mitigation or status, and — if it is a correctness fix — a matching assertion in
`_workflow/lib/_selftest.mjs`. Resolved entries stay (marked resolved), never deleted.
